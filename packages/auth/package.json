{
  "name": "@deepsight/auth",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Auth helpers — lint via app configs'",
    "lint:fix": "echo 'Auth helpers — lint via app configs'",
    "test": "echo 'Auth helpers — tested via app test suites'"
  },
  "dependencies": {
    "next": "16.1.6",
    "next-auth": "^5.0.0-beta.30"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "typescript": "^5"
  }
}
