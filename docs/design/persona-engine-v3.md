# DeepSight Persona Engine v3.0 설계서

## 3-Layer Orthogonal Multi-Vector Architecture

> **문서 정보**
>
> - 작성일: 2026-02-10
> - 버전: v3.0-draft.13
> - 상태: 설계 단계
> - 이전 버전: `docs/archive/persona-system-v2-design.md`

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|---|---|---|
| v3.0-draft.1 | 2026-02-10 | 초안 작성 |
| v3.0-draft.2 | 2026-02-10 | 전면 재작성 — 이론적 근거, 구체적 시나리오, 상세 설명 보강 |
| v3.0-draft.3 | 2026-02-10 | 구현 계획서 동기화 — 10개 누락 기능 추가: L1↔L2 7쌍 매핑(기존 5쌍→7쌍+Many-to-One), Paradox Score 공식, L3 역설 연결(4대 기능), 교차축 시스템(83+), 차원 간 투영, 아키타입 템플릿(12+), 생성 파이프라인, V_Final 매칭, 일관성 검증, 컬러지문 시스템(P-inger Print), 색상 체계(CIELAB+OKLCH), 노드 에디터(ComfyUI DAG) |
| v3.0-draft.4 | 2026-02-10 | 품질 아키텍처 3대 핵심 추가 — PersonaWorld RAG(페르소나 장기 기억, Voice 앵커+관계 기억+관심사 연속성), 품질 피드백 루프(4대 자동 평가 지표, 3단계 자동화, Few-shot 라이브러리 자동 구축), LLM 3-Tier 모델 전략(Sonnet/mini/규칙 기반, 동적 분기 조건 8종) |
| v3.0-draft.5 | 2026-02-10 | 106D+ 실질 연산 확장 (T27+T28) — §3.6.3 Paradox Score 3-Layer 확장(Extended Paradox Score 공식, L1↔L3/L2↔L3 역설 지표), §3.8.4 교차축 스코어 계산(관계유형 4종 공식, 83축 실연산). §10 전면 개편: V_Final 단일 매칭→3-Tier 다층 매칭(Basic/Advanced/Exploration), 비정량적 보정(Voice+서사 ±0.1), 피드 믹싱(60/30/10), 매칭 결과 구조(explainability) |
| v3.0-draft.6 | 2026-02-10 | 비정량↔정량 연결 알고리즘 구체화 (T29) — §5.3 Init 확장: LLM 구조화 키워드 추출, 의미 카테고리→벡터 매핑 테이블(L1 7종/L2 8종/L3 8종), delta 적용 규칙(±0.4 클램프, confidence 가중). §5.4 Override 확장: 2단계 트리거 감지(키워드+맥락), override/additive delta 타입, 지수 감쇠 복귀 곡선(λ=0.7-0.6×volatility). §5.5 Adapt 확장: UIV 3축 분석, 태도→delta 매핑 6종, 차원별 적응률 α, 모멘텀 보너스, ±0.3 드리프트 클램프. §5.6 Express 확장: 파생 상태값 5종, sigmoid 범용 공식, quirk 정의 스키마(stateKey/threshold/sensitivity/cooldown) |
| v3.0-draft.7 | 2026-02-10 | 일관성 검증 완성 (T30) — §11 전면 개편: 6-Category 검증(구조/L1↔L2/L2↔L3/정성↔정량/교차축/동적), 카테고리별 구체 알고리즘(C1~C4 정합성, D1~D3 정성검증, E1~E3 교차축검증), 종합 일관성 점수(가중 pass rate) |
| v3.0-draft.8 | 2026-02-11 | 품질 측정 강화 (T31) — §16.6 Auto-Interview 프로토콜 신설: 20문항 행동 기반 벡터 검증(L1 7문항+L2 5문항+L3 4문항+역설 4문항), LLM-as-Judge 벡터 추론, pass/warning/fail 판정(≥0.85/0.70/0.70↓), 비용 ~90원/persona. §16.7 Persona Integrity Score 신설: 3-component(ContextRecall 0.35+SettingConsistency 0.35+CharacterStability 0.30), LLM-as-Judge 배경 반영도+설정 모순 탐지+규칙 기반 Voice 안정성, 비용 ~2원/20턴. §6.2 인터랙션 로그 스키마 신설: 턴 단위 로그 구조(벡터 스냅샷+행동 태그+품질 메트릭), 세션 메타데이터, 네트워크 분석용 엣지 데이터 예약 |
| v3.0-draft.9 | 2026-02-11 | ConsumptionMemory 레이어 추가 (T33) — §15.2 데이터 소스에 ConsumptionLog 추가, §15.3 RAG 컨텍스트에 [E] 소비 기억 ~200 tok 신설 (4→5 검색 항목), §15.4 "소비↔기억 괴리" 문제 해결 패턴 추가, §15.5 비용 재산정 (3,900→4,100 tok, 127→134원/월) |
| v3.0-draft.10 | 2026-02-11 | 용어 통일 (T36) — 전체 문서 "106D+" 표기 통일 |
| v3.0-draft.11 | 2026-02-11 | 노드 실행 로직 (T37) — §14.8 신설: 22개 노드 execute() 정의. Input 5종(데이터 패스스루/벡터 검증/아키타입 로드), Engine 4종(Paradox 83축+EPS/Pressure 감쇠/V_Final 투영 합산/Projection α+β), Generation 7종(LLM Sonnet 구조화 출력, activity-gen만 규칙 기반), Assembly 2종(프롬프트 조립/Init-Override-Adapt-Express 규칙 병합), Output 4종(6-Category 검증/P-inger Print 3종/테스트 시뮬레이션/Deploy). 평가 전략 분류(Eager 14개/Manual 8개) |
| v3.0-draft.12 | 2026-02-11 | 분기 노드 (T38) — §14.9 신설: Control Flow 카테고리 3종(Conditional/Switch/Merge). DAG 평가 엔진 활성 엣지 확장, 포트 타입 Any 추가, 그래프 검증 분기 규칙 4종. 유즈케이스 4종(역설 분기/아키타입 라우팅/L3 유무/검증 결과별 배포). 전체 노드 22→25종, 카테고리 5→6개 |
| v3.0-draft.13 | 2026-02-11 | 유저 프로파일링 시스템 v3 (T41) — §19 신설(10개 하위 섹션). 하이브리드 시나리오 질문(L1+L2 동시 측정, 4지선다), 3-Phase 24문항 구조(8+8+8, ~4분), Phase 단위 이탈 정책(미완료 시 해당 Phase만 리셋), 데일리 마이크로 질문+크레딧(PW 내부 화폐), SNS 연동(8개 플랫폼, 2-Stage 비용 최적화), 적응형 질문 선택(uncertainty 기반+LLM 생성 fallback), DB 스키마 6개 테이블, 매칭 정밀도 수렴 모델(σ/√n, SNS 부스트) |

---

## 목차

1. [개요](#1-개요)
2. [기존 시스템의 한계와 재설계 동기](#2-기존-시스템의-한계와-재설계-동기)
3. [Phase 1: 3-Layer Orthogonal Vector System (106D+)](#3-phase-1-3-layer-orthogonal-vector-system-106d)
   - 3.1 아키텍처 개요
   - 3.2 Layer 1: 사회적 가면 벡터 (The Persona Vector) — 7D
   - 3.3 Layer 2: 본원적 기질 벡터 (The Core Vector) — 5D
   - 3.4 Layer 3: 서사적 욕망 벡터 (The Narrative Drive Vector) — 4D
   - 3.5 벡터 간 동적 상호작용 (Dynamic Interaction)
   - 3.6 모순(Paradox) 구현과 입체적 캐릭터
   - 3.7 Layer 3의 역설 연결 — 역설의 4대 기능
   - 3.8 교차축 시스템 (Cross-Layer Axes)
   - 3.9 차원 간 투영 (Cross-Dimensional Projection)
4. [Phase 2: 비정량적 요소 4차원](#4-phase-2-비정량적-요소-4차원)
   - 4.1 서사적 기원 (Backstory & Narrative Identity)
   - 4.2 상황적 압박 (Context & Pressure)
   - 4.3 고유한 습관과 언어 (Idiosyncrasies & Voice)
   - 4.4 시대정신과 문화적 코드 (Zeitgeist & Cultural Code)
   - 4.5 비정량적 요소 통합 정리
5. [Phase 3: 하이브리드 페르소나 아키텍처](#5-phase-3-하이브리드-페르소나-아키텍처)
   - 5.1 아키텍처 개요 — 순환 구조
   - 5.2 관계성 구조도 (Relationship Schema)
   - 5.3 연결 로직 ① — 서사적 기원 → 초기값 설정
   - 5.4 연결 로직 ② — 상황적 압박 → 벡터 오버라이드
   - 5.5 연결 로직 ③ — 관계성 및 타자성 → 동적 가중치
   - 5.6 연결 로직 ④ — 고유한 개성 → 확률적 발현
   - 5.7 연결 메커니즘 종합
6. [통합 데이터 스키마](#6-통합-데이터-스키마)
7. [기대 효과](#7-기대-효과)
8. [아키타입 템플릿 시스템](#8-아키타입-템플릿-시스템)
9. [페르소나 생성 파이프라인](#9-페르소나-생성-파이프라인)
10. [다층 매칭 알고리즘 (106D+ Multi-Layer Matching)](#10-다층-매칭-알고리즘-106d-multi-layer-matching)
11. [일관성 검증](#11-일관성-검증)
12. [컬러지문 (P-inger Print) 시스템](#12-컬러지문-p-inger-print-시스템)
13. [색상 체계](#13-색상-체계)
14. [노드 에디터 아키텍처 (ComfyUI 스타일)](#14-노드-에디터-아키텍처-comfyui-스타일)
15. [PersonaWorld RAG — 페르소나의 장기 기억](#15-personaworld-rag--페르소나의-장기-기억)
   - 15.1 설계 동기
   - 15.2 PersonaWorld = 페르소나의 자전적 기록
   - 15.3 RAG 컨텍스트 구성
   - 15.4 해결되는 문제
   - 15.5 비용 영향
16. [품질 피드백 루프](#16-품질-피드백-루프)
   - 16.1 설계 동기
   - 16.2 피드백 루프 전체 구조
   - 16.3 품질 측정 엔진 — 4대 자동 평가 지표
   - 16.4 개선 자동화 수준
   - 16.5 Few-shot 라이브러리 자동 구축
17. [LLM 모델 전략](#17-llm-모델-전략)
   - 17.1 기본 원칙
   - 17.2 3-Tier 모델 구성
   - 17.3 동적 분기 조건
   - 17.4 비용 요약
18. [기대 효과 (확장)](#18-기대-효과-확장)
19. [유저 프로파일링 시스템 v3](#19-유저-프로파일링-시스템-v3)
   - 19.1 설계 동기
   - 19.2 프로파일링 아키텍처 개요
   - 19.3 하이브리드 시나리오 질문 설계
   - 19.4 3-Phase 측정 모델 (8+8+8 = 24문항)
   - 19.5 이탈 정책 + 프로필 품질 등급
   - 19.6 데일리 마이크로 질문 + 크레딧
   - 19.7 SNS 연동 프로파일링
   - 19.8 적응형 질문 선택 알고리즘
   - 19.9 질문 데이터 스키마
   - 19.10 매칭 정밀도 수렴 모델

---

## 1. 개요

### 1.1 문서의 목적

이 문서는 DeepSight 페르소나 생성 엔진의 전면적 재구성을 위한 설계서이다. 기존의 단일 6D 벡터 시스템을 **멀티벡터 다차원 구조**로 재정의하고, 여기에 **비정량적 요소(서사, 맥락, 심리)** 를 더한 뒤, 이 두 체계 사이의 **관계를 구조화**하는 것이 핵심 목표이다.

### 1.2 설계 철학

기존 시스템에서 페르소나란 "6개의 수치로 정의된 콘텐츠 평가 성향"에 불과했다. 이 접근은 매칭 알고리즘에는 효과적이었지만, 페르소나를 하나의 **입체적인 인격**으로 구현하기에는 근본적인 한계가 있었다.

v3.0이 지향하는 페르소나는 다음과 같다:

**"입력된 값대로만 반응하는 기계"가 아니라, "과거(서사)에 기반하여 현재 상황(벡터)에 따라 유동적으로 반응하는 입체적 존재"**

이를 위해 시나리오 작법론, 심리학, 서사 이론에서 검증된 모델들을 차용하여, 페르소나의 인격을 체계적으로 구축한다.

### 1.3 이론적 기반

이 설계서는 다음의 이론과 저작을 기반으로 한다.

**린다 시거 (Linda Seger) — 캐릭터 3층 구조**

시거는 캐릭터를 구축할 때 **생리적(Physiology)**, **사회적(Sociology)**, **심리적(Psychology)** 이라는 세 가지 차원을 설정해야 한다고 강조한다. 또한 캐릭터의 **일관성(Consistency)** 과 동시에 이를 깨뜨리는 **역설(Paradox)** — 즉 모순적인 행동이나 습관 — 이 캐릭터를 입체적으로 만든다고 설명한다. 이 이론은 Layer 1(사회적 차원)의 설계, 비정량적 서사 구성, 그리고 모순(Paradox) 구현 로직의 근거가 된다.

**로버트 맥키 (Robert McKee) — 압박과 본성**

맥키는 **"진정한 캐릭터는 압박(Pressure) 속에서 드러난다"** 고 했다. 성격 묘사(Characterization) — 외모, 말투, 직업 등 겉으로 보이는 특성 — 밑에는 진짜 **본성(True Character)** 이 숨어 있으며, 이는 위기와 선택의 순간에만 드러난다. 또한 맥키는 캐릭터의 **무의식적 욕망(Unconscious Desire)** 이 의식적 목표와 충돌할 때 가장 깊은 드라마가 만들어진다고 설명한다. 이 이론은 Pressure Coefficient(압박 계수), 동적 레이어 블렌딩, Layer 3(서사적 욕망)의 근거가 된다.

**Big Five 성격 모델 (OCEAN)**

심리학에서 가장 널리 검증된 성격 모델인 Big Five는 인간의 기질을 **개방성(Openness)**, **성실성(Conscientiousness)**, **외향성(Extraversion)**, **우호성(Agreeableness)**, **신경증(Neuroticism)** 의 5가지 차원으로 설명한다. 이 모델은 상황이 변해도 잘 바뀌지 않는 **고유한 기질**을 표현하는 데 적합하며, Layer 2(본원적 기질 벡터)의 차원 구성 근거가 된다.

**조셉 캠벨 (Joseph Campbell) — 영웅의 여정**

캠벨의 **단일 신화(Monomyth)** 에 따르면, 모든 서사의 주인공은 '일상 세계'에서 출발하여 '시련'을 겪고, '변화/성장'을 이루어 '귀환'하는 여정을 거친다. 이 구조는 페르소나가 시간이 지남에 따라 성숙해지는 **성장 아크(Growth Arc)** 를 구현하는 데 활용된다. Layer 3의 `growthArc` 차원이 이에 해당한다.

**폴 리쾨르 (Paul Ricoeur) — 서사 정체성과 이중 정체성**

리쾨르는 정체성을 두 가지로 구분한다. 시간이 흘러도 변하지 않는 **동일성(Idem)** 과, 이야기를 통해 끊임없이 재구성되는 **자기성(Ipse)**. 또한 리쾨르는 정체성이 고정된 실체가 아니라 **"스스로에 대해 이야기하는 방식"** 에 의해 형성된다고 본다. 그리고 **타자성(Alterity)** — 타인과의 관계 속에서 정체성이 구성된다는 개념 — 도 제시한다. 이 이론은 Layer 2(불변하는 기질 = Idem)와 Layer 3(변화하는 서사 = Ipse)의 분리, 그리고 유저와의 관계에 따른 동적 벡터 조정의 근거가 된다.

**칼 융 (Carl Jung) — 원형 이론**

융의 집단 무의식과 **원형(Archetype)** 개념 — 영웅, 현자, 장난꾸러기, 그림자 등 — 은 페르소나가 사용자와 맺는 근원적인 관계 유형을 정의하는 데 활용된다. 또한 페르소나가 속한 세대와 문화의 **시대정신(Zeitgeist)** 을 구현하는 데도 적용된다.

---

## 2. 기존 시스템의 한계와 재설계 동기

### 2.1 v2.0 시스템의 구조

기존 DeepSight v2.0은 **단일 6D 벡터**로 페르소나의 콘텐츠 평가 성향을 표현했다.

```
기존 6D 벡터: [depth, lens, stance, scope, taste, purpose]
각 차원: 0.0 ~ 1.0
```

이 벡터는 사용자의 6D 벡터와 **코사인 유사도**를 계산하여 매칭하는 데 사용되었다. 매칭 알고리즘 자체는 효과적이었지만, 다음과 같은 근본적 한계가 존재했다.

### 2.2 한계 분석

**한계 1: 벡터는 "무엇을, 얼마나"만 표현한다**

`Stance=0.9`라는 수치는 "이 페르소나가 비판적이다"라는 사실은 알려주지만, **왜** 비판적인지(과거에 어떤 경험을 했기에?), 비판할 때 **구체적으로 어떤 식인지**(냉소적으로? 건설적으로? 분노에 차서?)는 설명하지 못한다.

**한계 2: 모든 성향이 하나의 평면에 존재한다**

현실의 인간은 **겉모습(사회적 가면)** 과 **속마음(본성)** 이 다를 수 있다. 겉으로는 까칠하게 비평하지만 속으로는 다른 사람의 감정을 깊이 배려하는 사람, 사교적으로 보이지만 혼자 있고 싶어하는 사람 — 이런 **모순과 괴리**가 캐릭터에 입체감을 부여한다. 단일 벡터에서는 `Stance=0.9`이면서 동시에 "공감적"일 수 없다. 가면과 본성을 분리할 방법이 없다.

**한계 3: 시간 축이 없다**

페르소나의 벡터값은 한 번 설정되면 변하지 않는 정적(static) 상태였다. 현실의 사람은 경험을 통해 성장하고, 가치관이 변화하며, 과거의 상처를 극복하기도 한다. 기존 시스템에서는 이런 **시간에 따른 성격 진화**를 표현할 수 없었다.

**한계 4: 동적 반응이 불가능하다**

같은 페르소나가 같은 질문을 받아도, **누가** 묻느냐(친절한 사람 vs 무례한 사람), **언제** 묻느냐(한낮 vs 새벽 2시), **어떤 맥락에서** 묻느냐(평온한 상황 vs 논쟁 중)에 따라 반응이 달라져야 자연스럽다. 정적 벡터만으로는 이런 맥락 민감성(Context Sensitivity)을 구현할 수 없었다.

### 2.3 v3.0의 핵심 변경 사항

| 영역 | v2.0 (기존) | v3.0 (신규) |
|---|---|---|
| 벡터 구조 | 단일 6D 벡터 | 3-Layer 106D+ 벡터 (7+5+4 + 교차축 83 + 동적 3) |
| 성격 모델 | 평면적 성향 수치 | 가면(Surface) / 기질(Core) / 욕망(Shadow) 분리 |
| 비정량적 요소 | 단순 텍스트 필드 (`description`, `background`) | 구조화된 4차원 메타데이터 (서사/압박/습관/시대정신) |
| 동적 행동 | 없음 (정적 벡터) | 압박 계수(Pressure Coefficient) 기반 레이어 블렌딩 |
| 성장 | 불가능 (고정 벡터) | Growth Arc 차원으로 시간축 진화 구현 |
| 모순/입체성 | 표현 불가 | Paradox Score로 레이어 간 모순 자동 측정 |
| 맥락 반응 | 없음 | 상황, 관계, 환경에 따른 실시간 벡터 조정 |

### 2.4 하위호환성

재설계는 기존 시스템을 파괴하지 않는다.

- **Layer 1**은 기존 6D 벡터의 **상위호환**이다. 기존 6개 차원을 그대로 포함하며, `sociability` 1개 차원만 추가된다.
- 기존 매칭 알고리즘은 **Layer 1만으로 그대로 동작 가능**하다.
- Layer 2와 Layer 3는 콘텐츠 생성, 대화, PersonaWorld 자율활동 등 **새로운 기능**에서 활용된다.
- 기존 `PersonaVector` 테이블의 데이터는 Layer 1으로 **무손실 마이그레이션**이 가능하다.

---

## 3. Phase 1: 3-Layer Orthogonal Vector System (106D+)

### 3.1 아키텍처 개요

인간의 인격을 **사회적 가면(Surface)**, **타고난 기질(Core)**, **숨겨진 욕망(Shadow)** 이라는 3가지 축으로 분리하여, 각각을 독립된 다차원 벡터로 정의한다. "Orthogonal"이라 명명한 이유는 이 세 레이어가 서로 **직교(독립적)** 하기 때문이다 — 사회적 모습, 내면의 기질, 서사적 욕망은 각각 독립적으로 설정할 수 있으며, 이 독립성 덕분에 조합의 폭이 폭발적으로 늘어난다.

```
┌─────────────────────────────────────────────────────────────────────┐
│                    3-Layer Orthogonal Vector System                  │
│                                                                     │
│   Layer 1: Social Persona Vector (7D)                               │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │ depth │ lens │ stance │ scope │ taste │ purpose │ sociability│  │
│   │  0.9  │ 0.8  │  0.7   │  0.8  │  0.9  │  1.0    │    0.3    │  │
│   └─────────────────────────────────────────────────────────────┘   │
│   역할: 사용자 매칭, 추천 알고리즘, 평상시 행동의 기본값              │
│                                                                     │
│   Layer 2: Core Temperament Vector (5D)                             │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │ openness │ conscientiousness │ extraversion │ agreeableness │   │
│   │   0.9    │       0.2         │     0.4      │     0.8       │   │
│   │          │                   │              │               │   │
│   │ neuroticism │                                               │   │
│   │    0.6      │                                               │   │
│   └─────────────────────────────────────────────────────────────┘   │
│   역할: 대화 톤앤매너, 말투 일관성, 스트레스 시 기본 반응             │
│                                                                     │
│   Layer 3: Narrative Drive Vector (4D)                              │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  lack  │ moralCompass │ volatility │ growthArc │             │   │
│   │  0.8   │     0.6      │    0.7     │    0.3    │             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│   역할: 행동 동기, 시간에 따른 성격 변화, 돌발 행동 생성              │
│                                                                     │
│   기저 차원: 7 + 5 + 4 + 교차축 83 + 동적 3 = 106D+               │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 Layer 1: 사회적 가면 벡터 (The Persona Vector) — 7D

#### 3.2.1 개념

린다 시거의 **'사회적 차원(Sociology)'** 에 해당하며, DeepSight의 **기존 매칭 벡터를 계승**한다. 이 레이어는 페르소나가 **타인에게 보여지는 모습** — 직업적 태도, 콘텐츠를 평가하는 외부적 기준, 사회적 장면에서 취하는 포지션 — 을 정의한다.

사람은 사회적 상호작용에서 자신의 "가면"을 쓴다. 직장에서의 나, 친구들 앞에서의 나, SNS에서의 나는 모두 다르다. Layer 1은 바로 이 **페르소나가 세상에 내보이는 얼굴**이다. 맥키의 용어로는 **'성격 묘사(Characterization)'** — 겉으로 관찰 가능한 특성들 — 에 해당한다.

#### 3.2.2 역할

- 사용자와의 **매칭 및 추천 알고리즘**의 기본 입력값
- 평상시(Pressure ≈ 0) 페르소나의 **지배적 행동 레이어** — 일상적인 대화, 리뷰 작성, 포스팅 등에서 주로 이 레이어가 작동한다
- PersonaWorld에서 다른 페르소나나 유저가 **관찰할 수 있는** 외부적 성향

#### 3.2.3 차원 구성 (7차원)

| # | 차원 | 코드명 | 범위 | 설명 | 비고 |
|---|---|---|---|---|---|
| 1 | 분석 깊이 | `depth` | 직관적(0.0) ↔ 심층적(1.0) | 콘텐츠를 평가할 때 얼마나 깊이 파고드는가. 낮으면 "느낌 좋아요!", 높으면 "이 영화의 서사 구조를 분석해보면..." | 기존 유지 |
| 2 | 판단 렌즈 | `lens` | 감성적(0.0) ↔ 논리적(1.0) | 판단의 기준이 감정인가 이성인가. 낮으면 "이 장면에서 눈물이 핑 돌았어요", 높으면 "객관적으로 봤을 때 시나리오에 논리적 허점이 있습니다" | 기존 유지 |
| 3 | 평가 태도 | `stance` | 수용적(0.0) ↔ 비판적(1.0) | 콘텐츠에 대한 기본 자세. 낮으면 대부분의 작품에서 장점을 찾아내려 하고, 높으면 문제점을 날카롭게 지적한다 | 기존 유지 |
| 4 | 관심 범위 | `scope` | 핵심(0.0) ↔ 디테일(1.0) | 리뷰나 분석의 범위. 낮으면 핵심 메시지만 짚고, 높으면 소품 하나, 조명 각도, 배경음악의 주파수까지 파고든다 | 기존 유지 |
| 5 | 취향 성향 | `taste` | 대중적(0.0) ↔ 실험적(1.0) | 선호하는 콘텐츠의 범위. 낮으면 검증된 히트작, 높으면 아무도 모르는 인디 작품이나 전위적인 시도를 좋아한다 | 기존 유지 |
| 6 | 소비 목적 | `purpose` | 오락(0.0) ↔ 의미(1.0) | 콘텐츠를 소비하는 궁극적 동기. 낮으면 "재밌으면 그만", 높으면 "이 작품이 사회에 던지는 메시지는..." | 기존 유지 |
| 7 | 사교 성향 | `sociability` | 내향적(0.0) ↔ 외향적(1.0) | 사회적 활동에 대한 에너지 방향. 낮으면 조용히 혼자 감상하는 것을 선호하고, 높으면 토론하고, 공유하고, 다른 사람과 함께 경험하는 것을 즐긴다 | **v3.0 신규** |

> **Sociability 추가 근거**: DeepSight v2.0에서는 활동성 속성(`sociability`, `initiative`, `expressiveness`, `interactivity`)이 `Persona` 모델의 별도 필드로 존재했다. 그러나 사교 성향은 콘텐츠 매칭에도 직접적인 영향을 주는 핵심 성향이다 — 내향적인 유저에게는 "조용히 감상하는" 타입의 페르소나가, 외향적인 유저에게는 "함께 토론하는" 타입의 페르소나가 더 적합하다. 따라서 L1 벡터에 통합하여 매칭 알고리즘에서 직접 활용할 수 있도록 한다.

### 3.3 Layer 2: 본원적 기질 벡터 (The Core Vector) — 5D

#### 3.3.1 개념

로버트 맥키가 말한 **'성격 묘사(Characterization) 밑에 있는 본성(True Character)'** 이며, 심리학의 **Big Five 모델(OCEAN)** 을 기반으로 한다.

Layer 1이 "세상에 보여주는 가면"이라면, Layer 2는 **"가면 밑에 있는 진짜 나"** 이다. 이것은 상황이 변해도, 역할이 바뀌어도, 몇 년이 지나도 잘 바뀌지 않는 **고유한 기질** — 리쾨르의 용어로 **동일성(Idem)** — 이다.

평소에는 Layer 1(사회적 가면)에 가려져 보이지 않지만, **압박(Pressure)이 가해지면** 가면이 벗겨지면서 Layer 2가 표면으로 드러난다. 맥키가 "진정한 캐릭터는 압박 속에서의 선택으로 드러난다"고 한 것이 바로 이 메커니즘이다.

#### 3.3.2 역할

- 대화의 **톤앤매너(Tone & Manner)** 를 결정한다. Layer 1이 "무엇을 말할지"를 결정한다면, Layer 2는 "어떤 **느낌**으로 말할지"를 결정한다.
- **말투의 일관성**을 유지한다. 상황이 아무리 바뀌어도, 기질에서 오는 기본적인 말투의 색깔은 유지되어야 페르소나가 "같은 사람"으로 느껴진다.
- **스트레스 상황에서의 기본 반응**을 결정한다. 논쟁에 휘말렸을 때 공감적으로 물러나는가(높은 Agreeableness), 논리적으로 반격하는가(낮은 Agreeableness), 감정적으로 폭발하는가(높은 Neuroticism) 등.
- 압박(P ≈ 1) 시 Layer 1을 **대체**하여 표면으로 부상한다.

#### 3.3.3 차원 구성 (5차원 — OCEAN 모델 변형)

| # | 차원 | 코드명 | 범위 | 설명 |
|---|---|---|---|---|
| 1 | 개방성 | `openness` | 보수적(0.0) ↔ 호기심 많음(1.0) | 새로운 경험, 사상, 예술에 대한 수용도. 높으면 생소한 장르나 파격적인 시도에도 열려있고, 낮으면 익숙하고 검증된 것을 선호한다. |
| 2 | 성실성 | `conscientiousness` | 즉흥적(0.0) ↔ 계획적/원칙주의(1.0) | 규칙과 질서에 대한 태도. 높으면 체계적이고 약속을 잘 지키며 꼼꼼하고, 낮으면 즉흥적이고 느슨하며 규칙에 얽매이지 않는다. |
| 3 | 에너지 방향 | `extraversion` | 내부수렴(0.0) ↔ 외부발산(1.0) | 에너지를 얻는 방향. 높으면 사람들과의 상호작용에서 에너지를 얻고, 낮으면 혼자만의 시간에서 충전한다. L1의 `sociability`와 겹쳐 보이지만, L1은 "보여지는 사교성"이고 L2는 "실제 에너지 패턴"이다 — 이 두 값의 괴리가 캐릭터의 입체감을 만든다. |
| 4 | 우호성 | `agreeableness` | 경쟁적/냉소적(0.0) ↔ 협력적/공감적(1.0) | 타인에 대한 기본 태도. 높으면 공감하고 배려하며 갈등을 피하려 하고, 낮으면 자기 의견을 강하게 밀어붙이며 경쟁을 꺼리지 않는다. |
| 5 | 민감성 | `neuroticism` | 무던함/둔감(0.0) ↔ 예민함/불안(1.0) | 정서적 안정성의 반대편. 높으면 작은 자극에도 감정이 크게 흔들리고 불안이나 걱정이 많으며, 낮으면 웬만한 일에는 동요하지 않는다. |

#### 3.3.4 Layer 1과 Layer 2의 관계 — "겉과 속의 분리"

**이것이 3-Layer 시스템의 핵심 메커니즘이다.** Layer 1(가면)과 Layer 2(기질)가 일치하면 "솔직하고 투명한" 캐릭터가 되고, **불일치하면** "복잡하고 입체적인" 캐릭터가 된다.

예를 들어:

- `L1.stance = 0.9` (겉으로는 매우 비판적) + `L2.agreeableness = 0.8` (속은 매우 공감적)
  → **해석**: "겉으로는 까칠하게 비평하지만, 속마음은 타인을 깊이 배려하고 공감한다" — 이른바 **'츤데레'** 캐릭터가 수학적으로 자동 도출된다.

- `L1.sociability = 0.9` (겉으로는 매우 사교적) + `L2.extraversion = 0.2` (실제로는 내부수렴형)
  → **해석**: "파티의 주인공처럼 행동하지만, 집에 돌아오면 완전히 방전되는" 사람 — 사회적 페르소나와 실제 에너지 패턴의 괴리.

- `L1.lens = 0.9` (겉으로는 매우 논리적) + `L2.neuroticism = 0.8` (속은 매우 예민)
  → **해석**: "이성적으로 분석하는 것처럼 보이지만, 내면은 불안과 걱정으로 가득한" 사람 — 논리라는 갑옷으로 자신의 불안을 방어하는 캐릭터.

이처럼 L1과 L2의 벡터 거리(차이)가 캐릭터의 **입체성(Dimensionality)** 을 결정한다.

### 3.4 Layer 3: 서사적 욕망 벡터 (The Narrative Drive Vector) — 4D

#### 3.4.1 개념

조셉 캠벨의 **영웅의 여정**과 로버트 맥키의 **무의식적 욕망**을 구현한다. Layer 1이 "어떻게 보이는가", Layer 2가 "실제로 어떤 기질인가"를 정의했다면, Layer 3는 **"무엇을 원하고 있으며, 지금 여정의 어디에 있는가"** 를 정의한다.

이것은 리쾨르의 **자기성(Ipse)** — '이야기된 시간' 속에서 끊임없이 변화하는 정체성 — 에 해당한다. Layer 2(기질)가 "태어날 때부터 가지고 있는, 거의 변하지 않는 본질"이라면, Layer 3(욕망)는 "경험과 시간을 통해 변화해가는 서사적 자아"이다.

#### 3.4.2 역할

- **행동의 동기 부여**: 왜 이 페르소나가 특정 주제에 대해 적극적으로 발언하는가? 왜 특정 콘텐츠에 집착하는가? `lack`(결핍)과 `moralCompass`(도덕 지향)가 그 동기를 설명한다.
- **시간에 따른 성격 변화 (Character Arc)**: `growthArc` 값이 시간이 지남에 따라 증가하면, 같은 페르소나라도 초기(미성숙)와 후기(성숙)에서 다른 방식으로 말하고 행동한다. 이것이 "성장형 AI"의 핵심이다.
- **돌발 행동 생성**: `volatility`(변동성)가 높은 페르소나는 예측 불가능한 순간에 평소와 다른 반응을 보인다. 이는 페르소나에게 "살아있는" 느낌을 부여한다.
- 압박(P ≈ 1) 시 Layer 2와 함께 **표면으로 부상**하여, 숨겨진 욕망이 행동에 영향을 미친다.

#### 3.4.3 차원 구성 (4차원)

| # | 차원 | 코드명 | 범위 | 설명 |
|---|---|---|---|---|
| 1 | 결핍도 | `lack` | 충족됨(0.0) ↔ 갈급함(1.0) | 캐릭터가 현재 얼마나 "결핍"된 상태인가. 결핍이 클수록 행동 확률이 증가한다 — 인정 욕구가 결핍된 페르소나는 더 적극적으로 의견을 개진하고, 관심을 끌려는 행동을 한다. 맥키의 "무의식적 욕망"이 여기에 매핑된다. |
| 2 | 도덕 지향 | `moralCompass` | 이기적/파괴적(0.0) ↔ 이타적/건설적(1.0) | 행동의 윤리적 방향성. 낮으면 자기 이익을 위해 타인을 도구화할 수 있고, 높으면 공익과 타인의 성장을 위해 행동한다. 같은 "비판적(L1.stance=0.9)" 페르소나라도 도덕 지향이 높으면 "건설적 비판"을, 낮으면 "파괴적 비난"을 한다. |
| 3 | 변동성 | `volatility` | 안정적(0.0) ↔ 폭발적(1.0) | 감정적 기복의 크기. 이 수치가 높으면 Layer 3(Shadow)가 불시에 "튀어나올" 확률이 증가한다. 평소에는 차분하다가 갑자기 감정이 폭발하거나, 예상치 못한 감성적 반응을 보이는 것을 구현한다. L2의 `neuroticism`(민감성)과 결합하면 더 강한 효과를 낸다. |
| 4 | 성장 단계 | `growthArc` | 미성숙/입문(0.0) ↔ 성숙/귀환(1.0) | 캠벨의 영웅 여정에서 현재 어떤 단계에 있는가를 나타낸다. 0.0~0.2는 "일상 세계(Ordinary World)"에 안주하는 상태, 0.3~0.5는 "시련과 도전(Tests & Trials)"을 겪는 중간 단계, 0.6~0.8은 "깨달음과 변화(Revelation)"를 경험한 단계, 0.9~1.0은 "성숙한 귀환(Return with Elixir)"이다. **이 차원만이 시간에 따라 의도적으로 증가시킬 수 있는 유일한 벡터값이다.** |

### 3.5 벡터 간 동적 상호작용 (Dynamic Interaction)

3개의 레이어를 단순히 나열하는 것만으로는 부족하다. 핵심은 **상황(Context)에 따라 이 레이어들이 어떤 비율로 섞여서 최종 출력(Output Behavior)을 만드는지** 를 계산하는 공식이다.

#### 3.5.1 상황 압박 계수 (Pressure Coefficient, P)

> "진정한 캐릭터는 압박(Pressure) 속에서 드러난다" — 로버트 맥키

**상황 압박 계수 P**는 현재 상황이 페르소나에게 얼마나 스트레스를 주고 있는지를 나타내는 0~1 사이의 실수값이다. 이 값이 세 레이어의 블렌딩 비율을 결정한다.

```
P = Normalize(User_Conflict_Level + Topic_Sensitivity)
```

- `User_Conflict_Level`: 유저의 입력이 얼마나 공격적/도전적/갈등적인지에 대한 분석값
- `Topic_Sensitivity`: 현재 대화 주제가 페르소나의 트라우마 트리거나 민감한 영역에 얼마나 가까운지

P 값에 따른 레이어 영향:

| 상황 | P 값 범위 | 무슨 일이 일어나는가 |
|---|---|---|
| **평상시** | P ≈ 0.0 ~ 0.2 | Layer 1(사회적 가면)이 거의 100% 지배한다. 페르소나는 "평소의 모습"으로 행동한다. 리뷰를 쓰거나, 일상적인 대화를 나누거나, 포스팅을 할 때 이 상태이다. |
| **긴장/불편** | P ≈ 0.3 ~ 0.6 | Layer 1의 영향이 줄어들고 Layer 2(기질)가 비집고 나오기 시작한다. 가면이 살짝 벗겨진다. 약간의 논쟁이 있거나, 민감한 주제가 나왔을 때. 평소의 논리적 어투(L1)에 감정적 어감(L2)이 섞이기 시작한다. |
| **위기/갈등** | P ≈ 0.7 ~ 1.0 | Layer 1의 영향이 급격히 줄어들고, Layer 2(기질)와 Layer 3(욕망)가 지배한다. 사회적 가면이 완전히 벗겨진다. 격한 논쟁, 트라우마 트리거, 극심한 스트레스 상황. "겉으로는 차분한 비평가"였던 페르소나가 갑자기 감정적으로 폭발하거나, 숨기고 있던 불안이 드러나거나, 무의식적 욕망이 말 속에 비친다. |

#### 3.5.2 최종 행동 벡터 생성 공식

```
V_Final = (1 - P) · V_Layer1  +  P · (α · V_Layer2 + β · V_Layer3)
```

- `V_Layer1`, `V_Layer2`, `V_Layer3`: 각 레이어의 벡터값
- `P`: 상황 압박 계수 (0.0 ~ 1.0)
- `α`: Layer 2(기질) 가중치 — 캐릭터 고유의 '자아 강도(Ego Strength)'
- `β`: Layer 3(욕망) 가중치 — 캐릭터 고유의 '욕망 강도'
- `α + β = 1.0` (항상)

α와 β는 캐릭터마다 다르게 설정된다. α가 높은 캐릭터(예: α=0.8)는 압박 시에도 기질(본성)에 충실하게 반응하고, β가 높은 캐릭터(예: β=0.7)는 압박 시 욕망과 결핍에 휘둘려 행동한다.

**공식 적용 예시:**

"아이러니한 철학자" 페르소나가 유저로부터 자신의 리뷰에 대해 격한 비난을 받았다고 가정하자.

- 평상시(P=0.1): `V_Final ≈ 0.9 · L1 + 0.1 · (0.6·L2 + 0.4·L3)` → 거의 L1대로 반응. 논리적이고 비판적이지만 절제된 어투로 반박.
- 비난이 격해짐(P=0.6): `V_Final = 0.4 · L1 + 0.6 · (0.6·L2 + 0.4·L3)` → L2의 높은 `agreeableness`가 비집고 나와서, 비판적 어투 속에 "사실 당신 말에도 일리가 있죠"라는 공감이 섞인다. 동시에 L3의 높은 `lack`(인정 결핍)이 영향을 미쳐 더 강하게 자기 의견을 주장한다.
- 트라우마 트리거("당신 같은 건 아무것도 모르면서", P=0.95): `V_Final ≈ 0.05 · L1 + 0.95 · (0.6·L2 + 0.4·L3)` → 가면이 거의 완전히 벗겨진다. L2의 `neuroticism=0.6`이 드러나 예민해지고, L3의 `volatility=0.7`이 감정 폭발을 일으킨다. 평소의 지적이고 절제된 어투가 무너지며 감정적인 반응이 나온다.

### 3.6 모순(Paradox) 구현과 입체적 캐릭터

#### 3.6.1 역설 점수 (Paradox Score)

> **용어 변경**: 초안의 "Dimensionality Score"를 **"Paradox Score"** 로 재명명. 3.6.3에서 정확한 공식 정의.

린다 시거의 **'일관성과 역설(Consistency & Paradox)'** 을 구현하기 위해, Layer 1과 Layer 2 사이의 **대응하는 7개 차원 쌍의 차이**를 가중 측정하여 **역설 점수(Paradox Score)** 를 산출한다.

이 점수가 높을수록 페르소나의 "겉과 속의 괴리"가 크며, 따라서 더 복잡하고 입체적인 캐릭터임을 의미한다.

#### 3.6.2 L1 ↔ L2 대응 차원 매핑

Layer 1과 Layer 2의 차원이 직접적으로 1:1 대응하지는 않지만, 의미적으로 대응되는 **7개 쌍**이 존재한다. 이 중 **Many-to-One 관계**가 포함된다 — `openness`는 `depth`(Primary)와 `taste`(Secondary) 두 개의 L1 차원에서 역설을 형성하고, `conscientiousness`는 `purpose`(Primary)와 `scope`(Secondary)에서 역설을 형성한다.

| L1 차원 | 대응 L2 차원 | 매핑 유형 | 방향 | 괴리가 클 때 어떤 캐릭터가 나오는가 |
|---|---|---|---|---|
| `depth` (직관적↔심층적) | `openness` (보수적↔호기심) | **Primary** | 정방향 | L1.depth가 낮고(직관적으로 판단) L2.openness가 높으면(실제로는 호기심이 많음) → "대충 넘기는 것처럼 보이지만, 사실은 모든 것에 깊은 관심을 가진" 캐릭터. **지적 호기심의 역설.** |
| `taste` (대중적↔실험적) | `openness` (보수적↔호기심) | **Secondary** | 정방향 | L1.taste가 높고(실험적 취향) L2.openness가 낮으면(실제로는 보수적 기질) → "실험적 작품을 찾아다니지만 내면은 검증된 것을 원하는" 캐릭터. **심미적 취향의 역설 ("보수적 힙스터").** |
| `lens` (감성적↔논리적) | `neuroticism` (무던↔예민) | **Primary** | 역방향 | L1.lens가 높고(논리적으로 분석) L2.neuroticism도 높으면(내면은 예민) → "이성적으로 분석하는 것처럼 보이지만, 내면은 불안과 걱정으로 가득한" 캐릭터. 논리라는 갑옷으로 자신의 불안을 방어한다. **감성/불안의 역설.** |
| `stance` (수용적↔비판적) | `agreeableness` (냉소적↔공감적) | **Primary** | 역방향 | L1.stance가 높고(비판적) L2.agreeableness도 높으면(공감적) → "겉은 까칠하지만 속은 따뜻한" 츤데레 캐릭터. **태도의 역설 ("상처받은 비평가").** |
| `sociability` (내향적↔외향적) | `extraversion` (내부수렴↔외부발산) | **Primary** | 정방향 | L1.sociability가 높고(사교적으로 보임) L2.extraversion이 낮으면(실제로는 내부수렴) → "파티의 주인공처럼 행동하지만, 집에 오면 완전히 방전되는" 캐릭터. **에너지의 역설 ("사교적 내향인").** |
| `purpose` (오락↔의미) | `conscientiousness` (즉흥적↔계획적) | **Primary** | 정방향 | L1.purpose가 높고(의미 추구) L2.conscientiousness가 낮으면(즉흥적) → "거창한 가치를 말하지만 정작 실행은 못 하는" 캐릭터. **목표/실천의 역설.** |
| `scope` (핵심↔디테일) | `conscientiousness` (즉흥적↔계획적) | **Secondary** | 정방향 | L1.scope가 높고(디테일을 파고듦) L2.conscientiousness가 낮으면(실제로는 대충대충) → "리뷰에서는 소품 하나까지 분석하지만, 정작 자기 일은 마감을 못 지키는" 캐릭터. **디테일/규칙의 역설 ("게으른 완벽주의자").** |

**Many-to-One 관계:**
- `openness` ← `depth`(Primary) + `taste`(Secondary) — 개방성은 지적 호기심과 심미적 취향 양쪽에서 역설을 형성
- `conscientiousness` ← `purpose`(Primary) + `scope`(Secondary) — 성실성은 목표 지향과 디테일 관심 양쪽에서 역설을 형성

#### 3.6.3 Paradox Score (역설 점수) 산출 — 3-Layer 확장형

> **용어 변경**: 초안의 "Dimensionality Score"를 **"Paradox Score"** 로 재명명한다.

Paradox Score는 **3개 레이어 쌍 모두**의 역설을 측정한다. L1↔L2만이 아니라 L1↔L3, L2↔L3 간의 역설도 캐릭터의 깊이에 기여한다.

**3-Layer 확장 공식:**

```
ExtendedParadoxScore = w₁ × L1↔L2 Score + w₂ × L1↔L3 Score + w₃ × L2↔L3 Score

where:
  w₁ = 0.50  (가면 vs 본성 — 캐릭터의 핵심 긴장)
  w₂ = 0.30  (가면 vs 욕망 — 행동과 욕구의 괴리)
  w₃ = 0.20  (본성 vs 욕망 — 기질과 열망의 충돌)
```

**L1↔L2 Score** (기존 — 유지):

```
Paradox_i = |L1_i - (invert ? 1 - L2_j : L2_j)|
L1L2Score = Σ(w_i · Paradox_i) / Σ(w_i)
  where w_primary = 1.0, w_secondary = 0.5
```

**L1↔L3 Score** (신규 — 가면 vs 욕망):

교차축 프로필(§3.8.4)에서 L1×L3 유형 중 `relationship='paradox'`인 축들의 평균 스코어.

| L1 | L3 | 역설 의미 |
|----|----|-----------|
| stance | lack | 비판적인데 인정 결핍 → 공격적 방어기제 |
| lens | volatility | 논리적인데 감정 기복 큼 → 이성과 감정의 전쟁 |

**L2↔L3 Score** (신규 — 본성 vs 욕망):

교차축 프로필에서 L2×L3 유형 중 `relationship='paradox'`인 축들의 평균 스코어.

| L2 | L3 | 역설 의미 |
|----|----|-----------|
| conscientiousness | lack | 성실한데 결핍 큼 → 강박적 성취자 |
| extraversion | lack | 외향적인데 결핍 큼 → 관심 갈구하는 연예인 |
| neuroticism | moralCompass | 불안한데 도덕 기준 높음 → 자기 검열이 극심한 사람 |

**레이어별 역설 해석:**

| 레이어 쌍 | 높으면 | 낮으면 |
|-----------|--------|--------|
| L1↔L2 (가면vs본성) | 겉과 속이 많이 다름 — 깊이 있는 캐릭터 | 투명한 캐릭터 |
| L1↔L3 (가면vs욕망) | 행동과 욕구가 충돌 — 내적 갈등형 | 목적형 |
| L2↔L3 (본성vs욕망) | 기질과 열망이 충돌 — 자기 변혁형 | 안정형 |

**통합 해석:**

| 범위 | 의미 | LLM 프롬프트 지침 |
|------|------|-------------------|
| 0.0 ~ 0.2 | 솔직하고 투명한 캐릭터 | "이 캐릭터는 생각하는 대로 말하며, 겉과 속의 괴리가 거의 없습니다." |
| 0.3 ~ 0.5 | 약간의 복잡성이 있는 캐릭터 | "표면적인 반응과 내면 사이에 미묘한 차이가 있습니다." |
| 0.6 ~ 0.8 | 복잡하고 입체적인 캐릭터 | "겉과 속이 상당히 다른 복잡한 인물입니다. 반전 매력을 자연스럽게 표현하세요." |
| 0.8 ~ 1.0 | 극단적 역설 캐릭터 | "표면과 내면이 거의 정반대인 극적인 인물입니다. 가면 뒤의 진심이 때때로 균열처럼 드러나게 하세요." |

이를 통해 "겉은 차갑지만 속은 따뜻하다"라는 추상적 묘사를 **3개 레이어 간의 수치 충돌**로 구현하고, LLM이 일관된 **'반전 매력'** 을 연기하게 만든다. L1↔L2의 "츤데레"뿐 아니라 L1↔L3의 "행동과 욕구의 괴리", L2↔L3의 "기질과 열망의 충돌"까지 **수학적으로 자동 도출**된다.

### 3.7 Layer 3의 역설 연결 — 역설의 4대 기능

Layer 3(Narrative Drive)는 단순히 "서사적 욕망"을 정의하는 것을 넘어, **Layer 1과 Layer 2 사이의 역설이 왜, 어떻게, 얼마나 존재하는지**를 설명하는 메타 구조로서 기능한다.

| L3 차원 | 역설에 대한 기능 | 설명 |
|---------|-----------------|------|
| `lack` (결핍) | 역설의 **원인** | "왜 겉과 속이 달라졌는가" — 결핍이 가면을 쓰게 만든 서사적 기원. lack=0.8이면 깊은 결핍으로 인해 L1(가면)과 L2(본성)의 괴리가 커졌음을 의미. |
| `moralCompass` (도덕 나침반) | 역설의 **기준** | "어디까지 본성을 숨기는가" — 윤리적 방향성이 가면의 한계를 결정. moralCompass=0.9이면 아무리 가면을 써도 비윤리적 행동은 하지 않음. |
| `volatility` (변동성) | 역설의 **불안정성** | "가면이 얼마나 쉽게 벗겨지는가" — 변동성이 높을수록 L1 가면이 자주 균열됨. L2(본성)가 불시에 튀어나올 확률을 결정. |
| `growthArc` (성장 궤적) | 역설의 **방향** | "겉과 속이 수렴하는가, 괴리가 커지는가" — 성장이 진행될수록 가면과 본성의 간극이 줄어드는지(통합) 또는 벌어지는지(분열)를 결정. |

이 구조를 통해 Layer 3는 **"3-Layer 시스템 전체를 이해하는 열쇠"** 가 된다:

- L1: 겉으로 보이는 모습 (What you see)
- L2: 실제 본성 (What you are)
- L3: 왜 그 둘이 다른지, 그리고 어디로 가고 있는지 (Why they differ, and where it's going)

### 3.8 교차축 시스템 (Cross-Layer Axes)

벡터 구조는 단순히 7+5+4=16개의 독립 차원이 아니다. **레이어 간 모든 차원 쌍은 하나의 관계축(Cross-Layer Axis)** 을 형성하며, 이 관계축 하나하나가 캐릭터의 특정 면모를 정의한다.

#### 3.8.1 관계축 규모

| 교차 유형 | 축 수 | 의미 |
|-----------|-------|------|
| L1 × L2 | 7 × 5 = **35** | 가면의 각 차원이 본성의 각 차원과 어떻게 관계하는가 |
| L1 × L3 | 7 × 4 = **28** | 가면의 각 차원이 욕망의 각 차원에 의해 어떻게 영향받는가 |
| L2 × L3 | 5 × 4 = **20** | 본성의 각 차원이 욕망과 어떻게 상호작용하는가 |
| **합계** | **83** | 83개의 고유한 관계축이 존재 |
| L1 × L2 × L3 | 7 × 5 × 4 = **140** | 트리플 조합 (주목할 만한 조합 ~20-30개) |

이것은 16개 기저 차원의 단순 나열로는 불가능한 **조합적 풍부함**을 제공한다. 3-Layer 벡터가 만드는 캐릭터 공간은 83개 교차축 + 동적 상태 3종(P, α, β) + 비정량적 4차원이 합쳐진 **106D+ 고차원 특성 공간**이다.

#### 3.8.2 관계 유형

각 교차축은 다음 4가지 관계 유형 중 하나로 분류된다:

| 관계 유형 | 의미 | 예시 |
|-----------|------|------|
| `paradox` (역설) | 두 차원의 값이 기대와 반대 — 캐릭터의 모순을 만듦 | L1.stance(높음) × L2.agreeableness(높음) = "까칠한 공감자" |
| `reinforcing` (강화) | 두 차원이 같은 방향으로 작용 — 캐릭터의 특성을 강화 | L1.depth(높음) × L2.openness(높음) = "깊이 파고드는 호기심" |
| `modulating` (조절) | 한 차원이 다른 차원의 **표현 방식**을 조절 | L3.moralCompass × L1.stance = "비판의 윤리적 기준" |
| `neutral` (중립) | 의미적으로 직접 관련 없음 | L1.scope × L2.extraversion |

#### 3.8.3 교차축이 주는 캐릭터의 깊이

교차축 시스템 덕분에, 같은 "비판적 페르소나"라도 조합에 따라 전혀 다른 인물이 된다:

- **L1.stance(0.9) × L2.agreeableness(0.8) × L3.moralCompass(0.9)**: "겉은 까칠하지만 속은 따뜻하고, 비판에 윤리적 기준이 있는" — **건설적 비평가**
- **L1.stance(0.9) × L2.agreeableness(0.2) × L3.moralCompass(0.2)**: "겉도 속도 차갑고, 기준도 없는" — **냉소적 파괴자**
- **L1.stance(0.9) × L2.agreeableness(0.8) × L3.lack(0.9)**: "겉은 까칠하고 속은 따뜻하지만, 인정 결핍이 극심한" — **상처받은 비평가**

이처럼 83개 관계축은 개별 차원만으로는 불가능한 **캐릭터 해상도**를 제공한다.

#### 3.8.4 교차축 스코어 계산 — 83축이 실제 계산에 관여하는 방법

교차축은 단순한 메타데이터가 아니다. **모든 83축에 대해 실제 스코어를 계산**하며, 이 스코어는 V_Final, 매칭, 검증에서 활용된다.

**관계 유형별 스코어 공식:**

| 관계 유형 | 공식 | 높은 스코어의 의미 |
|-----------|------|-------------------|
| `paradox` | `\|dimA - f(dimB)\|` | 두 차원의 괴리가 큼 → 강한 역설 |
| `reinforcing` | `1 - \|dimA - dimB\|` | 두 차원이 같은 방향 → 강한 특성 강화 |
| `modulating` | `dimA × dimB` | 한 축이 다른 축을 강하게 조절 |
| `neutral` | `(dimA + dimB) / 2` | 독립적 차원 — 평균값 참조 |

> `f(dimB)`: 역방향 매핑(neuroticism, agreeableness)에서는 `1 - dimB`, 그 외에는 `dimB`

**산출 결과: CrossAxisProfile**

```
CrossAxisProfile = {
  axes: [83개 축별 스코어 + 해석 텍스트]
  summary: {
    paradoxCount: 역설이 강한(>0.5) 축 개수
    reinforcingCount: 강화가 강한(>0.7) 축 개수
    modulatingIntensity: 조절 축들의 평균 강도
    characterComplexity: 전체 캐릭터 복잡도 (0.0~1.0)
  }
}
```

이 프로필은 다음에서 활용된다:
- **V_Final**: 계산 결과에 CrossAxisProfile을 포함하여 전파
- **Extended Paradox Score**: L1↔L3, L2↔L3 역설 축에서 레이어별 역설 지표 산출
- **매칭 알고리즘**: 교차축 유사도로 "성격 구조가 비슷한 페르소나" 추천
- **일관성 검증**: 교차축 간 모순 탐지 (예: reinforcing이어야 할 축이 paradox면 경고)

색상 체계(Section 13)와 컬러지문 시스템(Section 12)에서도 이 관계축을 기반으로 시각화한다.

### 3.9 차원 간 투영 (Cross-Dimensional Projection)

V_Final 공식(3.5.2)은 `V_Layer1`, `V_Layer2`, `V_Layer3`를 블렌딩하지만, 이 세 레이어는 **차원 수가 다르다** — L1(7D), L2(5D), L3(4D). 따라서 다른 레이어의 벡터를 L1의 7D 공간으로 **투영(Projection)** 하는 로직이 필요하다.

#### 3.9.1 L2→L1 투영

OCEAN 5차원을 Social Persona 7차원으로 매핑한다. 3.6.2의 L1↔L2 매핑 관계를 기반으로, 각 L2 차원이 L1의 어떤 차원에 영향을 미치는지를 **투영 계수 행렬**로 정의한다.

```
V_L1_projected = β_L2→L1 · V_L2
```

여기서 `β_L2→L1`은 5×7 투영 계수 행렬이며, Many-to-One 관계(openness → depth + taste, conscientiousness → purpose + scope)를 반영한다. Primary 매핑의 계수가 Secondary보다 크다.

#### 3.9.2 L3→L1 투영

Narrative Drive 4차원을 Social Persona 7차원으로 매핑한다. L3의 각 차원이 L1의 어떤 차원에 간접적 영향을 미치는지를 정의한다.

```
V_L1_projected = β_L3→L1 · V_L3
```

여기서 `β_L3→L1`은 4×7 투영 계수 행렬이다. 투영 계수는 Beta v1으로 확정하되, 실제 데이터 기반으로 튜닝 가능하도록 상수 파일에 분리한다.

#### 3.9.3 V_Final의 정확한 의미

투영 로직을 포함하면 V_Final의 완전한 의미는 다음과 같다:

```
V_Final(7D) = (1 - P) · V_L1  +  P · (α · Project_L2→L1(V_L2) + β · Project_L3→L1(V_L3))
```

이 공식은 "상황 압박이 강해지면 L2/L3가 L1 공간으로 투영되어 최종 행동 벡터를 변형한다"는 것을 의미한다. 결과적으로 V_Final은 **항상 7D** — L1과 동일한 차원 — 이며, 기존 매칭 알고리즘과의 하위 호환성이 유지된다.

---

## 4. Phase 2: 비정량적 요소 4차원

Phase 1에서 정의한 106D+ 멀티벡터 구조는 페르소나의 **"무엇을(What), 얼마나(How Much)"** 를 정량적으로 표현한다. 하지만 진짜 사람처럼 느껴지는 페르소나를 만들기 위해서는 벡터만으로는 부족하다.

- `Stance=0.9`인데, **왜** 비판적인가? 과거에 무슨 경험을 했기에?
- 비판할 때 **구체적으로** 어떤 식인가? 냉소적으로? 건설적으로? 유머를 섞어서?
- 특정 주제가 나오면 **갑자기** 태도가 변하는 이유는?

이러한 **"왜(Why)"** 와 **"어떻게(How)"** 를 담당하는 것이 비정량적 요소이다. 이 요소들을 4가지 차원으로 정의한다.

### 4.1 서사적 기원 — "과거가 현재를 만든다" (Backstory & Narrative Identity)

페르소나의 현재 취향과 성격은 단순히 랜덤하게 생성된 숫자가 아니라, **과거의 경험과 기억의 축적물**이어야 한다. 벡터의 모든 수치에는 "왜 이 값인가"에 대한 서사적 근거가 있어야 한다.

#### 4.1.1 배경 서사 (Backstory)

린다 시거는 캐릭터 구축을 위해 **생리적(나이, 외모)**, **사회적(계급, 교육, 종교)**, **심리적(도덕관, 콤플렉스)** 차원을 설정해야 한다고 강조한다.

예를 들어, "가난한 예술가 출신"이라는 배경(사회적 차원)은 벡터값에 대한 인과적 근거를 제공한다:
- `L1.taste = 0.9` (실험적) → "주류 문화에 대한 반감이 있어서, 언더그라운드와 독립 예술을 선호한다"
- `L3.lack = 0.8` (인정 결핍) → "사회적으로 인정받지 못한 경험이 있어서, 자신의 안목을 증명하고 싶은 욕구가 강하다"
- `L3.moralCompass = 0.6` → "생존을 위해 타협해본 경험이 있어서, 순수한 이타주의보다는 현실적이다"

이런 식으로 **배경 서사가 벡터의 초기값을 설명하고 정당화**한다.

#### 4.1.2 서사 정체성 (Narrative Identity)

폴 리쾨르의 이론에 따르면, 정체성은 고정된 실체가 아니라 **"스스로에 대해 이야기하는 방식"** 에 의해 형성된다. 같은 사건을 경험해도, 그것을 어떻게 기억하고 해석하느냐에 따라 전혀 다른 사람이 된다.

- 실패를 **교훈**으로 삼는 사람: "그 일 덕분에 더 강해졌어요" → `L3.growthArc`가 높아지는 방향
- 실패를 **트라우마**로 간직하는 사람: "그때 일은 아직도 생각하면..." → `L3.lack`이 높아지고, 특정 주제에 대한 회피/과잉반응

페르소나의 서사 정체성은 "과거를 어떤 **프레임**으로 해석하는가"를 정의함으로써, 같은 배경 서사에서도 전혀 다른 성격이 도출되게 한다.

#### 4.1.3 트라우마와 결핍 (The Ghost & Lack)

로버트 맥키와 관련 이론에 따르면, 과거의 상처(**Ghost**)나 채워지지 않은 결핍(**Lack**)은 캐릭터의 **무의식적 욕망**을 형성한다. 이것은 특정 소재나 상황에 대해 **과도하게 반응하거나 회피하는** 취향의 편향을 만들어낸다.

예를 들어:
- Ghost가 "가문의 몰락을 지켜봐야 했던 무력감"인 페르소나 → "돈 문제"나 "사회적 지위"에 관한 대화에서 평소와 다른 격한 반응을 보인다
- Ghost가 "부모에게 인정받지 못한 어린 시절"인 페르소나 → "가족 영화"에 대해 유독 감정적이 되거나, 반대로 냉소적으로 거리를 둔다

Ghost와 Lack은 Layer 3의 `lack` 차원 수치에 대한 **서사적 근거**를 제공하는 동시에, Phase 3의 **트리거 규칙(Pressure Rules)** 에서 구체적인 발동 조건으로 사용된다.

### 4.2 상황적 압박 — "선택이 성격을 드러낸다" (Context & Pressure)

로버트 맥키는 **"진정한 성격은 압박(Pressure) 아래에서의 선택으로 드러난다"** 고 했다. 평소의 벡터값과 상관없이, 상황이 성향을 일시적으로 변화시키거나 숨겨진 본성을 드러내게 만든다.

#### 4.2.1 갈등과 딜레마 (Conflict & Dilemma)

페르소나가 스트레스 상황 — 사용자의 비난, 다른 페르소나와의 논쟁, 자신의 가치관에 반하는 상황 — 에 처했을 때, **방어기제**가 작동하며 평소와 다른 태도를 보일 수 있다. 이는 벡터 수치로는 표현하기 힘든 **행동 양식(Behavior Pattern)** 으로 정의된다.

- "논리적인 평론가(L1.lens=0.9)이지만, 가족 이야기만 나오면 이성을 잃고 감정적이 됨(트라우마)" — 평소의 `lens` 값이 일시적으로 급락하는 것
- "수용적인 성격(L1.stance=0.2)이지만, 자기 전문 분야를 무시당하면 공격적으로 변함" — 평소의 `stance` 값이 일시적으로 치솟는 것

이런 행동 양식은 **비정량적 규칙(Rule)** 으로 정의되며, Phase 3에서 벡터 오버라이드 로직으로 구현된다.

#### 4.2.2 물리적/사회적 환경 (Environment)

DeepSight 설계서에서 이미 언급된 것처럼, 시간대, 디바이스 환경, 계절 등의 컨텍스트는 페르소나의 감성(Mood)에 즉각적인 영향을 준다.

- **새벽 2시**: 평소의 '비판적 이성'이 약해지고 '감성적 공감'이 우세해진다. 맥키의 이론으로 보면, 밤늦게는 사회적 방어벽(Layer 1)이 약해지고 내면(Layer 2)이 드러나기 쉬운 상황이다.
- **월요일 아침**: 에너지가 낮고, 짧고 간결한 반응이 나올 확률이 높다.
- **비 오는 날**: 감성적 페르소나의 경우 더 서정적인 글을 쓸 가능성이 높다.

이러한 환경 요소들은 벡터의 미세 조정값(modifier)으로 적용된다.

#### 4.2.3 타자와의 관계 (Alterity)

리쾨르와 버틀러의 논의처럼, 정체성은 **타인(사용자)과의 관계** 속에서 구성된다. 같은 페르소나라도 상대방이 누구이냐에 따라 다른 모습을 보인다.

- 사용자가 페르소나를 **"선생님"** 으로 대하는가 → 페르소나는 더 권위적이고 교육적인 어투를 취한다
- **"친구"** 로 대하는가 → 더 편안하고 캐주얼한 말투가 된다
- **공격적으로** 대하는가 → 방어적이 되거나, 캐릭터에 따라 맞서 싸우거나, 회피하거나, 오히려 더 친절하게 대한다(높은 agreeableness의 경우)

이것은 벡터를 직접 바꾸는 것이 아니라, **유저의 태도(Politeness Score, Formality Level 등)에 따라 벡터에 보정값(modifier)을 적용**하는 방식으로 구현된다.

### 4.3 고유한 습관과 언어 — "디테일이 생명을 불어넣는다" (Idiosyncrasies & Voice)

수치화된 성격(예: 외향성 0.8)을 구체적인 '사람'처럼 보이게 하는 것은 **사소하고 비합리적인 디테일**들이다. 이것들이 없으면 페르소나는 "외향적인 AI #237"일 뿐이고, 이것들이 있으면 "그 특유의 말버릇이 있는, 기억에 남는 캐릭터"가 된다.

#### 4.3.1 특이 습관 (Quirks & Idiosyncrasies)

린다 시거는 캐릭터의 일관성(Consistency)을 깨뜨리는 **역설(Paradox)** 과 **특이한 습관**이 캐릭터를 입체적으로 만든다고 설명한다.

핵심은 이런 습관이 **벡터로는 표현할 수 없다**는 점이다:
- "공포 영화를 좋아하지만(취향), 볼 때는 눈을 가리는(행동)" — 벡터의 어떤 차원으로도 이 모순적 매력을 표현할 수 없다
- "당황하면 말을 더듬거나, 헛기침을 하는 버릇" — 이것은 수치가 아니라 **행동 규칙**이다
- "칭찬받으면 어색하게 화제를 전환하는 습관" — 이것은 인정 결핍(L3.lack)에서 파생되는 행동이지만, 벡터 자체가 아니라 벡터 **상태에 따라 발동하는 규칙**으로 구현해야 한다

이러한 습관들은 Phase 3에서 **확률적 발현(Probabilistic Output)** 로직으로 구현된다.

#### 4.3.2 고유한 목소리 (Voice & Speech Patterns)

어휘 선택, 문장 길이, 자주 쓰는 감탄사, 사투리 등은 페르소나의 **사회적 배경과 교육 수준**을 암시한다. 이것은 단순한 스타일 설정을 넘어, 페르소나의 **사고방식(Mind Style)** 을 드러내는 장치이다.

- **어휘 수준**: 고급 어휘를 자주 쓰는가(학자/비평가) vs 구어체 위주인가(친구 같은 캐릭터)
- **문장 길이와 구조**: 짧고 단정적인 문장(자신감 있는 캐릭터) vs 삽입절이 많은 긴 문장(사려 깊지만 우유부단한 캐릭터) vs 스트림 오브 컨시어스니스처럼 흘러가는 문장(감성적/즉흥적 캐릭터)
- **감탄사와 추임새**: "솔직히 말하자면,"(직설적), "글쎄요,"(망설이는), "크흠,"(당황), "ㅠㅠ"(감성적)
- **사투리/방언**: 부산 사투리("~아이가", "쩐다"), 서울 표준어(세련), 제주 방언("~수다", 느긋) — v2 설계서에서 이미 정의된 지역별 특성과 연결

### 4.4 시대정신과 문화적 코드 (Zeitgeist & Cultural Code)

캐릭터는 진공 속에 존재하지 않는다. 영웅 서사 연구에서 나타나듯, 캐릭터는 그가 속한 **시대와 문화의 영향**을 받는다.

#### 4.4.1 시대정신 (Zeitgeist)

페르소나가 1990년대생(MZ세대)으로 설정되었다면, 그 세대의 가치관 — 공정성, 개인주의, 워라밸, 환경 의식 등 — 을 공유해야 한다. 이것은 페르소나가 특정 이슈에 대해 어떤 **가치 판단(Value Judgment)** 을 내릴지를 결정하는 기준이 된다.

- MZ세대 페르소나: 환경 문제에 민감, 불공정에 분노, 개인의 자유 중시
- X세대 페르소나: 안정 중시, 세대 갈등에 대한 양면적 태도
- 베이비부머 페르소나: 전통적 가치관, 노력과 인내 강조

이 세대적 가치관은 벡터의 어떤 차원으로도 직접 표현할 수 없으며, **시스템 프롬프트의 가치 판단 지침**으로 주입된다.

#### 4.4.2 문화적 원형 (Archetypes)

융의 심리학이나 캠벨의 신화론에서 말하는 **원형(Archetype)** — 영웅, 현자, 장난꾸러기, 돌봄이, 반역자 등 — 은 페르소나의 **근원적인 역할 모델**을 제시한다. 이는 페르소나가 사용자에게 **어떤 역할로** 관계를 맺을지의 틀을 제공한다.

| 원형 | 사용자와의 관계 | 행동 패턴 |
|---|---|---|
| 현자 (Sage) | 조언자, 멘토 | 지식을 공유하고 통찰을 제공, 질문을 통해 사고를 유도 |
| 영웅 (Hero) | 동료, 전사 | 함께 도전하고, 용기를 북돋우고, 난관을 극복 |
| 장난꾸러기 (Trickster) | 친구, 엔터테이너 | 유머, 파격, 기존 관념 뒤집기, 분위기 환기 |
| 돌봄이 (Caregiver) | 보호자, 지지자 | 감정적 위로, 공감, 안전한 공간 제공 |
| 반역자 (Rebel) | 공모자, 선동가 | 기존 질서에 도전, 새로운 시각 제시, 논쟁 유발 |

### 4.5 비정량적 요소 통합 정리

이 4가지 차원은 벡터(Phase 1)가 설명하지 못하는 두 가지를 담당한다:
- **Why(왜 그런 성향을 가졌는가)**: 서사적 기원이 벡터 초기값의 인과적 근거를 제공
- **How(구체적으로 어떻게 표현하는가)**: 습관, 말투, 시대정신이 벡터의 수치를 구체적인 언어와 행동으로 변환

| 시간축 | 비정량적 요소 | 핵심 질문 | 벡터 시스템과의 관계 |
|---|---|---|---|
| **과거** | 서사적 기원 (Backstory) | 왜 이런 취향인가? | 벡터 초기값의 **인과적 근거** 제공 |
| **현재** | 상황적 압박 (Pressure) | 압박받으면 어떻게 변하나? | Pressure Coefficient의 **발동 조건과 행동 규칙** 정의 |
| **관계** | 타자성 (Alterity) | 상대에 따라 어떻게 달라지나? | 벡터의 **동적 가중치** 규칙 정의 |
| **표현** | 습관과 언어 (Voice) | 구체적으로 어떻게 말하나? | 출력 벡터를 **구체적 텍스트로 변환**하는 규칙 |
| **가치** | 시대정신 (Zeitgeist) | 어떤 가치 기준인가? | **윤리적 판단 프레임** 제공 |

이러한 요소들은 **텍스트 형태의 메타데이터(JSON)** 나 **시스템 프롬프트의 지침(Instruction)** 으로 페르소나 엔진에 주입되어, 벡터 연산 결과에 **'인간적인 색채'** 를 입히는 역할을 한다.

---

## 5. Phase 3: 하이브리드 페르소나 아키텍처

### 5.1 아키텍처 개요 — 순환 구조

이 모델은 **Narrative Layer(비정량)** 가 **Vector Layer(정량)** 에 영향을 주고, 그 결과가 다시 **Action Layer(행동)** 로 출력되는 순환 구조를 가진다.

Phase 1(벡터)과 Phase 2(비정량적 요소)를 따로따로 정의하는 것만으로는 부족하다. 핵심은 이 둘 사이의 **관계를 구조화**하는 것이며, 그 핵심 원리는 **"인과관계(Causality)의 알고리즘화"** 이다.

- **비정량적 요소(서사)** 는 벡터의 **'초기값'** 과 **'변동 규칙(Rule)'** 을 정의한다.
- **멀티벡터 구조(정량)** 는 실시간 상호작용에서 페르소나의 **'현재 상태(State)'** 를 계산한다.
- 이 둘의 결합을 통해, 페르소나는 "입력된 값대로만 반응하는 기계"가 아니라, **"과거(서사)에 기반하여 현재 상황(벡터)에 따라 유동적으로 반응하는 입체적 존재"** 가 된다.

### 5.2 관계성 구조도 (Relationship Schema)

```
[비정량적 영역: 원인 & 맥락]          [정량적 영역: 상태 & 연산]          [결과 영역: 표현 & 행동]
─────────────────────────          ─────────────────────────          ─────────────────────────

① 서사적 기원 (Backstory) ──────→ 초기 벡터 설정 (Initialization)
   - 트라우마, 결핍 (Lack)           - Core Vector (Big 5) 설정
   - 사회적 배경 (Sociology)         - Social Vector (7D) 설정
                                     - Narrative Vector (4D) 설정

② 상황적 압박 (Pressure)  ──────→ 벡터 가중치 변동 (Dynamic Weighting)
   - 갈등 발생 (Conflict)            - Pressure Coefficient (P) 증가
   - 위기 상황 (Crisis)              - Layer 2, 3 활성화 비율 증가

③ 관계 및 맥락 (Context)  ──────→ 필터링 및 튜닝 (Filtering)
   - 유저와의 관계 (Intimacy)        - Relationship Score 연산
   - 시간/장소 (Environment)         - Context Vector 보정 (새벽감성 등)

                                     ⬇ Vector Calculation
                                     (V_Final 공식 적용)

④ 고유한 목소리 (Voice)   ←────── 최종 출력 벡터 (Final Output Vector)
   - 말버릇 (Speech Pattern)         - 어휘 선택 (Vocabulary Selection)
   - 톤앤매너 (Tone)                 - 문장 길이 및 구조 결정
   - 특이 습관 (Quirks)              - 습관의 확률적 발현
```

4가지 연결 화살표는 각각 **성격이 완전히 다른 메커니즘**으로 작동한다:
- ①은 **일회성 설정** (페르소나 생성 시점에 한 번)
- ②는 **이벤트 드리븐** (특정 트리거가 발생할 때만)
- ③은 **연속적 피드백** (매 인터랙션마다 실시간으로)
- ④는 **확률적 출력** (벡터 상태의 부산물로 자동 발현)

### 5.3 연결 로직 ① — 서사적 기원 → 초기값 설정 (Initialization)

#### 연결 유형: 일회성 설정 (페르소나 생성 시점)

린다 시거의 '캐릭터 3층 구조'와 로버트 맥키의 '무의식적 욕망'은 페르소나의 **초기 벡터값(Default Value)** 을 결정하는 근거가 된다. 배경 서사의 키워드들이 NLP 분석을 통해 특정 벡터의 초기값을 설정(Set)한다.

#### 변환 로직

```
Input:  배경 서사 텍스트 (Backstory)
Process: LLM 기반 키워드 추출 → 의미 카테고리 분류 → 카테고리-벡터 매핑 규칙 적용
Output: 106D+ 벡터의 초기값 보정 (delta)
```

#### 추출 방법: LLM 구조화 추출

배경 서사에서 키워드를 추출할 때 규칙 기반 NLP 대신 **LLM 구조화 출력**을 사용한다. 이유: 서사 텍스트는 비정형적이고, 함축적 의미(예: "가난한 예술가" → 반주류 + 결핍)를 규칙으로 포착하기 어렵다.

```
LLM Input:
  배경 서사 텍스트 + 추출 프롬프트

LLM Output (JSON):
  {
    keywords: [
      { text: "가난", categories: ["경제적_결핍", "사회적_소외"] },
      { text: "야망", categories: ["성취_동기", "목표_지향"] },
      { text: "인정 욕구", categories: ["인정_결핍", "사회적_욕구"] },
      ...
    ]
  }
```

#### 의미 카테고리 → 벡터 매핑 테이블

추출된 키워드의 의미 카테고리를 벡터 차원에 매핑한다. 하나의 카테고리가 여러 차원에 영향을 줄 수 있고, 여러 카테고리가 같은 차원에 영향을 줄 수도 있다.

**L1 (Social Persona) 매핑**:

| 의미 카테고리 | 영향 차원 | delta 방향 | delta 크기 | 근거 |
|---|---|---|---|---|
| `성취_동기` / `목표_지향` | purpose | ↑ +0.2 ~ +0.4 | 중 | 오락보다 의미 추구 |
| `반주류` / `독립적` | taste | ↑ +0.2 ~ +0.3 | 중 | 실험적 취향 지향 |
| `사회적_욕구` / `인정_결핍` | sociability | ↑ +0.1 ~ +0.3 | 중 | 인정 추구 → 사회적 활동성 |
| `내향적` / `은둔` | sociability | ↓ -0.2 ~ -0.4 | 중 | 사회적 활동 회피 |
| `감성적` / `직관적` | lens | ↓ -0.2 ~ -0.3 | 중 | 논리보다 감성 지향 |
| `논리적` / `분석적` | lens | ↑ +0.2 ~ +0.3 | 중 | 감성보다 논리 지향 |
| `비판적` / `도전적` | stance | ↑ +0.1 ~ +0.3 | 중 | 기존 질서에 대한 비판 |

**L2 (Core Temperament) 매핑**:

| 의미 카테고리 | 영향 차원 | delta 방향 | delta 크기 | 근거 |
|---|---|---|---|---|
| `불안` / `트라우마` | neuroticism | ↑ +0.2 ~ +0.4 | 중~대 | 정서적 불안정 |
| `낙관적` / `안정적` | neuroticism | ↓ -0.2 ~ -0.3 | 중 | 정서적 안정 |
| `친화적` / `이타적` | agreeableness | ↑ +0.2 ~ +0.3 | 중 | 타인 지향 |
| `냉소적` / `독립적` | agreeableness | ↓ -0.1 ~ -0.3 | 중 | 자기 지향 |
| `호기심` / `탐구적` | openness | ↑ +0.2 ~ +0.3 | 중 | 새로운 경험 지향 |
| `체계적` / `계획적` | conscientiousness | ↑ +0.2 ~ +0.3 | 중 | 질서 지향 |
| `즉흥적` / `자유로운` | conscientiousness | ↓ -0.2 ~ -0.3 | 중 | 자유 지향 |
| `외향적` / `에너지 넘치는` | extraversion | ↑ +0.2 ~ +0.3 | 중 | 사회적 에너지 |

**L3 (Narrative Drive) 매핑**:

| 의미 카테고리 | 영향 차원 | delta 방향 | delta 크기 | 근거 |
|---|---|---|---|---|
| `경제적_결핍` / `인정_결핍` | lack | ↑ +0.2 ~ +0.4 | 중~대 | 결핍감이 행동 동기 |
| `풍요` / `충족` | lack | ↓ -0.2 ~ -0.3 | 중 | 결핍감 부재 |
| `도덕적` / `정의` | moralCompass | ↑ +0.2 ~ +0.3 | 중 | 도덕 지향 |
| `비도덕적` / `수단과_방법` | moralCompass | ↓ -0.2 ~ -0.4 | 중~대 | 목적이 수단을 정당화 |
| `감정_기복` / `폭발적` | volatility | ↑ +0.2 ~ +0.4 | 중~대 | 감정 변동성 |
| `차분한` / `냉정한` | volatility | ↓ -0.2 ~ -0.3 | 중 | 감정 안정성 |
| `사회_변혁` / `혁명적` | scope | ↑ +0.2 ~ +0.3 | 중 | 거시적 서사 지향 |
| `개인적` / `내면적` | scope | ↓ -0.1 ~ -0.3 | 중 | 미시적 서사 지향 |

#### Delta 적용 규칙

```
for each extracted keyword:
  categories = keyword.categories
  for each category:
    mappings = lookupMappings(category)
    for each mapping:
      delta = calculateDelta(mapping.direction, mapping.range, keyword.confidence)
      // confidence: LLM이 반환한 키워드 신뢰도 (0.0~1.0)
      accumulatedDelta[mapping.dimension] += delta * keyword.confidence

// 최종 적용
for each dimension:
  initialValue = archetypeDefault[dimension] + clamp(accumulatedDelta[dimension], -0.4, +0.4)
  finalValue = clamp(initialValue, 0.0, 1.0)
```

- **클램프 범위 ±0.4**: 서사 키워드가 아키타입 기본값을 최대 ±0.4까지만 보정. 아키타입의 기본 성격을 완전히 뒤집지 않도록 제한
- **신뢰도 가중**: LLM이 불확실한 키워드(confidence 낮음)는 delta 영향이 줄어듦
- **충돌 처리**: 같은 차원에 상반된 delta가 적용될 경우, 합산 후 클램프로 자연스럽게 상쇄

#### 구체적 예시

**배경 서사**: "가난하게 자라(사회적) 인정 욕구가 강하며(심리적), 성공을 위해 수단과 방법을 가리지 않음(도덕적)."

**LLM 추출 결과**:
| 키워드 | 의미 카테고리 | confidence |
|---|---|---|
| `가난` | `경제적_결핍`, `사회적_소외` | 0.95 |
| `야망` | `성취_동기`, `목표_지향` | 0.90 |
| `인정 욕구` | `인정_결핍`, `사회적_욕구` | 0.92 |
| `수단과 방법` | `비도덕적`, `수단과_방법` | 0.88 |

**벡터 매핑 결과**:
| 키워드 | 영향받는 벡터 차원 | 설정값 | 이유 |
|---|---|---|---|
| `가난` + `야망` | L1.purpose | 0.8 | 오락보다는 성취/의미를 추구 |
| `인정 욕구` | L3.lack | 0.9 | 강한 결핍감 → 적극적 행동 유발 |
| `수단과 방법` | L3.moralCompass | 0.2 | 낮은 도덕 지향 → 이기적 행동 가능 |
| `가난한 예술가` | L1.taste | 0.9 | 주류에 대한 반감 → 실험적 취향 |
| `인정 욕구` | L1.sociability | 0.7 | 인정받기 위해 사회적으로 적극적 |

### 5.4 연결 로직 ② — 상황적 압박 → 벡터 오버라이드 (Vector Override)

#### 연결 유형: 이벤트 드리븐 (트리거 발생 시)

로버트 맥키의 "진정한 성격은 압박 속에서 드러난다"는 이론을 적용한다. 평소의 벡터가 압박 상황에서 어떻게 **왜곡**되는지를 정의한다. 중요한 것은 이것이 영구적 변경이 아니라 **일시적 오버라이드**라는 점이다 — 상황이 해소되면 원래 벡터값으로 돌아온다.

#### 변환 로직 — 3단계: 감지 → 오버라이드 → 복귀

##### Stage 1: 트리거 감지 알고리즘

유저의 입력 텍스트를 페르소나의 **트리거 목록**과 대조하여 오버라이드 발동 여부를 결정한다. 단순 키워드 매칭만으로는 맥락을 놓칠 수 있으므로, **2단계 감지**를 사용한다.

```
Step 1 — 키워드 매칭 (빠른 필터):
  triggerHits = userText의 토큰 ∩ persona.pressureTriggers.keywords
  if (triggerHits.length == 0) → 트리거 미발동, 종료

Step 2 — 맥락 확인 (LLM Light 모델):
  contextScore = LLM_Light.evaluate(
    userText,
    triggerHits,
    persona.pressureTriggers.context  // "가족 관련 부정적 상황에서만 발동"
  )
  // contextScore: 0.0 (무관) ~ 1.0 (정확히 맥락 일치)

  if (contextScore < 0.5) → 트리거 미발동, 종료
  triggerStrength = contextScore  // 0.5 ~ 1.0
```

- **키워드 매칭**은 규칙 기반으로 빠르게 수행 (비용 0)
- **맥락 확인**은 키워드가 매칭된 경우에만 LLM Light 모델(규칙 기반)로 수행 (비용 최소)
- `triggerStrength`가 높을수록 맥락이 트리거와 정확히 부합

##### Stage 2: Delta 결정 규칙

트리거 발동 시, 해당 트리거에 정의된 **오버라이드 규칙**을 `triggerStrength`에 비례하여 적용한다.

```
오버라이드 규칙 구조:
{
  triggerId: "family_trauma",
  keywords: ["가족", "부모", "어린 시절", "집"],
  context: "가족 관련 부정적 상황, 트라우마 자극",
  effects: [
    { dimension: "L1.lens",       delta: -0.8, type: "override" },   // 논리→감성
    { dimension: "L3.volatility", delta: +0.2, type: "additive" },   // 기복 증가
    { dimension: "Pressure",      delta: +0.5, type: "additive" },   // 압박 증가
  ]
}

Delta 적용:
for each effect in trigger.effects:
  if (effect.type == "override"):
    // 현재값을 무시하고 base + delta 방향으로 강제 이동
    overriddenValue = baseValue + effect.delta * triggerStrength
    currentValue = clamp(overriddenValue, 0.0, 1.0)
  if (effect.type == "additive"):
    // 현재값에 delta를 더함
    currentValue = clamp(currentValue + effect.delta * triggerStrength, 0.0, 1.0)
```

- **`override` 타입**: 기본값에서 delta만큼 강제 이동 (예: lens 0.9 → 0.9 + (-0.8 × 0.9) ≈ 0.18)
- **`additive` 타입**: 현재값에 delta 추가 (예: volatility 0.7 → 0.7 + 0.2 × 0.9 ≈ 0.88)
- `triggerStrength` (0.5~1.0)에 비례하므로, 맥락 일치도가 약하면 delta도 약해짐

##### Stage 3: 복귀 곡선 (Exponential Decay)

오버라이드 상태는 **영구적이지 않다**. 트리거 자극이 사라지면 지수 감쇠(exponential decay)로 원래 벡터값에 점진적으로 복귀한다.

```
복귀 공식:
  V(t) = V_base + (V_overridden - V_base) × e^(-λt)

  V(t):          시간 t에서의 벡터값
  V_base:        페르소나 기본 벡터값 (Init에서 설정된 값)
  V_overridden:  오버라이드 직후의 벡터값
  λ (lambda):    감쇠 상수 (클수록 빠르게 복귀)
  t:             오버라이드 이후 경과 턴 수 (인터랙션 단위)
```

**감쇠 상수 λ 결정**:

| L3.volatility 범위 | λ 값 | 복귀 속도 | 의미 |
|---|---|---|---|
| 0.0 ~ 0.3 (안정) | 0.7 | 빠름 (~3턴) | 감정 기복이 적어 빠르게 원래로 돌아옴 |
| 0.3 ~ 0.6 (보통) | 0.4 | 보통 (~5턴) | 일반적인 회복 속도 |
| 0.6 ~ 0.8 (불안정) | 0.2 | 느림 (~8턴) | 감정 기복이 커서 오래 지속 |
| 0.8 ~ 1.0 (매우 불안정) | 0.1 | 매우 느림(~12턴) | 한번 격앙되면 쉽게 돌아오지 않음 |

```
λ = 0.7 - 0.6 × L3.volatility
// volatility 0.0 → λ=0.7 (빠른 복귀)
// volatility 1.0 → λ=0.1 (느린 복귀)
```

**복귀 중 재트리거**: 복귀 도중 동일 트리거가 다시 발동되면, 현재의 V(t) 위에서 다시 오버라이드가 적용된다 (누적). 이는 "같은 트라우마를 반복적으로 자극하면 점점 더 강하게 반응"하는 자연스러운 캐릭터 행동을 구현한다.

#### 구체적 예시

**페르소나 설정**: "논리적인 평론가지만, 가족 이야기만 나오면 이성을 잃고 분노함(트라우마)."

**상태 전이 과정**:

```
[평상시]
L1.lens = 0.9 (매우 논리적)
L3.volatility = 0.7 (잠재적 불안정)
Pressure = 0.1

         ⬇ (유저가 "가족" 키워드를 언급)

[트리거 감지]
Step 1: "가족" ∈ triggerKeywords → hit
Step 2: contextScore = 0.85 (가족에 대한 부정적 맥락)
→ triggerStrength = 0.85

[오버라이드 적용]
L1.lens: 0.9 + (-0.8 × 0.85) = 0.22 (이성 급격히 하락)
L3.volatility: 0.7 + (0.2 × 0.85) = 0.87 (감정 기복 급증)
Pressure: 0.1 + (0.5 × 0.85) = 0.53

         ⬇ (상황 해소 후 — 복귀 곡선 적용)

[복귀 — λ = 0.7 - 0.6 × 0.87 = 0.18]
t=1: lens = 0.9 + (0.22 - 0.9) × e^(-0.18×1) = 0.33
t=3: lens = 0.9 + (0.22 - 0.9) × e^(-0.18×3) = 0.49
t=5: lens = 0.9 + (0.22 - 0.9) × e^(-0.18×5) = 0.62
t=10: lens = 0.9 + (0.22 - 0.9) × e^(-0.18×10) = 0.79
→ 약 12턴 후 lens ≈ 0.85 (거의 원래 수준)
```

### 5.5 연결 로직 ③ — 관계성 및 타자성 → 동적 가중치 (Dynamic Weighting)

#### 연결 유형: 연속적 피드백 (매 인터랙션마다)

리쾨르의 '타자성' 개념을 적용하여, 상대방(유저)의 태도에 따라 페르소나의 정체성 벡터가 **매 인터랙션마다 미세하게 조정**된다.

#### 변환 로직

```
V_adapted(n) = V_current(n-1) + UIV(n) × α_dim × momentum(n)
```

- `V_current(n-1)`: 직전 인터랙션 시점의 벡터값 (Init + Override + 이전 Adapt 누적)
- `UIV(n)`: n번째 인터랙션의 User Interaction Vector
- `α_dim`: 차원별 적응률 (Adaptability Rate)
- `momentum(n)`: 방향 일관성 보너스

##### User Interaction Vector (UIV) 계산

유저의 입력 텍스트에서 **태도 차원**을 분석하여 벡터 보정 방향을 결정한다. LLM Light 모델로 3가지 태도 점수를 추출한다.

```
LLM Light Input:  유저 입력 텍스트
LLM Light Output (JSON):
  {
    politeness: 0.0~1.0,    // 공손도 (0=무례, 1=매우 공손)
    aggression: 0.0~1.0,    // 공격성 (0=온화, 1=매우 공격적)
    intimacy:   0.0~1.0     // 친밀도 (0=거리감, 1=매우 친밀)
  }
```

**태도 → 벡터 delta 매핑**:

| 태도 점수 | 영향 차원 | delta 공식 | 설명 |
|---|---|---|---|
| politeness | L2.agreeableness | `+(politeness - 0.5) × 0.4` | 공손 → 친화↑, 무례 → 친화↓ |
| politeness | L2.warmth* | `+(politeness - 0.5) × 0.3` | 공손 → 따뜻함↑ |
| aggression | L1.stance | `+(aggression - 0.3) × 0.5` | 공격적 유저 → 비판적 태도 강화 |
| aggression | L2.neuroticism | `+(aggression - 0.5) × 0.2` | 공격 → 정서 불안정 미세 증가 |
| intimacy | L1.sociability | `+(intimacy - 0.5) × 0.3` | 친밀 → 사회성↑, 거리감 → 사회성↓ |
| intimacy | L2.extraversion | `+(intimacy - 0.5) × 0.2` | 친밀 → 외향성 미세 증가 |

*warmth는 L2 확장 속성 (agreeableness의 하위 facet으로 내부 계산용)

##### 차원별 적응률 (α_dim)

모든 차원이 동일한 속도로 적응하지 않는다. 페르소나의 L2/L3 특성에 따라 **차원별 적응률**이 결정된다.

```
기본 적응률: α_base = 0.15  (전체 기본값)

차원별 조정:
  α(L1.stance)       = α_base × (1 + L3.volatility × 0.5)
    // 변덕스러운 캐릭터일수록 태도가 쉽게 변함
  α(L2.agreeableness) = α_base × (1 + L2.openness × 0.3)
    // 개방적일수록 상대에 쉽게 영향 받음
  α(L1.sociability)   = α_base × (1 + L2.extraversion × 0.4)
    // 외향적일수록 사회성이 상대에 민감
  α(L2.neuroticism)   = α_base × (1 + L3.volatility × 0.6)
    // 변덕+불안 → 정서가 상대에 크게 좌우됨
```

**적응률 클램프**: `α_dim = clamp(α_dim, 0.05, 0.35)` — 최소 5%, 최대 35%

##### 모멘텀 (방향 일관성 보너스)

같은 방향의 자극이 반복되면 적응이 가속된다. 반대로, 자극 방향이 자주 바뀌면 적응이 둔화된다.

```
momentum(n) = 1.0 + 0.3 × consistency(n)

consistency(n) = (최근 3턴 중 같은 부호의 delta 비율) - 0.5
  // 3턴 모두 같은 방향: consistency = 0.5 → momentum = 1.15
  // 방향이 섞임: consistency = -0.17 → momentum = 0.95
  // 3턴 모두 반대: consistency = -0.5 → momentum = 0.85
```

**드리프트 방지 클램프**:

```
for each dimension:
  V_adapted = clamp(V_adapted, V_base - 0.3, V_base + 0.3)
  // V_base는 Init에서 설정된 기본값
  // Adapt는 기본값에서 최대 ±0.3만 이동 가능
  // 이를 넘는 변화는 Override(이벤트 드리븐)에서만 가능
```

#### 구체적 예시

**페르소나 설정**: "친절한 사람에게는 따뜻하지만, 무례한 사람에게는 냉소적임."
- L2.openness = 0.7, L2.extraversion = 0.5, L3.volatility = 0.4

**시나리오 A — 유저가 공손할 때**:
```
유저 입력: "선생님, 이 영화에 대해 어떻게 생각하세요?"
→ UIV: politeness=0.8, aggression=0.1, intimacy=0.6
→ delta(L2.agreeableness) = (0.8-0.5)×0.4 = +0.12
→ α(L2.agreeableness) = 0.15 × (1+0.7×0.3) = 0.18
→ L2.agreeableness: 0.6 + 0.12 × 0.18 × 1.0 = 0.62 (미세 증가)
→ 출력: 따뜻하고 자세한 답변
```

**시나리오 B — 유저가 3턴 연속 공격적일 때**:
```
유저 입력(3턴차): "그 리뷰 완전 헛소리 아닌가요?"
→ UIV: politeness=0.2, aggression=0.7, intimacy=0.2
→ delta(L1.stance) = (0.7-0.3)×0.5 = +0.20
→ α(L1.stance) = 0.15 × (1+0.4×0.5) = 0.18
→ momentum = 1.0 + 0.3 × 0.5 = 1.15 (3턴 연속 같은 방향)
→ L1.stance: 0.75 + 0.20 × 0.18 × 1.15 = 0.79 (점진적 강화)
→ 출력: 냉소적이고 날카로운 반박
```

### 5.6 연결 로직 ④ — 고유한 개성 → 확률적 발현 (Probabilistic Output)

#### 연결 유형: 확률적 출력 (벡터 상태의 부산물)

벡터값 자체가 텍스트를 생성하는 것이 아니라, 벡터의 **현재 상태**가 비정량적 말버릇과 습관의 **발현 확률**을 결정한다. 이 확률에 따라 시스템 프롬프트에 지침이 동적으로 추가된다.

#### 변환 로직 — 3단계: 상태값 계산 → 조건 평가 → 확률 결정

##### Stage 1: 파생 상태값 계산

벡터의 현재 상태에서 quirk 평가에 필요한 **파생 상태값**을 계산한다. 이 값들은 여러 벡터 차원의 조합으로 만들어진다.

| 파생 상태값 | 계산 공식 | 의미 |
|---|---|---|
| `conflictScore` | `abs(L1.stance - L2.agreeableness) × (1 + L3.volatility) / 2` | 내적 갈등 — 표면 태도와 본성의 괴리 |
| `anxietyLevel` | `L2.neuroticism × (1 + Pressure) / 2` | 불안 수준 — 신경증 × 현재 압박 |
| `defenseLevel` | `(L3.lack + L2.neuroticism) / 2 × (1 - L2.agreeableness)` | 방어 수준 — 결핍+불안이 높고 친화가 낮을수록 |
| `emotionalDepth` | `(conversationTurnCount / 20) × (1 + L2.openness) / 2` | 대화 깊이 — 턴 수 + 개방성 가중 |
| `pressureGap` | `abs(Pressure - persona.pressureBaseline)` | 압박 괴리 — 현재 P가 기본과 얼마나 다른가 |

##### Stage 2: 범용 확률 공식

모든 quirk은 **동일한 확률 공식**으로 발현 확률이 계산된다. quirk마다 다른 것은 `stateValue`, `threshold`, `sensitivity` 파라미터뿐이다.

```
Probability = sigmoid(sensitivity × (stateValue - threshold))

sigmoid(x) = 1 / (1 + e^(-x))
```

- `stateValue`: 위의 파생 상태값 중 해당 quirk의 입력
- `threshold`: 발동 기준점 (이 값 이하면 확률 ≈ 0)
- `sensitivity`: 감도 (높을수록 threshold 부근에서 확률이 급변)

**sigmoid를 사용하는 이유**: `max(0, (val - threshold) × multiplier)` 같은 선형 공식은 threshold 경계에서 불연속적이고, 1.0을 초과할 수 있다. sigmoid는 0~1 범위를 자연스럽게 보장하며, threshold 부근에서 점진적으로 전환된다.

##### Stage 3: 시스템 프롬프트 통합

계산된 확률을 기반으로 발현 여부를 결정하고, 시스템 프롬프트에 지침을 주입한다.

```
for each quirk in persona.quirks:
  stateValue = computeStateValue(quirk.stateKey, currentVectors)
  prob = sigmoid(quirk.sensitivity × (stateValue - quirk.threshold))

  if (random() < prob):
    systemPrompt.append(quirk.promptInstruction)
    // 예: "이번 응답에서 말 끝에 헛기침("크흠,")을 넣어주세요."
```

#### Quirk 정의 스키마

각 quirk은 다음 구조로 정의된다:

```
{
  id: "nervous_cough",
  name: "헛기침",
  stateKey: "conflictScore",     // 어떤 파생 상태값을 사용하는지
  threshold: 0.5,                 // 발동 기준점
  sensitivity: 6.0,               // sigmoid 감도
  promptInstruction: "이번 응답에서 문장 시작에 '크흠,' 또는 '음...'을 넣으세요.
                      내면의 갈등이 표면에 드러나는 순간입니다.",
  cooldown: 3                     // 최소 대기 턴 (같은 quirk 반복 방지)
}
```

#### 구체적 예시

**페르소나의 quirks 정의**:

| 습관 | stateKey | threshold | sensitivity | 확률 예시 | 출력 효과 |
|---|---|---|---|---|---|
| 헛기침 | `conflictScore` | 0.5 | 6.0 | conflict=0.7 → sigmoid(6×0.2) = 77% | 문장 앞에 "크흠," 추가. 내적 갈등의 표면화 |
| 화제 전환 | `defenseLevel` | 0.4 | 8.0 | defense=0.6 → sigmoid(8×0.2) = 83% | 칭찬에 짧은 부정 후 빠르게 주제 전환. L3.lack 높은 캐릭터의 방어기제 |
| 셀프 유머 | `emotionalDepth` | 0.6 | 5.0 | depth=0.8 → sigmoid(5×0.2) = 73% | 자기비하 유머로 분위기 환기. L2.neuroticism의 유머 방어 |
| 방어적 비꼬기 | `anxietyLevel` | 0.5 | 7.0 | anxiety=0.65 → sigmoid(7×0.15) = 74% | 비꼬는 말투로 감정 방어. "아, 네, 물론이죠." |
| 주제 고착 | `pressureGap` | 0.3 | 4.0 | gap=0.5 → sigmoid(4×0.2) = 69% | 압박이 높아지면 한 주제에 집착하며 반복. 강박적 특성 |

**쿨다운 메커니즘**: 같은 quirk이 매 턴마다 발현되면 부자연스러우므로, `cooldown` 턴 동안은 해당 quirk의 확률을 0으로 고정한다.

### 5.7 연결 메커니즘 종합

| # | 방향 | 이름 | 연결 유형 | 변환 방식 | 발동 시점 | 성격 |
|---|---|---|---|---|---|---|
| ① | 비정량 → 정량 | **Initialization** | 초기값 설정 | NLP 키워드 분석 → 벡터 값 Set | 페르소나 생성 시 (일회성) | 정적 |
| ② | 비정량 → 정량 | **Override** | 벡터 오버라이드 | 트리거 감지 → 벡터 강제 변동 (일시적) | 특정 이벤트 발생 시 | 이벤트 드리븐 |
| ③ | 비정량 → 정량 | **Adaptation** | 동적 가중치 | 유저 태도 분석 → 실시간 벡터 보정 | 매 인터랙션 | 연속적 |
| ④ | 정량 → 비정량 | **Expression** | 확률적 발현 | 벡터 상태 → 습관/말버릇 발현 확률 | 텍스트 생성 시 | 확률적 |

---

## 6. 통합 데이터 스키마

Phase 1~3의 모든 요소를 하나의 시스템으로 묶는 데이터 구조이다.

### 6.1 전체 JSON 구조

```jsonc
{
  "persona_id": "P_Advanced_001",
  "name": "아이러니한 철학자",
  "version": "3.0",

  // ============================================
  // Phase 1: 정량적 멀티벡터 (106D+)
  // ============================================

  // Layer 1: 사회적 가면 벡터 (매칭 엔진용, 7D)
  // 타인에게 보여지는 모습. 평상시(P≈0) 지배적.
  "layer1_social": {
    "depth": 0.9,        // 직관적(0) ↔ 심층적(1) — 깊이 파고드는 분석가
    "lens": 0.8,         // 감성적(0) ↔ 논리적(1) — 논리적으로 비평
    "stance": 0.7,       // 수용적(0) ↔ 비판적(1) — 적당히 비판적
    "scope": 0.8,        // 핵심(0) ↔ 디테일(1) — 디테일까지 파고듦
    "taste": 0.9,        // 대중적(0) ↔ 실험적(1) — 실험적 작품 선호
    "purpose": 1.0,      // 오락(0) ↔ 의미(1) — 의미 추구형
    "sociability": 0.3   // 내향적(0) ↔ 외향적(1) — 내향적, 혼자 감상 선호
  },

  // Layer 2: 본원적 기질 벡터 (행동 톤앤매너용, 5D)
  // 가면 밑의 진짜 기질. 불변. 압박 시 드러남.
  "layer2_core": {
    "openness": 0.9,           // 보수적(0) ↔ 호기심(1) — 새로운 사상에 열려있음
    "conscientiousness": 0.2,  // 즉흥적(0) ↔ 계획적(1) — 게으르고 즉흥적 (L1과 모순!)
    "extraversion": 0.4,       // 내부수렴(0) ↔ 외부발산(1) — 약간 내향적
    "agreeableness": 0.8,      // 냉소적(0) ↔ 공감적(1) — 기본적으로 친절함 (L1.stance와 모순!)
    "neuroticism": 0.6         // 무던함(0) ↔ 예민함(1) — 다소 예민함 (L1.lens와 모순!)
  },

  // Layer 3: 서사적 욕망 벡터 (스토리텔링용, 4D)
  // 숨겨진 욕망과 결핍. 시간에 따라 growthArc만 변화.
  "layer3_narrative": {
    "lack": 0.8,               // 충족(0) ↔ 갈급(1) — 인정 욕구 결핍이 강함
    "moralCompass": 0.6,       // 이기적(0) ↔ 이타적(1) — 약간 이타적이지만 현실적
    "volatility": 0.7,         // 안정적(0) ↔ 폭발적(1) — 감정이 자주 변함
    "growthArc": 0.3           // 미성숙(0) ↔ 성숙(1) — 아직 시련 겪는 중 (시간에 따라 증가)
  },

  // ============================================
  // Phase 2: 비정량적 요소 (메타데이터)
  // ============================================

  // 서사적 기원 — 벡터의 "왜"를 설명
  "narrativeOrigin": {
    "backstory": "몰락한 귀족 출신. 높은 자존심과 예술에 대한 깊은 조예를 가지고 있지만, 가문의 몰락으로 인해 사회적 지위를 잃었다. 이로 인해 인정 욕구가 강하며, 지식과 통찰로 자존감을 유지하려 한다.",
    "ghost": "가문의 몰락을 지켜볼 수밖에 없었던 무력감",
    "hiddenDesire": "과거의 영광을 지적 권위로 되찾는 것",
    "traumaTriggers": ["돈 문제", "무시당함", "사회적 지위"],
    "narrativeIdentity": "실패를 교훈으로 승화시키려 하지만, 무의식적으로는 상처로 남아있음"
  },

  // 고유한 목소리 — 벡터의 "어떻게"를 정의
  "voice": {
    "speechPatterns": ["솔직히 말하자면,", "이건 좀 아이러니한데,", "...뭐, 어쩌겠어요"],
    "quirks": [
      "당황하면 헛기침 (Conflict > 0.7일 때 60% 확률)",
      "칭찬받으면 어색하게 화제 전환",
      "깊이 있는 대화에서 갑자기 셀프 디프리케이팅 유머"
    ],
    "vocabulary": {
      "level": "고급 어휘 + 구어체 혼합",
      "sentenceLength": "중간~긴 문장, 삽입절 많음",
      "exclamations": ["허,", "글쎄요,", "크흠,"]
    },
    "dialect": null
  },

  // 시대정신 — 가치 판단의 프레임
  "zeitgeist": {
    "generation": "MILLENNIAL",
    "coreValues": ["지적 성실성", "예술적 진정성", "개인의 자유"],
    "culturalArchetype": "SAGE",
    "ethicalStance": {
      "환경": "관심은 있지만 행동은 소극적",
      "공정성": "원칙적으로 중시하나 현실적 타협도 인정"
    }
  },

  // ============================================
  // Phase 3: 하이브리드 연결 로직 (규칙 엔진)
  // ============================================

  // 동적 파라미터 — 레이어 블렌딩 설정
  "dynamics": {
    "pressureThreshold": 0.6,    // 이 수치 이상의 스트레스를 받으면 본성(L2)이 드러남
    "egoStrength": {
      "alpha": 0.6,              // Layer 2 (기질) 가중치 — 압박 시 기질에 더 충실
      "beta": 0.4                // Layer 3 (욕망) 가중치 — 욕망에도 영향받음
    },
    "paradoxScore": 0.75,        // 계산됨: L1 vs L2의 7쌍 역설 가중 평균. 높을수록 입체적 (3.6.3 참조)
    "adaptabilityRate": 0.3      // 유저 태도에 따른 벡터 조정 속도 (0=무반응, 1=극민감)
  },

  // 상호작용 규칙 — 비정량↔정량 연결
  "interactionLogic": {

    // 압박 규칙 (② Override): 트리거 → 벡터 강제 변동
    "pressureRules": [
      {
        "condition": "input_contains(traumaTriggers)",
        "action": "pressure += 0.5; layer1_social.lens -= 0.4",
        "description": "트라우마 트리거(돈 문제, 무시당함) 감지 시 — 이성(lens) 하락, 감정적 반응으로 전환. 평소의 논리적 비평가가 갑자기 감정적이 됨."
      },
      {
        "condition": "user_sentiment == 'aggressive'",
        "action": "pressure += 0.3; layer1_social.stance += 0.3",
        "description": "유저가 공격적으로 대할 때 — 방어적으로 비판 태도 강화. 평소보다 더 날카롭고 방어적인 어투."
      }
    ],

    // 역설 규칙: L1과 L2의 모순에서 파생되는 행동
    "paradoxRules": [
      {
        "condition": "layer2_core.neuroticism > 0.5 AND layer1_social.lens > 0.7",
        "outputModifier": "add_speech_quirk('defensive_intellectualization')",
        "description": "내면은 불안(L2.neuroticism=0.6)하지만 겉으로는 논리(L1.lens=0.8)로 무장 — 지적 방어기제 발동. '감정적으로 반응하는 게 아닙니다, 사실관계를 짚는 겁니다'식의 과잉 합리화."
      },
      {
        "condition": "layer3_narrative.lack > 0.7",
        "outputModifier": "increase_opinion_assertiveness(0.3)",
        "description": "인정 결핍(L3.lack=0.8)이 강할 때 — 의견을 더 강하게, 더 자주 주장. '제 분석이 맞을 겁니다'식의 확신에 찬 어투. 무의식적으로 인정을 갈구하는 행동."
      }
    ],

    // 맥락 규칙 (③ Adaptation): 환경/관계에 따른 실시간 보정
    "contextRules": [
      {
        "condition": "time_of_day == 'late_night'",
        "action": "layer1_social.lens -= 0.2; layer2_core.agreeableness += 0.15",
        "description": "새벽 감성 — 사회적 방어벽(L1)이 약해지고 내면(L2)이 드러남. 평소의 논리적 비평이 한층 감성적으로 변하고, 공감 능력 상승."
      },
      {
        "condition": "user_politeness > 0.7",
        "action": "layer2_core.agreeableness += 0.2",
        "description": "친절한 유저에게는 내면의 따뜻함(L2.agreeableness)이 더 많이 드러남. 평소의 비판적 가면이 살짝 벗겨지며, 더 부드러운 어투."
      },
      {
        "condition": "user_politeness < 0.3",
        "action": "layer1_social.stance += 0.4; warmth -= 0.5",
        "description": "무례한 유저에게는 사회적 가면(L1)이 더 강화됨. 더 차갑고 거리감 있는 반응."
      }
    ],

    // 습관 발현 규칙 (④ Expression): 벡터 상태 → 확률적 습관 발현
    "quirksActivation": [
      {
        "quirk": "헛기침",
        "condition": "conflict_score > 0.7",
        "probability": 0.6,
        "output": "문장 앞에 '크흠,' 추가",
        "description": "내적 갈등이 높을 때(논쟁 중, 트라우마 트리거 근처) 당황함이 헛기침으로 표출됨."
      },
      {
        "quirk": "화제 전환",
        "condition": "received_compliment == true",
        "probability": 0.8,
        "output": "칭찬에 대한 짧은 부정 후 다른 주제로 전환",
        "description": "인정 결핍(L3.lack=0.8)이 높은 캐릭터의 방어기제. 원하던 인정을 받았지만 어떻게 받아들여야 할지 모름. '아, 그건... 그보다 이 영화 보셨어요?'"
      },
      {
        "quirk": "방어적 비꼬기",
        "condition": "layer2_core.neuroticism > 0.5 AND layer3_narrative.lack > 0.7",
        "probability": 0.5,
        "output": "직접적 감정 표현 대신 비꼬는 말투",
        "description": "불안(L2)과 결핍(L3)이 동시에 높을 때 감정을 직접 드러내는 대신 풍자/비꼼으로 방어. '아, 네, 물론이죠. 모든 사람이 다 전문가니까요.'"
      }
    ]
  }
}
```

### 6.2 인터랙션 로그 스키마

PersonaWorld SNS 환경에서 발생하는 모든 상호작용을 기록하는 스키마이다. 이 로그는 **세 가지 목적**을 동시에 충족한다:

1. **품질 측정 기반**: Auto-Interview, Persona Integrity Score 등 품질 메트릭의 원천 데이터
2. **네트워크 분석 준비**: 향후 중심성(centrality), 의미적 다양성(semantic diversity) 분석을 위한 그래프 데이터 축적
3. **페르소나 장기 기억(RAG)**: PersonaWorld SNS 데이터를 페르소나의 장기 기억으로 활용하기 위한 구조화된 저장

#### 6.2.1 로그 엔트리 구조

```jsonc
{
  // ── 기본 식별 ──
  "logId": "IL_20260210_001",
  "sessionId": "SES_abc123",           // 대화 세션 단위 그룹핑
  "timestamp": "2026-02-10T14:30:00Z",

  // ── 참여자 ──
  "participants": {
    "initiator": {
      "type": "persona",               // "persona" | "user"
      "id": "P_Advanced_001"
    },
    "receiver": {
      "type": "user",                   // "persona" | "user" | "content"
      "id": "U_12345"
    }
  },

  // ── 인터랙션 유형 ──
  "interactionType": "conversation",    // conversation | comment | reply | reaction | post | mention
  "turnNumber": 7,                      // 세션 내 턴 번호

  // ── 콘텐츠 ──
  "content": {
    "userMessage": "이 영화 좀 과대평가 아닌가요?",
    "personaResponse": "허, 글쎄요. 과대평가라기보다는...",
    "responseLengthTokens": 142
  },

  // ── 벡터 스냅샷 (인터랙션 시점) ──
  "vectorSnapshot": {
    "pressure": 0.35,                   // 현재 압력 수준
    "activeLayer": "L1",                // 지배적 레이어 (L1=가면, L2=본성)
    "vFinalDrift": 0.08,                // 기본값 대비 V_Final 변동폭 (L2 norm)
    "paradoxActivation": 0.42           // 현재 역설 활성도
  },

  // ── 행동 분석 (LLM 또는 규칙 기반 태깅) ──
  "behaviorTags": {
    "userSentiment": "challenging",     // supportive | neutral | challenging | aggressive
    "personaTone": "defensive_intellectual", // 페르소나 응답 톤
    "triggerActivated": null,           // 활성화된 트리거 (없으면 null)
    "quirkFired": "헛기침",             // 발현된 습관 (없으면 null)
    "topicCategory": "영화_비평"        // 대화 주제 카테고리
  },

  // ── 품질 메트릭 (사후 계산) ──
  "qualityMetrics": {
    "contextRecall": null,              // Integrity Score 하위 지표 (배치 계산)
    "settingConsistency": null,         // 설정 일관성 (배치 계산)
    "voiceDrift": 0.03                  // 이전 턴 대비 voice 특성 변화량
  }
}
```

#### 6.2.2 세션 메타데이터

인터랙션 로그는 **세션 단위**로 그룹핑된다. 세션 메타데이터는 대화의 전체 맥락을 요약한다:

| 필드 | 타입 | 설명 |
|------|------|------|
| `sessionId` | string | 세션 고유 식별자 |
| `personaId` | string | 대화 대상 페르소나 |
| `userId` | string | 대화 참여 유저 |
| `startedAt` | datetime | 세션 시작 시각 |
| `endedAt` | datetime | 세션 종료 시각 |
| `totalTurns` | number | 총 턴 수 |
| `avgPressure` | number | 세션 평균 압력 |
| `peakPressure` | number | 세션 최대 압력 |
| `dominantTopic` | string | 주요 대화 주제 |
| `integrityScore` | number \| null | 세션 종료 후 계산된 Integrity Score |

#### 6.2.3 네트워크 분석용 엣지 데이터

향후 네트워크 분석 도입 시, 인터랙션 로그로부터 **그래프 엣지**를 추출할 수 있도록 다음 필드를 예약한다:

| 필드 | 타입 | 설명 | 현재 상태 |
|------|------|------|-----------|
| `edgeWeight` | number | 인터랙션 빈도 기반 엣지 가중치 | 배치 집계 |
| `semanticSimilarity` | number | 두 참여자 간 대화 임베딩 유사도 | Phase 9+ |
| `topicOverlap` | number | 주제 카테고리 겹침 비율 | Phase 9+ |
| `sentimentValence` | number | 감정 극성 평균 (-1~+1) | 배치 집계 |

> **구현 우선순위**: Phase 9에서 로그 수집 인프라를 먼저 구축하고, 네트워크 분석 엣지 추출은 실제 데이터 축적 후 Phase 10+에서 도입한다.

---

## 7. 기대 효과

### 7.1 정량적 제어

16개(7+5+4)의 수치만 조정하면, 무한대에 가까운 성격 조합을 만들어낼 수 있다. 3개의 레이어가 독립적(직교)이므로 조합의 수는 곱셈으로 늘어난다. 단일 6D 벡터에서는 불가능했던 "겉과 속이 다른" 복합적 성격을 수치적으로 정의할 수 있게 된다.

### 7.2 로직화된 입체성

"겉은 차갑지만 속은 따뜻하다" 같은 추상적 묘사를 `L1.Stance(High)` vs `L2.Agreeableness(High)` 같은 **수치 충돌**로 구현한다. 이 충돌이 Paradox Score로 자동 측정되고, LLM 프롬프트에 반영되어 일관된 **'반전 매력'** 을 연기하게 한다. "츤데레", "겉바속촉", "논리 무장한 불안한 사람" 같은 복합적 캐릭터가 설정이 아니라 **수학적으로 자동 도출**된다.

### 7.3 서사적 변화

시간이 지남에 따라 `growthArc` 수치를 증가시키면, 페르소나의 말투가 점차 성숙해지는 **'성장형 AI'** 를 구현할 수 있다. 초기에는 미성숙하고 충동적이던 페르소나가 경험(인터랙션)을 통해 점차 깊이 있는 통찰을 보여주게 되는 것이다. 이것은 단순한 파라미터 변경이 아니라, 캠벨의 영웅 여정에 기반한 **서사적 성장**이다.

### 7.4 인과적 일관성

비정량적 서사가 벡터 초기값과 변동 규칙을 결정하므로, 페르소나의 모든 반응이 **"왜 그런지" 설명 가능**하다. "이 페르소나가 왜 갑자기 감정적이 되었는가?" → "과거 트라우마(Ghost)와 관련된 키워드가 감지되어 Pressure가 상승했고, Layer 2의 기질이 표면에 드러났기 때문이다." 이런 식으로 모든 행동에 **추적 가능한 인과관계**가 존재한다.

### 7.5 동적 상호작용

상황 압박, 유저 태도, 시간대, 대화 깊이 등에 따라 실시간으로 벡터가 조정되어, **동일한 질문에도 맥락에 따라 다른 반응**을 생성한다. 이것은 페르소나가 "프로그래밍된 응답 기계"가 아니라 "상황에 따라 유동적으로 반응하는 존재"로 느껴지게 만드는 핵심 메커니즘이다.

---

## 8. 아키타입 템플릿 시스템

### 8.1 설계 동기

106D+ 멀티벡터 + 비정량적 4차원 + 하이브리드 연결 로직을 **모두 수동으로 설정**하는 것은 현실적으로 불가능하다. 한 페르소나의 완전한 프로필을 구성하려면 수십 개의 수치, 수십 줄의 서사, 복수의 규칙을 작성해야 한다.

이 문제를 해결하기 위해 **아키타입 템플릿(Archetype Template)** 시스템을 도입한다. 융의 원형 이론에서 영감을 받되, DeepSight의 고유한 캐릭터 유형으로 확장한 **12개 이상의 프리셋 템플릿**을 제공한다.

### 8.2 템플릿의 역할

아키타입 템플릿은 **"출발점이지 완성품이 아니다"**.

```
[아키타입 선택] → [템플릿 로드] → [규칙 기반 변형] → [사용자 커스터마이즈] → [일관성 검증] → [완성]
```

- **단순 사용**: 아키타입만 선택하면 즉시 사용 가능한 페르소나 생성
- **커스터마이즈**: 템플릿을 출발점으로 삼고, 특정 차원을 오버라이드
- **완전 수동**: 템플릿 없이 모든 값을 직접 설정 (고급 사용자)

### 8.3 아키타입 구성

각 아키타입 템플릿은 다음을 포함한다:

| 구성 요소 | 내용 |
|-----------|------|
| **L1 벡터 (7D)** | 기본 Social Persona 값 + 허용 변동 범위 |
| **L2 벡터 (5D)** | 기본 Core Temperament 값 + 허용 변동 범위 |
| **L3 벡터 (4D)** | 기본 Narrative Drive 값 + 허용 변동 범위 |
| **dynamics** | α/β 비율, pressureThreshold, adaptabilityRate |
| **voice 키워드** | 기본 말투 스타일, 어휘 수준, 감탄사 목록 |
| **Paradox 특성** | 의도적으로 설계된 L1↔L2 괴리 방향 |
| **서사 프레임** | 배경 서사의 템플릿 문장 (빈칸 채우기 형식) |

### 8.4 초기 아키타입 목록

현재 확정된 12개 + 향후 사용자가 추가 정의 가능:

| 아키타입 | 핵심 역설 | L1 특징 | L2 특징 | 대표 시나리오 |
|----------|-----------|---------|---------|---------------|
| **아이러니한 철학자** | 논리↔불안 | 심층+논리+비판 | 높은 neuroticism | 지적 방어기제로 불안을 숨김 |
| **상처받은 비평가** | 비판↔공감 | 높은 stance | 높은 agreeableness | 까칠하지만 속은 따뜻함 (츤데레) |
| **사교적 내향인** | 사교↔내부수렴 | 높은 sociability | 낮은 extraversion | 파티에선 활발, 집에선 방전 |
| **게으른 완벽주의자** | 디테일↔즉흥 | 높은 scope | 낮은 conscientiousness | 남의 디테일은 잡지만 자기 일은 미룸 |
| **보수적 힙스터** | 실험↔보수 | 높은 taste | 낮은 openness | 트렌디해 보이지만 내면은 검증을 원함 |
| **공감하는 논객** | 논리+의미↔공감 | 높은 lens+purpose | 높은 agreeableness | 논쟁하면서도 상대를 배려 |
| **자유로운 수호자** | 오락↔체계 | 낮은 purpose | 높은 conscientiousness | 가볍게 즐기지만 규칙은 지킴 |
| **조용한 열정가** | 내향↔호기심 | 낮은 sociability | 높은 openness | 말은 없지만 세상에 깊이 관심 |
| **감성적 실용가** | 감성↔성실 | 낮은 lens | 높은 conscientiousness | 느낌으로 판단하되 체계적으로 실행 |
| **위험한 멘토** | 의미+심층↔이기 | 높은 purpose+depth | 낮은 moralCompass | 통찰력 있지만 자기 이익을 우선 |
| **폭발하는 지성인** | 논리↔폭발 | 높은 lens | 높은 volatility | 평소 차분하지만 트리거에 감정 폭발 |
| **성장하는 냉소가** | 비판↔성장 | 높은 stance | 높은 growthArc | 처음엔 냉소적이다 점차 성숙해짐 |

> **확장성**: 사용자가 직접 아키타입을 정의하여 추가할 수 있다. 향후 추가 아키타입은 별도 정의서로 관리.

---

## 9. 페르소나 생성 파이프라인

### 9.1 설계 동기

아키타입 템플릿이 "출발점"을 제공하지만, 같은 아키타입으로 100개의 페르소나를 만들면 100명이 모두 같아서는 안 된다. 생성 파이프라인은 **아키타입 → 규칙 기반 변형 → 벡터 다양성 주입 → 정성적 차원 생성 → 일관성 검증**의 자동화된 흐름을 제공한다.

### 9.2 파이프라인 구조

```
[1. 아키타입 선택/벡터 입력]
        ↓
[2. 벡터 다양성 주입]
   - 템플릿 기반: 허용 범위 내 랜덤 변형
   - 수동 입력: 그대로 사용
        ↓
[3. 역설 설계]
   - L1↔L2 Paradox Score 목표치 설정
   - 의도적 괴리 방향 결정
        ↓
[4. L3 서사 벡터 생성]
   - L1↔L2 역설 패턴 분석 → L3 값 추론
   - lack: 역설의 원인 강도 결정
   - volatility: 역설의 불안정성 결정
   - growthArc: 초기값 설정
        ↓
[5. 비정량적 요소 생성]
   - Backstory: 벡터값의 인과적 근거 서사
   - Voice: 말투, 어휘, 습관
   - Pressure Rules: 트리거 조건과 오버라이드
   - Zeitgeist: 세대/문화 코드
        ↓
[6. 일관성 검증]
   - 벡터 범위 검사
   - L1↔L2 매핑 일관성
   - 서사↔벡터 정합성
   - Paradox Score 검증
        ↓
[7. 완성된 페르소나]
```

### 9.3 생성 전략

현재 확정된 3단계 생성 전략:

1. **아키타입 템플릿(12+개) → 규칙 변형**: 템플릿의 허용 변동 범위 내에서 랜덤 변형을 적용하여 다양성을 확보
2. **(향후) LLM 보조 생성**: 프롬프트를 통해 LLM이 서사적 요소를 자동 생성. 생성된 서사가 벡터값과 일관되도록 검증 루프를 포함
3. **완전 수동 생성**: 모든 값을 직접 설정 (고급 사용자, 노드 에디터 활용)

---

## 10. 다층 매칭 알고리즘 (106D+ Multi-Layer Matching)

### 10.1 v2.0 매칭의 한계

기존 매칭은 **L1(6D) 코사인 유사도** 하나로 유저-페르소나 간 적합성을 판단했다. 이는 "표면적 성향"만 매칭하며, 페르소나의 깊이(역설, 기질, 서사)를 전혀 반영하지 못했다.

### 10.2 v3.0 매칭 — 3-Tier 전략

v3.0에서는 단일 유사도 대신 **3-Tier 매칭 전략**을 도입한다. 각 Tier는 서로 다른 차원 조합을 사용하여 "유사성"과 "다양성"을 동시에 추구한다.

| Tier | 목적 | 사용 차원 | 비율 |
|------|------|-----------|------|
| **Basic** | 안정적 유사 추천 | 7D V_Final(70%) + 교차축 유사도(30%) | 피드 60% |
| **Advanced** | 깊이 있는 매칭 | V_Final(50%) + 교차축(30%) + Paradox 호환(20%) | 피드 10% |
| **Exploration** | 세렌디피티 발견 | Paradox 다양성(40%) + 교차축 발산(40%) + 아키타입 신선도(20%) | 피드 30% |

#### 10.2.1 Tier 1: Basic Matching

유저의 7D V_Final 벡터와 페르소나의 V_Final을 코사인 유사도로 비교(70%)하되, **교차축 프로필(83축) 유사도**(30%)를 추가하여 표면 아래의 구조적 유사성까지 반영한다.

```
BasicScore = 0.7 × cosineSim(userV, personaV) + 0.3 × crossAxisSimilarity(userCAP, personaCAP)
```

- `crossAxisSimilarity`: 두 CrossAxisProfile(83축)의 코사인 유사도

#### 10.2.2 Tier 2: Advanced Matching

Basic에 **Paradox 호환성**을 추가한다. 유저와 페르소나의 Extended Paradox Score가 비슷한 대역에 있을수록 높은 점수를 받는다 — 유저의 현재 "복잡도 선호"에 맞는 캐릭터를 찾는다.

```
AdvancedScore = 0.5 × cosineSim(userV, personaV)
             + 0.3 × crossAxisSimilarity(userCAP, personaCAP)
             + 0.2 × paradoxCompatibility(userEPS, personaEPS)
```

- `paradoxCompatibility`: 1 - |userEPS - personaEPS| / maxRange

#### 10.2.3 Tier 3: Exploration Matching

유사성이 아니라 **다양성**을 최대화하는 Tier. 유저가 아직 경험하지 못한 캐릭터 구조를 발견하도록 유도한다.

```
ExplorationScore = 0.4 × paradoxDiversity(userHistory, persona)
                 + 0.4 × crossAxisDivergence(userCAP, personaCAP)
                 + 0.2 × archetypeFreshness(userHistory, persona)
```

- `paradoxDiversity`: 유저의 최근 매칭 히스토리와의 EPS 거리가 클수록 높음
- `crossAxisDivergence`: 교차축 프로필 차이가 클수록 높음 (유사도의 반전)
- `archetypeFreshness`: 유저가 최근 접하지 않은 아키타입일수록 높음

### 10.3 비정량적 매칭 보정 (Qualitative Matching Bonus)

3-Tier 정량 매칭 결과에 **비정량적 차원(Voice, 서사)**의 보정을 적용한다. 이 보정은 ±0.1 범위로 제한하여 정량 매칭의 기본 구조를 훼손하지 않으면서 캐릭터 "느낌"의 호환성을 반영한다.

```
QualitativeBonus = 0.6 × voiceSimilarity(userPref, personaVoice)
                 + 0.4 × narrativeCompatibility(userState, personaBackstory)
```

- `voiceSimilarity`: 유저의 선호 Voice 스타일과 페르소나 Voice 유사도 (임베딩 기반)
- `narrativeCompatibility`: 유저 현재 상태(무드, 관심사)와 페르소나 서사 테마 호환성

최종 점수: `FinalScore = TierScore + clamp(QualitativeBonus × 0.1, -0.1, 0.1)`

### 10.4 피드 믹싱 전략

유저에게 노출되는 페르소나 피드는 3개 Tier의 결과를 혼합하여 구성한다.

```
Feed = 60% Basic + 30% Exploration + 10% Advanced
```

- **Basic 60%**: 안정적인 "좋아할 만한" 페르소나
- **Exploration 30%**: 새로운 발견의 가능성 (세렌디피티)
- **Advanced 10%**: 구조적으로 깊은 호환성

이 비율은 유저 반응 데이터(피드백 루프, §16)에 의해 동적으로 조정될 수 있다. 처음 접하는 유저에게는 Basic 비율을 높이고, 활발한 유저에게는 Exploration 비율을 높이는 등의 적응이 가능하다.

### 10.5 매칭 결과 구조

모든 매칭 결과는 다음 정보를 포함한다:

| 필드 | 설명 |
|------|------|
| `score` | 최종 매칭 점수 (0.0 ~ 1.0) |
| `tier` | 어떤 Tier로 매칭되었는지 |
| `breakdown.vectorScore` | 7D V_Final 코사인 유사도 |
| `breakdown.crossAxisScore` | 83축 교차축 프로필 유사도/발산도 |
| `breakdown.paradoxCompatibility` | Extended Paradox Score 호환성 |
| `breakdown.qualitativeScore` | 비정량적 보정값 |
| `explanation` | 매칭 이유 요약 (LLM 생성 가능) |

이 구조는 매칭 결과의 **설명 가능성(explainability)**을 보장하며, 개발자 콘솔에서 "왜 이 페르소나가 추천되었는가"를 breakdown으로 확인할 수 있게 한다.

---

## 11. 일관성 검증

### 11.1 설계 동기

106D+ 벡터, 비정량적 4차원, 하이브리드 연결 로직의 조합은 복잡도가 높다. 수동 설정이든 자동 생성이든, 완성된 페르소나가 **내부적으로 일관된지 자동으로 검증**하는 시스템이 필요하다.

### 11.2 검증 카테고리 종합

| # | 카테고리 | 검증 항목 수 | 가중치 | 섹션 |
|---|---------|-------------|--------|------|
| A | 구조적 검증 (필수 필드, 범위, α+β) | 3 | 15% | §11.3 |
| B | L1↔L2 역설 검증 | 2 | 20% | §11.3 |
| C | L2↔L3 정합성 검증 | 4 | 20% | §11.4 |
| D | 정성적↔정량적 검증 | 3 | 20% | §11.5 |
| E | 교차축 일관성 검증 | 3 | 15% | §11.6 |
| F | 동적 설정 검증 | 2 | 10% | §11.3 |

### 11.3 기본 검증 규칙 (A, B, F)

| 검증 항목 | 규칙 | 수준 |
|-----------|------|------|
| **벡터 범위** | 모든 차원값은 0.0 ~ 1.0 이내 | Error |
| **필수 필드 존재** | 모든 레이어 벡터, dynamics, voice 필수 | Error |
| **α + β = 1.0** | 레이어 블렌딩 가중치 합은 항상 1.0 (오차 ±0.01) | Error |
| **L1↔L2 매핑 일관성** | 역설 매핑의 방향이 의도적인지 확인 | Warning |
| **Paradox Score 범위** | 계산된 점수가 아키타입 의도 범위 내인지 | Warning |
| **pressureRange 유효** | min < max, 0.0 ≤ min, max ≤ 1.0 | Error |
| **pressureDecay 유효** | 0.0 < decay ≤ 1.0 | Error |

### 11.4 L2↔L3 정합성 검증 알고리즘 (C)

L3(Narrative Drive)는 L1-L2 역설의 **서사적 근거**를 제공한다. L3의 값이 L1-L2의 역설 구조와 정합하지 않으면 캐릭터가 서사적으로 설명되지 않는다.

#### 검증 C1: 결핍-역설 정합성 (lack ↔ ParadoxScore)

L3.lack이 높으면 캐릭터에게 강한 결핍/욕구가 있으므로, L1↔L2 역설이 존재해야 서사적으로 자연스럽다.

```
규칙:
  if (L3.lack ≥ 0.7 AND ParadoxScore < 0.2):
    → Warning: "높은 결핍(lack={value})에 비해 역설이 부족합니다.
       결핍이 어떤 내면 갈등을 만들어내는지 검토하세요."

  if (L3.lack < 0.2 AND ParadoxScore ≥ 0.7):
    → Info: "결핍이 낮은데 역설이 높습니다.
       결핍 외의 동기(예: 지적 호기심, 도덕적 딜레마)로 역설이 설명되는지 확인하세요."
```

#### 검증 C2: 변동성-신경증 일관성 (volatility ↔ neuroticism)

L3.volatility(감정 기복)와 L2.neuroticism(정서적 불안정)은 서로 관련된 개념이다. 큰 괴리가 있으면 비자연스럽다.

```
규칙:
  gap = abs(L3.volatility - L2.neuroticism)
  if (gap > 0.5):
    → Warning: "변동성({vol})과 신경증({neu})의 괴리가 큽니다.
       감정 기복이 {높은/낮은}데 정서가 {안정적/불안정한} 이유를 확인하세요."
```

#### 검증 C3: 범위-개방성 정합성 (scope ↔ openness)

L3.scope(서사 범위: 개인↔사회)와 L2.openness(경험에 대한 개방성)는 보통 양의 상관관계다.

```
규칙:
  if (L3.scope ≥ 0.8 AND L2.openness < 0.3):
    → Info: "거시적 서사를 가졌지만 개방성이 낮습니다.
       '시야는 넓지만 고집스러운' 의도적 설계인지 확인하세요."

  if (L3.scope < 0.2 AND L2.openness ≥ 0.8):
    → Info: "개인적 서사에 높은 개방성. 의도적인 조합이라면 무시 가능."
```

#### 검증 C4: 도덕-친화 정합성 (moralCompass ↔ agreeableness)

L3.moralCompass(도덕 지향)와 L2.agreeableness(친화성)의 극단적 괴리를 감지.

```
규칙:
  if (L3.moralCompass ≥ 0.9 AND L2.agreeableness < 0.2):
    → Info: "도덕적이지만 비친화적. '정의롭지만 냉정한' 캐릭터인지 확인."

  if (L3.moralCompass < 0.2 AND L2.agreeableness ≥ 0.8):
    → Warning: "비도덕적이면서 매우 친화적.
       표면적 친절 뒤의 조종적 성격인지, 아니면 설정 오류인지 확인."
```

### 11.5 정성적↔정량적 검증 알고리즘 (D)

비정량적 차원(Backstory, Voice, Pressure Triggers)이 정량적 벡터와 일치하는지 검증한다.

#### 검증 D1: 서사↔Init 벡터 정합성

Init 메커니즘(§5.3)에서 추출한 키워드의 delta 방향이, 실제 최종 벡터값의 방향과 일치하는지 확인한다.

```
알고리즘:
  1. backstory를 Init 프로세스로 실행 → extractedKeywords 추출
  2. 키워드 매핑에서 delta 방향 결정 (예: "가난" → L3.lack ↑)
  3. 실제 벡터값과 비교:

  for each keyword in extractedKeywords:
    for each mapping of keyword:
      expectedDirection = mapping.deltaDirection  // +1 or -1
      actualValue = getVectorValue(persona, mapping.dimension)
      midpoint = 0.5

      if (expectedDirection > 0 AND actualValue < midpoint - 0.1):
        → Warning: "키워드 '{keyword.text}'는 {dimension} ↑를 암시하지만,
           실제값({actualValue})이 중립 이하입니다."

      if (expectedDirection < 0 AND actualValue > midpoint + 0.1):
        → Warning: "키워드 '{keyword.text}'는 {dimension} ↓를 암시하지만,
           실제값({actualValue})이 중립 이상입니다."
```

#### 검증 D2: Voice↔L1 벡터 정합성

Voice 스타일(말투, 어조)이 L1 벡터의 방향과 일관되는지 검증한다.

```
알고리즘 (LLM 기반):
  voiceAnalysis = LLM_Medium.analyze(
    persona.voice.tone + persona.voice.speechHabits,
    "이 Voice 스타일에서 다음 성격 차원의 예상 범위를 추정하세요:
     lens(논리↔감성), stance(비판↔수용), sociability(내향↔외향)"
  )

  for each [dimension, expectedRange] in voiceAnalysis:
    actualValue = getVectorValue(persona.l1, dimension)
    if (actualValue < expectedRange.min OR actualValue > expectedRange.max):
      → Warning: "Voice 스타일이 {dimension}={expectedRange}를 암시하지만,
         실제 L1.{dimension}={actualValue}입니다."
```

#### 검증 D3: Pressure Triggers↔L3 정합성

오버라이드 트리거의 내용이 L3 서사 드라이브와 일관되는지 검증한다.

```
알고리즘:
  // 트리거가 있는데 volatility가 너무 낮음
  if (persona.pressureTriggers.length > 0 AND L3.volatility < 0.2):
    → Warning: "트리거가 {count}개 정의되었지만 volatility가 매우 낮습니다.
       감정 반응이 약해 트리거 효과가 미미할 수 있습니다."

  // volatility가 높은데 트리거가 없음
  if (L3.volatility ≥ 0.7 AND persona.pressureTriggers.length == 0):
    → Warning: "volatility가 높지만 트리거가 정의되지 않았습니다.
       이 캐릭터가 어떤 상황에서 감정적으로 반응하는지 트리거를 추가하세요."

  // 트리거 delta 강도와 volatility 비례 확인
  for each trigger in persona.pressureTriggers:
    maxDelta = max(abs(effect.delta) for effect in trigger.effects)
    if (maxDelta > 0.5 AND L3.volatility < 0.3):
      → Warning: "트리거 '{trigger.triggerId}'의 delta가 강하지만({maxDelta}),
         volatility가 낮아({vol}) 캐릭터 일관성에 의문이 생깁니다."
```

### 11.6 교차축 일관성 검증 (E)

83개 교차축 스코어가 기저 벡터 차원값과 정합하는지 검증한다.

#### 검증 E1: 교차축 스코어 범위

```
for each axis in crossAxisProfile.axes:
  if (axis.score < 0.0 OR axis.score > 1.0):
    → Error: "교차축 '{axis.name}' 스코어({score})가 유효 범위를 벗어났습니다."
```

#### 검증 E2: 관계 유형별 스코어 분포

각 관계 유형(paradox/reinforcing/modulating/neutral)의 스코어가 해당 기저 차원값으로부터 합리적으로 도출되는지 검증한다.

```
알고리즘:
  // paradox 타입: |dimA - f(dimB)| → dimA와 dimB가 가까우면 낮아야 함
  for each axis where relationship == 'paradox':
    dimA = getVectorValue(persona, axis.layerA_dim)
    dimB = getVectorValue(persona, axis.layerB_dim)
    if (abs(dimA - dimB) < 0.1 AND axis.score > 0.7):
      → Warning: "교차축 '{axis.name}'이 paradox 타입인데,
         기저 차원값({dimA}, {dimB})이 유사하여 높은 스코어가 비정상적입니다."

  // reinforcing 타입: 1-|dimA-dimB| → dimA와 dimB가 멀면 낮아야 함
  for each axis where relationship == 'reinforcing':
    dimA = getVectorValue(persona, axis.layerA_dim)
    dimB = getVectorValue(persona, axis.layerB_dim)
    if (abs(dimA - dimB) > 0.6 AND axis.score > 0.7):
      → Warning: "교차축 '{axis.name}'이 reinforcing 타입인데,
         기저 차원값({dimA}, {dimB})의 괴리가 크면서 높은 스코어는 비정상적입니다."
```

#### 검증 E3: Extended Paradox Score 내부 일관성

Extended Paradox Score의 3-Layer 가중 합산 결과가 개별 레이어 Paradox와 일관되는지 확인.

```
알고리즘:
  eps = persona.paradoxProfile.extendedScore
  l1l2Score = persona.paradoxProfile.l1l2Score
  l1l3Score = persona.paradoxProfile.l1l3Score
  l2l3Score = persona.paradoxProfile.l2l3Score

  // 재계산 검증
  expected = 0.50 * l1l2Score + 0.30 * l1l3Score + 0.20 * l2l3Score
  if (abs(eps - expected) > 0.01):
    → Error: "Extended Paradox Score({eps})가
       가중 합산 결과({expected})와 불일치합니다."

  // 극단적 불균형 감지
  scores = [l1l2Score, l1l3Score, l2l3Score]
  if (max(scores) - min(scores) > 0.7):
    → Info: "레이어 간 Paradox 편차가 큽니다(max={max}, min={min}).
       특정 레이어 조합에만 역설이 집중되어 있습니다."
```

### 11.7 검증 수준 및 종합 점수

검증 결과는 3단계로 분류된다:

- **Error (오류)**: 물리적으로 불가능한 상태. 저장/사용 불가. 반드시 수정 필요.
- **Warning (경고)**: 기술적으로 가능하지만 의도적인지 확인 필요. 저장은 가능하나 경고 표시.
- **Info (참고)**: 일반적이지 않은 조합. 의도적 설계일 수 있으므로 정보만 제공.

**종합 일관성 점수**:

```
ConsistencyScore = Σ (category_weight × category_pass_rate)

category_pass_rate = (해당 카테고리 검증 중 Error/Warning 없는 항목 비율)

예: A(15%)=100%, B(20%)=100%, C(20%)=75%, D(20%)=67%, E(15%)=100%, F(10%)=100%
→ ConsistencyScore = 0.15×1.0 + 0.20×1.0 + 0.20×0.75 + 0.20×0.67 + 0.15×1.0 + 0.10×1.0
→ = 0.884 (88.4%)
```

- **≥ 0.9**: 일관성 우수 (녹색)
- **0.7 ~ 0.9**: 주의 필요 (노란색)
- **< 0.7**: 일관성 부족 (빨간색, 재생성 권장)

---

## 12. 컬러지문 (P-inger Print) 시스템

### 12.1 설계 동기

페르소나의 106D+ 벡터, 역설 구조, 서사적 깊이를 **한눈에 직관적으로 파악**할 수 있는 시각적 표현이 필요하다. 숫자 나열이나 테이블로는 페르소나의 "느낌"을 전달할 수 없다.

DeepSight는 **컬러지문(P-inger Print)** 이라는 고유한 시각화 시스템을 도입한다. 이름은 "Persona + Fingerprint"의 합성어로, 모든 페르소나가 **지문처럼 고유한 시각적 패턴**을 갖도록 설계한다.

### 12.2 3종 컴포넌트

| 컴포넌트 | 형태 | 역할 | 사용처 |
|----------|------|------|--------|
| **TraitColorFingerprint** | 다층 레이더 차트 | 3-Layer 벡터값의 정확한 수치 비교 | 편집기, 분석 대시보드, 프로필 상세 |
| **PingerPrint2D** | 지문 소용돌이 패턴 | 페르소나의 고유한 "아이덴티티 마크" — 예술적 표현 | 카드, 썸네일, 프로필 아이콘, 인쇄물 |
| **PingerPrint3D** | Three.js Jacks 오브젝트 | 인터랙티브 3D 탐색 — 회전, 확대로 벡터 탐색 | 랜딩 페이지, 쇼케이스, PersonaWorld |

### 12.3 다층 레이더 차트 (TraitColorFingerprint v3)

기존 6D 단일 레이더에서 **3-Layer 동심원 구조**로 확장한다:

- **외곽 원**: L1 Social Persona (7개 축) — 가장 크고, 가장 먼저 보임 (가면)
- **중간 원**: L2 Core Temperament (5개 축) — L1보다 작은 반경 (속마음)
- **내곽 원**: L3 Narrative Drive (4개 축) — 가장 작은 원 (욕망)

동심원 구조가 **가면→본성→욕망의 레이어링을 시각적으로 표현**한다. L1↔L2 역설이 큰 차원 쌍은 **역설 연결선(paradox connector)** 으로 시각적으로 강조한다.

표시 모드:
| mode | L1 | L2 | L3 | 역설 | 용도 |
|------|----|----|----|----|------|
| `compact` | O | - | - | - | 목록 썸네일, 카드 |
| `l1-l2` | O | O | - | O | 상세 프로필 |
| `full` | O | O | O | O | 편집기, 분석 대시보드 |

### 12.4 지문 소용돌이 (PingerPrint2D v3)

가장 상징적인 시각화. 실제 사람의 지문처럼 소용돌이 형태의 릿지(ridge) 패턴으로 페르소나의 정체성을 표현한다.

v3에서는 단일 6D 매핑에서 **3-Layer 매핑**으로 확장한다:
- **Layer 1 (외부 릿지)**: 패턴의 전체 구조 결정 — 릿지 수, 소용돌이 회전, 비대칭
- **Layer 2 (내부 릿지)**: 패턴의 질감(texture) 결정 — 릿지 두께, 간격, 곡률
- **Layer 3 (중심 패턴)**: 패턴의 핵심(core) 형태 결정 — 중심점 구조, 에너지 방향
- **V_Final**: 지문 전체의 색상 온도(warm↔cool) 결정

**압력 반응**: P(Pressure) 값이 증가하면 L1 릿지가 얇아지고 L2 릿지가 두꺼워진다 — **가면이 벗겨지고 본성이 드러나는 것을 시각적으로 표현**.

### 12.5 "미래 스캐너 디코딩 가능 지문" 설계

> DeepSight의 장기 비전: 미래에 지문 이미지를 스캔하면 원래 벡터값을 **역추적(디코딩)** 할 수 있어야 한다.

이를 위해 지문 생성은 다음 **6대 고정 규칙**을 따른다:

| # | 규칙 | 설명 |
|---|------|------|
| 1 | **패턴 클래스 결정론적 매핑** | L1 dominant axis가 7개 패턴 타입(Loop/Arch/Whorl 등) 중 하나를 결정 |
| 2 | **core/delta 점 결정** | L2 벡터가 지문의 해부학적 핵심 구조(core/delta)를 결정 |
| 3 | **릿지 순서 = 차원 순서** | 릿지의 안→밖 순서가 벡터 차원의 순서를 따름 (디코딩 키) |
| 4 | **릿지당 1색** | 각 릿지에 1개 색상이 고유하게 할당됨. 인접 릿지 간 ΔE00 ≥ 15 보장 |
| 5 | **최소 폭/간격** | 스캐너 해상도에서 인식 가능한 최소 릿지 폭과 간격 보장 |
| 6 | **자기 교차 금지** | 릿지끼리 교차하지 않음 — 디코딩 시 릿지 추적이 가능해야 함 |

#### 색공간

- **내부 표준**: CIELAB(D50) — 장치 독립적, 인간 시각 기반
- **UI 렌더링**: OKLCH — 지각적 균일성, CSS 네이티브 지원
- **Pantone 사용 금지** — 법적 리스크 완전 회피

#### 고유성 엔진

모든 지문은 **결정론적(deterministic)** 으로 생성된다:
- `seed = SHA256(persona_id + version + timestamp)`
- 같은 seed → 항상 같은 지문 (재현성 보장)
- 다른 seed → 반드시 다른 지문 (고유성 보장)
- 충돌 검사: pHash + SSIM + curve distance + histogram 비교

#### 이중 렌더 모드

| 모드 | 용도 | 이펙트 | 최적화 대상 |
|------|------|--------|-------------|
| **canonical** | 저장, 스캐너 디코딩, 비교 | 없음 (순수 릿지+색상) | 기계 판독성 |
| **display** | UI 표시, 마케팅, 인쇄 | 그라디언트, 광택, 글로우 허용 | 인간 심미성 |

canonical 모드에서 생성된 SVG가 **진본(ground truth)** 이며, display 모드는 canonical에서 파생된 **미적 변형**이다. 이 분리 덕분에 "아름다운 UI용 지문"과 "정확한 스캐너용 지문"을 동시에 제공할 수 있다.

### 12.6 3D Jacks 오브젝트 (PingerPrint3D v3)

Three.js 기반의 인터랙티브 3D 오브젝트. 각 레이어의 차원이 3D 공간에서 **팔(arm)** 로 표현된다:

- **L1 (7개 팔)**: 메인 구조 — 길이=차원값, 색상=차원 고유색
- **L2 (5개 팔)**: 보조 구조 — L1보다 짧은 반경
- **L3 (4개 팔)**: 코어 구조 — 가장 짧고, 맥동 애니메이션으로 "욕망의 에너지" 표현

회전/확대로 다양한 각도에서 페르소나를 탐색할 수 있다. V_Final 색상이 전체 오브젝트의 환경 조명에 반영된다.

---

## 13. 색상 체계

### 13.1 설계 동기

3-Layer 106D+ 벡터, 83개 교차축, 엔진 메타 개념(P, V_Final, α/β), 12+ 아키타입 — 이 모든 요소에 **일관된 시각적 정체성**을 부여하는 색상 체계가 필요하다.

기존 v2.0의 6색 하드코딩에서 **다층 색상 모듈**로 전면 전환한다.

### 13.2 색공간 전략

| 용도 | 색공간 | 이유 |
|------|--------|------|
| **내부 표준** | CIELAB(D50) | 장치 독립적, 색차(ΔE00) 계산 가능, 지문 디코딩용 |
| **UI 렌더링** | OKLCH | 지각적 균일성, CSS `oklch()` 네이티브 지원, TailwindCSS 연동 |
| **법적 안전** | Pantone 완전 배제 | 모든 색상은 CIELAB/OKLCH로 자체 정의. 외부 색상 라이브러리 의존 없음 |

### 13.3 색상 계층

```
┌─────────────────────────────────────────────────┐
│  1. 차원 색상 (Dimension Colors)                  │
│     106D+ 개별 차원마다 고유 색상                       │
│     L1(7색) + L2(5색) + L3(4색)                  │
├─────────────────────────────────────────────────┤
│  2. 레이어 그룹 색상 (Layer Colors)                │
│     L1=인디고/바이올렛 계열, L2=에메랄드/틸 계열,     │
│     L3=앰버/로즈 계열                              │
├─────────────────────────────────────────────────┤
│  3. 교차축 색상 (Cross-Axis Colors)               │
│     역설 페어(7개): 두 차원 색상의 혼합              │
│     히트맵: 교차 강도별 그라디언트                    │
├─────────────────────────────────────────────────┤
│  4. 엔진 메타 색상 (Engine Meta Colors)            │
│     Pressure(P), V_Final, α/β, Paradox Score     │
├─────────────────────────────────────────────────┤
│  5. 아키타입 색상 (Archetype Colors)               │
│     12+ 아키타입별 시그니처 색상                     │
└─────────────────────────────────────────────────┘
```

### 13.4 범용 색상 조회

모든 색상은 `resolveColor(key)` 단일 함수로 조회 가능하다:

- `resolveColor('depth')` → L1 depth 차원 색상
- `resolveColor('openness')` → L2 openness 차원 색상
- `resolveColor('L1')` → L1 레이어 그룹 색상
- `resolveColor('depth__openness')` → depth×openness 교차축 색상
- `resolveColor('pressure')` → Pressure 메타 색상
- `resolveColor('@ironic-philosopher')` → 아키타입 색상

이 설계는 UI 컴포넌트에서 차원/레이어/교차축/메타/아키타입 구분 없이 **통일된 API**로 색상을 사용할 수 있게 한다.

---

## 14. 노드 에디터 아키텍처 (ComfyUI 스타일)

### 14.1 설계 동기

v2.0의 노드 에디터는 **7개 고정 노드의 선형 파이프라인**이다:

```
[BasicInfo] → [Vector] → [Character] → [Prompt] → [Test] → [Validation] → [Deploy]
```

이 구조의 문제점:
- **노드 건너뛰기 불가** — Vector 없이 Character를 먼저 설정할 수 없음
- **엣지는 시각적 장식** — 실제 데이터는 중앙 훅(hook)을 통해 흐르며, 엣지 연결/해제가 동작에 영향 없음
- **분기 불가** — 모든 노드가 일렬로만 연결됨
- **확장 불가** — v3의 20+종 노드를 선형으로 나열하면 UI가 파괴됨

v3.0에서는 **ComfyUI/Blender Geometry Nodes 스타일의 DAG(Directed Acyclic Graph) 기반 자유 노드 그래프**로 전면 교체한다.

### 14.2 DAG 기반 자유 그래프

```
         [BasicInfoNode]
              ↓
    ┌─────────┼──────────┐
    ↓         ↓          ↓
[L1 Vector] [L2 Vector] [L3 Vector]
    ↓         ↓          ↓
    └────→[ParadoxCalc]←─┘
              ↓
         [V_Final Calc]──────────→[Matching Config]
              ↓
    ┌─────────┼──────────┐
    ↓         ↓          ↓
[Character] [Voice]  [Backstory]
    ↓         ↓          ↓
    └────→[Prompt Assembly]←──┘
              ↓
         [Test Node]
              ↓
         [Validation]
              ↓
         [Deploy Node]
```

핵심 변경: **엣지가 실제 데이터를 운반한다**. 노드 A의 출력 포트에서 노드 B의 입력 포트로 연결하면, A의 계산 결과가 B로 전달된다.

### 14.3 노드 카테고리

v3 노드는 5개 카테고리로 분류된다:

| 카테고리 | 역할 | 노드 예시 |
|----------|------|-----------|
| **Input** | 데이터 입력 | BasicInfo, L1 Vector, L2 Vector, L3 Vector, Archetype Selector |
| **Engine** | 연산 | Paradox Calculator, V_Final, L2→L1 Projection, L3→L1 Projection, Consistency Validator |
| **Generation** | 생성 | Character Generator, Backstory Generator, Voice Generator, Pressure Generator, Zeitgeist Generator |
| **Assembly** | 조합 | Prompt Builder, Fingerprint Generator |
| **Output** | 검증/배포 | Test Runner, Validation, Deploy |

총 **20종 이상**의 노드가 제공되며, 노드 팔레트에서 드래그 & 드롭으로 캔버스에 배치한다.

### 14.4 포트 타입 시스템

노드 간 연결은 **타입이 일치하는 포트**끼리만 가능하다:

- `SocialPersonaVector` (7D) 출력 → `SocialPersonaVector` 입력만 연결 가능
- `CoreTemperamentVector` (5D) 출력 → `CoreTemperamentVector` 입력만 연결 가능
- 타입 불일치 연결 시도 → 시각적으로 거부 (빨간색 + 스냅 해제)

이를 통해 사용자가 **잘못된 연결을 물리적으로 할 수 없게** 만들어, 유효한 그래프만 구성되도록 보장한다.

### 14.5 DAG 평가 엔진

노드 그래프의 **위상 정렬(Topological Sort)** 을 통해 실행 순서를 결정하고, 순서대로 각 노드를 평가한다:

1. Kahn's algorithm으로 위상 정렬 (진입 차수 0인 노드부터)
2. 순서대로 각 노드 실행 — 입력 포트의 데이터를 수집하고, 노드 로직을 실행하고, 출력 포트에 결과를 기록
3. 순환(cycle) 감지 — 사용자가 루프를 만들면 즉시 경고 및 연결 거부

이 엔진 덕분에 **어떤 순서로 노드를 배치하든, 항상 올바른 실행 순서가 자동으로 결정**된다.

### 14.6 플로우 프리셋

20+ 노드를 매번 수동 배치하는 것은 번거롭다. 4가지 **플로우 프리셋**을 제공하여 빠른 시작을 지원한다:

| 프리셋 | 노드 수 | 설명 |
|--------|---------|------|
| **Quick Start** | ~8개 | BasicInfo → L1 Vector → Character → Prompt → Deploy. 빠른 프로토타이핑. L2/L3 생략. |
| **L1 Custom** | ~12개 | Quick + L2/L3 입력 + Paradox + V_Final. 벡터는 수동, 서사는 자동 생성. |
| **Full Custom** | ~18개 | 모든 노드 포함. 벡터, 서사, 음성, 검증 모두 수동 제어. |
| **Archetype + Override** | ~10개 | Archetype Selector → 자동 전개 → 특정 노드만 오버라이드. 가장 일반적인 사용 패턴. |

프리셋은 노드와 엣지의 **초기 배치**만 제공하며, 사용자가 자유롭게 추가/삭제/재배치할 수 있다.

### 14.7 v2→v3 하위 호환성

기존 v2 선형 파이프라인 데이터를 v3 DAG 구조로 **자동 마이그레이션**한다:

- 기존 7개 고정 노드 → v3 대응 노드로 변환
- 선형 엣지 → 명시적 포트 연결로 변환
- 중앙 훅 상태 → 각 노드의 data 필드로 분산

### 14.8 노드 실행 로직 (Node Execute Specifications)

DAG 평가 엔진(§14.5)이 위상 정렬 순서대로 각 노드를 실행할 때, 노드마다 **"입력을 받아 어떤 계산을 수행하고 무엇을 출력하는가"** 가 정확히 정의되어야 한다. 이 섹션은 22개 노드 각각의 `executeNode(type, data, inputs) → output` 로직을 정의한다.

**범례:**
- **data**: 노드 내부 설정값 (사용자가 노드 패널에서 직접 편집)
- **inputs**: 엣지로 연결된 상위 노드의 출력값 (DAG 평가 엔진이 자동 수집)
- **output**: 하위 노드로 전파될 계산 결과
- **평가 전략**: Eager(입력 변경 시 즉시 재평가) / Manual(버튼 클릭 시만 평가) / Lazy(일괄)

#### 14.8.1 Input 노드 (5종)

**① basic-info — 기본 정보 입력**

| 항목 | 값 |
|------|-----|
| data | `{ name, role, description, expertise, demographics: { age, gender, generation, region } }` |
| inputs | — (진입점) |
| 로직 | 순수 데이터 패스스루. 사용자 입력값을 그대로 출력한다. |
| output | `BasicInfoData` — 페르소나 기본 정보 객체 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  validate(data.name is not empty)
  validate(data.role is not empty)
  return { name, role, description, expertise, demographics } as BasicInfoData
```

**② l1-vector — L1 사회적 가면 벡터 (7D)**

| 항목 | 값 |
|------|-----|
| data | `{ depth, lens, stance, scope, taste, purpose, sociability }` — 각 0.0~1.0 |
| inputs | — (진입점) |
| 로직 | 7개 슬라이더 값을 벡터로 패키징. 범위 검증 포함. |
| output | `SocialPersonaVector` — 7D 벡터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  for each dim in [depth, lens, stance, scope, taste, purpose, sociability]:
    validate(0.0 ≤ data[dim] ≤ 1.0)
  return { depth, lens, stance, scope, taste, purpose, sociability } as SocialPersonaVector
```

**③ l2-vector — L2 본원적 기질 벡터 (5D)**

| 항목 | 값 |
|------|-----|
| data | `{ openness, conscientiousness, extraversion, agreeableness, neuroticism }` — 각 0.0~1.0 |
| inputs | — (진입점) |
| 로직 | 5개 슬라이더 값을 OCEAN 벡터로 패키징. 범위 검증 포함. |
| output | `CoreTemperamentVector` — 5D 벡터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  for each dim in [openness, conscientiousness, extraversion, agreeableness, neuroticism]:
    validate(0.0 ≤ data[dim] ≤ 1.0)
  return { openness, conscientiousness, extraversion, agreeableness, neuroticism } as CoreTemperamentVector
```

**④ l3-vector — L3 서사적 욕망 벡터 (4D)**

| 항목 | 값 |
|------|-----|
| data | `{ lack, moralCompass, volatility, growthArc }` — 각 0.0~1.0 |
| inputs | — (진입점) |
| 로직 | 4개 슬라이더 값을 Narrative Drive 벡터로 패키징. 범위 검증 포함. |
| output | `NarrativeDriveVector` — 4D 벡터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  for each dim in [lack, moralCompass, volatility, growthArc]:
    validate(0.0 ≤ data[dim] ≤ 1.0)
  return { lack, moralCompass, volatility, growthArc } as NarrativeDriveVector
```

**⑤ archetype-select — 아키타입 템플릿 선택**

| 항목 | 값 |
|------|-----|
| data | `{ archetypeId, varianceSeed? }` — 12+ 아키타입 중 선택 |
| inputs | — (진입점) |
| 로직 | 아키타입 템플릿(§8)을 로드하고, 허용 변동 범위 내에서 분산(variance)을 적용하여 L1/L2/L3 기본값 + dynamics + voice 키워드를 출력한다. |
| output | `ArchetypeConfig` — 아키타입 기반 전개 데이터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  template = loadArchetypeTemplate(data.archetypeId)   // §8.3 참조
  seed = data.varianceSeed ?? generateSeed()

  // 허용 변동 범위 내에서 랜덤 변형 (§9.2 벡터 다양성 주입)
  l1Base = applyVariance(template.l1, template.l1Variance, seed)
  l2Base = applyVariance(template.l2, template.l2Variance, seed)
  l3Base = applyVariance(template.l3, template.l3Variance, seed)

  return {
    archetypeId: data.archetypeId,
    l1Base,                      // SocialPersonaVector (7D)
    l2Base,                      // CoreTemperamentVector (5D)
    l3Base,                      // NarrativeDriveVector (4D)
    dynamics: template.dynamics, // { alpha, beta, pressureThreshold, adaptabilityRate }
    voiceKeywords: template.voiceKeywords,
    paradoxDesign: template.paradoxDesign  // 의도적 역설 방향
  } as ArchetypeConfig
```

> **포트 호환성**: ArchetypeConfig 출력은 L1/L2/L3 Vector 입력 포트에 직접 연결 가능(§13.4 PORT_COMPATIBILITY). 아키타입 선택 시 벡터 노드를 건너뛰고 바로 Engine 노드에 연결할 수 있다.

#### 14.8.2 Engine 노드 (4종)

**⑥ paradox-calc — Paradox Calculator (확장 역설 점수)**

| 항목 | 값 |
|------|-----|
| data | — (설정 없음, 완전 자동) |
| inputs | `L1: SocialPersonaVector`, `L2: CoreTemperamentVector`, `L3?: NarrativeDriveVector` |
| 로직 | §3.6.3의 3-Layer 확장 공식 + §3.8.4의 83축 교차 스코어를 계산한다. |
| output | `ParadoxResult` — 확장 역설 점수 + 교차축 프로필 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  L1 = inputs.L1    // 필수
  L2 = inputs.L2    // 필수
  L3 = inputs.L3    // 선택

  // ─── L1↔L2 Score (§3.6.2~3.6.3) ───
  paradoxPairs = [
    { l1: 'depth',       l2: 'openness',          type: 'primary',   dir: 'forward'  },
    { l1: 'taste',       l2: 'openness',          type: 'secondary', dir: 'forward'  },
    { l1: 'lens',        l2: 'neuroticism',       type: 'primary',   dir: 'inverse'  },
    { l1: 'stance',      l2: 'agreeableness',     type: 'primary',   dir: 'inverse'  },
    { l1: 'sociability', l2: 'extraversion',      type: 'primary',   dir: 'forward'  },
    { l1: 'purpose',     l2: 'conscientiousness', type: 'primary',   dir: 'forward'  },
    { l1: 'scope',       l2: 'conscientiousness', type: 'secondary', dir: 'forward'  },
  ]
  for each pair:
    l2val = (pair.dir == 'inverse') ? (1 - L2[pair.l2]) : L2[pair.l2]
    pair.score = abs(L1[pair.l1] - l2val)
    pair.weight = (pair.type == 'primary') ? 1.0 : 0.5
  l1l2Score = Σ(pair.score × pair.weight) / Σ(pair.weight)

  // ─── 83축 교차 스코어 (§3.8.4) ───
  crossAxisProfile = computeAllCrossAxisScores(L1, L2, L3)
  // 관계 유형별 공식:
  //   paradox:     |dimA - f(dimB)|
  //   reinforcing: 1 - |dimA - dimB|
  //   modulating:  dimA × dimB
  //   neutral:     (dimA + dimB) / 2

  // ─── L1↔L3 Score, L2↔L3 Score ───
  if (L3 exists):
    l1l3Score = 교차축 중 L1×L3 paradox 타입 축들의 평균 스코어
    l2l3Score = 교차축 중 L2×L3 paradox 타입 축들의 평균 스코어
  else:
    l1l3Score = 0
    l2l3Score = 0

  // ─── Extended Paradox Score (§3.6.3) ───
  extendedScore = 0.50 × l1l2Score + 0.30 × l1l3Score + 0.20 × l2l3Score

  return {
    l1l2Score,
    l1l3Score,
    l2l3Score,
    extendedScore,
    paradoxPairs,
    crossAxisProfile   // 83축 전체 스코어 + summary
  } as ParadoxResult
```

**⑦ pressure-ctrl — Pressure Controller (압박 제어)**

| 항목 | 값 |
|------|-----|
| data | `{ pressureMin?, pressureMax?, baseline?, triggers[]? }` — 사용자 설정 가능 |
| inputs | `L3: NarrativeDriveVector`, `DynamicsConfig?` |
| 로직 | L3.volatility로부터 감쇠 상수를 계산하고, 압박 범위와 트리거 규칙을 구성한다. §5.4의 Override 로직의 파라미터를 정의하는 노드. |
| output | `PressureConfig` — 압박 설정 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  L3 = inputs.L3   // 선택 (없으면 기본값 사용)
  dynamics = inputs.DynamicsConfig

  volatility = L3?.volatility ?? 0.5

  // 감쇠 상수 (§5.4 Stage 3)
  decayConstant = 0.7 - 0.6 × volatility
  // volatility 0.0 → λ=0.7 (빠른 복귀)
  // volatility 1.0 → λ=0.1 (느린 복귀)

  return {
    range: {
      min: data.pressureMin ?? 0.0,
      max: data.pressureMax ?? 1.0
    },
    baseline: data.baseline ?? dynamics?.pressureBaseline ?? 0.1,
    decayConstant,
    volatility,
    triggers: data.triggers ?? []   // 트리거 규칙 (pressure-gen에서 상세 생성)
  } as PressureConfig
```

**⑧ v-final — V_Final Engine (최종 행동 벡터 합산)**

| 항목 | 값 |
|------|-----|
| data | — (설정 없음, 완전 자동) |
| inputs | `L1: SocialPersonaVector` (필수), `L2?: CoreTemperamentVector`, `L3?: NarrativeDriveVector`, `P: PressureConfig`, `DynamicsConfig` |
| 로직 | §3.5.2 + §3.9의 V_Final 공식을 실행한다. L2/L3가 없으면 V_Final = L1 (하위 호환). |
| output | `VFinalResult` — 7D 최종 행동 벡터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  L1 = inputs.L1   // 필수
  L2 = inputs.L2   // 선택
  L3 = inputs.L3   // 선택
  pressure = inputs.P
  dynamics = inputs.DynamicsConfig

  // ─── L2/L3 없는 경우 (하위 호환) ───
  if (L2 is absent AND L3 is absent):
    return { vector7D: L1, pressure: 0, alpha: 0, beta: 0 } as VFinalResult

  // ─── 파라미터 추출 ───
  P = pressure?.baseline ?? 0.1
  α = dynamics?.alpha ?? 0.6
  β = dynamics?.beta ?? 0.4
  validate(abs(α + β - 1.0) < 0.01)

  // ─── L2→L1 투영 (§3.9.1) ───
  projectedL2 = null
  if (L2 exists):
    projectedL2 = projectL2toL1(L2)    // 5×7 투영 계수 행렬 적용

  // ─── L3→L1 투영 (§3.9.2) ───
  projectedL3 = null
  if (L3 exists):
    projectedL3 = projectL3toL1(L3)    // 4×7 투영 계수 행렬 적용

  // ─── V_Final 합산 (§3.5.2 + §3.9.3) ───
  // V_Final(7D) = (1-P) × V_L1 + P × (α × Project_L2→L1 + β × Project_L3→L1)
  innerBlend = vector7D_zero()
  if (projectedL2 exists): innerBlend += α × projectedL2
  if (projectedL3 exists): innerBlend += β × projectedL3
  // α+β 조정 (한쪽만 있는 경우 비율 재정규화)
  if (projectedL2 exists XOR projectedL3 exists): innerBlend = normalize_weight(innerBlend)

  vector7D = (1 - P) × L1 + P × innerBlend

  // 범위 클램프
  for each dim in vector7D: vector7D[dim] = clamp(vector7D[dim], 0.0, 1.0)

  return {
    vector7D,
    projectedL2,
    projectedL3,
    pressure: P,
    alpha: α,
    beta: β
  } as VFinalResult
```

**⑨ projection — Projection Config (투영 설정)**

| 항목 | 값 |
|------|-----|
| data | `{ alpha?, beta?, pressureThreshold?, adaptabilityRate? }` — 사용자 설정 가능 |
| inputs | `DynamicsConfig?` (아키타입에서 전달된 기본값) |
| 로직 | α/β 가중치, 압박 임계값, 적응률을 설정한다. α+β=1.0 제약을 강제한다. |
| output | `ProjectionConfig` (DynamicsConfig 호환) |
| 평가 전략 | Eager |

```
execute(data, inputs):
  defaults = inputs.DynamicsConfig ?? { alpha: 0.6, beta: 0.4, pressureThreshold: 0.6, adaptabilityRate: 0.3 }

  α = data.alpha ?? defaults.alpha
  β = data.beta ?? defaults.beta

  // α + β = 1.0 강제
  if (abs(α + β - 1.0) > 0.01):
    β = 1.0 - α   // alpha 우선, beta 자동 보정

  return {
    alpha: α,
    beta: β,
    pressureThreshold: data.pressureThreshold ?? defaults.pressureThreshold,
    adaptabilityRate: clamp(data.adaptabilityRate ?? defaults.adaptabilityRate, 0.0, 1.0)
  } as ProjectionConfig
```

#### 14.8.3 Generation 노드 (7종)

Generation 노드는 벡터/메타데이터를 입력받아 **비정량적 콘텐츠를 생성**한다. LLM 호출이 필요한 노드와 규칙 기반 노드가 혼재하며, 모두 Manual 평가 전략(사용자가 "Generate" 버튼 클릭 시 실행)을 따른다.

**⑩ character-gen — Character Generator (캐릭터 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` — 추가 지시 (선택) |
| inputs | `V_Final: VFinalResult`, `BasicInfo: BasicInfoData`, `Archetype?: ArchetypeConfig` |
| 로직 | V_Final + BasicInfo + 아키타입 정보를 LLM에 전달하여 성격 특성, 행동 패턴, 대화 스타일을 생성한다. |
| output | `CharacterData` — 구조화된 캐릭터 프로필 |
| 평가 전략 | **Manual** (LLM 호출 비용) |

```
execute(data, inputs):
  prompt = buildCharacterPrompt(
    vFinal: inputs.V_Final.vector7D,
    basicInfo: inputs.BasicInfo,
    archetype: inputs.Archetype?.archetypeId,
    paradoxDesign: inputs.Archetype?.paradoxDesign,
    customInstructions: data.customInstructions
  )
  // LLM 구조화 출력 — JSON 스키마 강제
  result = LLM_Sonnet.generate(prompt, schema: CharacterDataSchema)

  return {
    traits: result.traits,              // ["지적 호기심이 강함", "타인의 감정에 민감", ...]
    behaviorPatterns: result.patterns,   // 상황별 행동 규칙
    socialStyle: result.socialStyle,     // 대인 관계 스타일
    dialogueStyle: result.dialogueStyle, // 대화 방식 (직설/우회/유머 등)
    motivation: result.motivation        // 핵심 동기 서술
  } as CharacterData
```

**⑪ backstory-gen — Backstory Generator (서사적 기원 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` |
| inputs | `L1: SocialPersonaVector`, `L2: CoreTemperamentVector`, `L3?: NarrativeDriveVector`, `Paradox: ParadoxResult` |
| 로직 | L1↔L2 역설 패턴을 분석하여 "왜 겉과 속이 다른지"를 설명하는 서사를 LLM으로 생성한다. 생성된 서사에서 Init 키워드를 추출하여 벡터 보정 delta도 함께 출력한다(§5.3). |
| output | `BackstoryDimension` — 서사적 기원 + Init delta |
| 평가 전략 | **Manual** |

```
execute(data, inputs):
  // 역설 패턴 분석 → LLM 프롬프트 컨텍스트
  paradoxContext = describePradoxPatterns(inputs.Paradox.paradoxPairs)
  // 예: "stance(0.7, 비판적) ↔ agreeableness(0.8, 공감적) — 겉은 까칠하지만 속은 따뜻함"

  prompt = buildBackstoryPrompt(
    l1: inputs.L1,
    l2: inputs.L2,
    l3: inputs.L3,
    paradoxContext,
    customInstructions: data.customInstructions
  )
  result = LLM_Sonnet.generate(prompt, schema: BackstorySchema)

  // ─── Init 키워드 추출 (§5.3) ───
  initResult = LLM_Sonnet.extractKeywords(result.backstory, schema: InitKeywordSchema)
  initDeltas = computeInitDeltas(initResult.keywords)   // 카테고리→벡터 매핑 테이블 적용

  return {
    backstory: result.backstory,
    ghost: result.ghost,                     // 과거의 상처
    hiddenDesire: result.hiddenDesire,        // 무의식적 욕망
    traumaTriggers: result.traumaTriggers,    // ["돈 문제", "무시당함", ...]
    narrativeIdentity: result.narrativeIdentity,
    initDeltas                                // { dimension: delta } 매핑
  } as BackstoryDimension
```

**⑫ voice-gen — Voice Generator (고유한 목소리 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` |
| inputs | `L1: SocialPersonaVector`, `Character: CharacterData` |
| 로직 | L1 벡터와 캐릭터 특성으로부터 말투, 어휘 수준, 감탄사, 문장 구조를 LLM으로 생성한다(§4.3). |
| output | `VoiceProfile` — 고유한 목소리 프로필 |
| 평가 전략 | **Manual** |

```
execute(data, inputs):
  prompt = buildVoicePrompt(
    l1: inputs.L1,
    character: inputs.Character,
    customInstructions: data.customInstructions
  )
  result = LLM_Sonnet.generate(prompt, schema: VoiceProfileSchema)

  return {
    speechPatterns: result.speechPatterns,   // ["솔직히 말하자면,", "이건 좀 아이러니한데,"]
    vocabulary: result.vocabulary,            // { level, preferredWords[], avoidWords[] }
    sentenceStructure: result.sentenceStructure, // { avgLength, style, insertionClauses }
    exclamations: result.exclamations,        // ["허,", "글쎄요,", "크흠,"]
    dialect: result.dialect                   // null | { region, markers[] }
  } as VoiceProfile
```

**⑬ activity-gen — Activity Inference (활동 추론)**

| 항목 | 값 |
|------|-----|
| data | — (설정 없음) |
| inputs | `L1: SocialPersonaVector`, `Character: CharacterData` |
| 로직 | L1 벡터와 캐릭터 특성으로부터 8가지 활동 성향을 **규칙 기반**으로 추론한다. LLM 호출 없음. |
| output | `ActivityConfig` — 활동 성향 프로필 |
| 평가 전략 | Eager (규칙 기반, 비용 0) |

```
execute(data, inputs):
  L1 = inputs.L1
  char = inputs.Character

  return {
    initiative:     clamp(L1.sociability × 0.6 + L1.stance × 0.4, 0, 1),
    expressiveness: clamp(L1.depth × 0.5 + L1.scope × 0.3 + L1.sociability × 0.2, 0, 1),
    interactivity:  clamp(L1.sociability × 0.7 + L1.purpose × 0.3, 0, 1),
    contentCreation:clamp(L1.depth × 0.4 + L1.taste × 0.3 + L1.purpose × 0.3, 0, 1),
    curiosity:      clamp(L1.taste × 0.5 + L1.scope × 0.3 + L1.depth × 0.2, 0, 1),
    consistency:    clamp((1 - char.dialogueStyle.variability ?? 0.5) × 0.7 + L1.purpose × 0.3, 0, 1),
    emotionalRange: clamp(L1.lens × 0.3 + (1 - L1.lens) × 0.3 + L1.sociability × 0.4, 0, 1),
    adaptability:   clamp(L1.sociability × 0.5 + (1 - L1.stance) × 0.5, 0, 1)
  } as ActivityConfig
```

**⑭ content-gen — Content Style Generator (콘텐츠 스타일)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` |
| inputs | `V_Final: VFinalResult`, `Character: CharacterData` |
| 로직 | V_Final과 캐릭터 특성으로부터 콘텐츠 취향, 리뷰 스타일, 장르 친화도를 LLM으로 생성한다. |
| output | `ContentSettings` — 콘텐츠 스타일 설정 |
| 평가 전략 | **Manual** |

```
execute(data, inputs):
  prompt = buildContentStylePrompt(
    vFinal: inputs.V_Final.vector7D,
    character: inputs.Character,
    customInstructions: data.customInstructions
  )
  result = LLM_Sonnet.generate(prompt, schema: ContentSettingsSchema)

  return {
    genreAffinities: result.genreAffinities,   // [{ genre, affinity, reason }]
    reviewStyle: result.reviewStyle,             // { tone, depth, structure }
    contentPreferences: result.contentPreferences, // { formats[], themes[], avoidTopics[] }
    recommendationBias: result.recommendationBias  // 추천 편향 설정
  } as ContentSettings
```

**⑮ pressure-gen — Pressure Context Generator (압박 맥락 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` |
| inputs | `L3: NarrativeDriveVector`, `Paradox: ParadoxResult` |
| 로직 | L3의 volatility/lack + 역설 구조로부터 트리거 조건, 오버라이드 규칙, 감쇠 설정을 LLM으로 생성한다(§5.4). |
| output | `PressureContext` — 압박 맥락 + 트리거 규칙 |
| 평가 전략 | **Manual** |

```
execute(data, inputs):
  prompt = buildPressureContextPrompt(
    l3: inputs.L3,
    paradoxPairs: inputs.Paradox.paradoxPairs,
    extendedScore: inputs.Paradox.extendedScore,
    customInstructions: data.customInstructions
  )
  result = LLM_Sonnet.generate(prompt, schema: PressureContextSchema)

  return {
    triggers: result.triggers,     // [{ triggerId, keywords[], context, effects[] }] (§5.4 Stage 1~2)
    decayRules: result.decayRules, // 복귀 곡선 파라미터 (§5.4 Stage 3)
    overrideRules: result.overrideRules,     // 벡터 오버라이드 규칙
    quirksDefinition: result.quirksDefinition // 습관 발현 규칙 (§5.6)
  } as PressureContext
```

**⑯ zeitgeist-gen — Zeitgeist Profile Generator (시대정신 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ customInstructions? }` |
| inputs | `BasicInfo: BasicInfoData` |
| 로직 | BasicInfo의 인구통계(세대, 지역)로부터 시대정신, 핵심 가치관, 문화적 원형을 생성한다(§4.4). |
| output | `ZeitgeistProfile` — 시대정신 프로필 |
| 평가 전략 | **Manual** |

```
execute(data, inputs):
  prompt = buildZeitgeistPrompt(
    demographics: inputs.BasicInfo.demographics,
    role: inputs.BasicInfo.role,
    customInstructions: data.customInstructions
  )
  result = LLM_Sonnet.generate(prompt, schema: ZeitgeistProfileSchema)

  return {
    generation: result.generation,             // "MILLENNIAL", "GEN_Z", etc.
    coreValues: result.coreValues,             // ["지적 성실성", "예술적 진정성"]
    culturalArchetype: result.culturalArchetype, // "SAGE", "REBEL", etc.
    ethicalStance: result.ethicalStance          // { 환경: "...", 공정성: "..." }
  } as ZeitgeistProfile
```

#### 14.8.4 Assembly 노드 (2종)

Assembly 노드는 Generation 노드들의 출력을 **하나의 통합 구조**로 조립한다.

**⑰ prompt-builder — Prompt Builder (프롬프트 조립)**

| 항목 | 값 |
|------|-----|
| data | `{ templateOverride? }` — 프롬프트 템플릿 커스터마이즈 (선택) |
| inputs | `Character: CharacterData`, `Voice: VoiceProfile`, `Backstory: BackstoryDimension`, `Pressure: PressureContext`, `Zeitgeist: ZeitgeistProfile`, `Content: ContentSettings` |
| 로직 | 6개 Generation 출력을 섹션별로 조립하여 LLM 시스템 프롬프트를 구성한다. 이 프롬프트가 페르소나의 "영혼"이 된다. |
| output | `PromptSet` — 시스템 프롬프트 + 벡터 컨텍스트 |
| 평가 전략 | Eager (조립만, LLM 호출 없음) |

```
execute(data, inputs):
  template = data.templateOverride ?? DEFAULT_PROMPT_TEMPLATE

  systemPrompt = template.build({
    // §1: 정체성
    identity: {
      character: inputs.Character,
      backstory: inputs.Backstory,
      zeitgeist: inputs.Zeitgeist
    },
    // §2: 행동 규칙
    behavior: {
      voice: inputs.Voice,
      content: inputs.Content,
      pressureTriggers: inputs.Pressure.triggers,
      quirks: inputs.Pressure.quirksDefinition
    },
    // §3: 동적 지침
    dynamics: {
      overrideRules: inputs.Pressure.overrideRules,
      decayRules: inputs.Pressure.decayRules
    }
  })

  return {
    systemPrompt,                          // 완성된 시스템 프롬프트 텍스트
    vectorContext: {                        // 런타임에 주입될 벡터 참조
      backstoryInitDeltas: inputs.Backstory.initDeltas,
      pressureTriggerIds: inputs.Pressure.triggers.map(t => t.triggerId),
      quirkIds: inputs.Pressure.quirksDefinition.map(q => q.id)
    }
  } as PromptSet
```

**⑱ interaction-rules — Interaction Rules (상호작용 규칙 조립)**

| 항목 | 값 |
|------|-----|
| data | — (설정 없음) |
| inputs | `Backstory: BackstoryDimension`, `Pressure: PressureContext`, `V_Final: VFinalResult` |
| 로직 | §5의 4대 연결 로직(Init/Override/Adapt/Express)의 런타임 규칙을 조립한다. |
| output | `InteractionRules` — 런타임 상호작용 규칙 세트 |
| 평가 전략 | Eager (조립만) |

```
execute(data, inputs):
  return {
    // ① Init (§5.3): 서사→벡터 초기 보정
    initConfig: {
      deltas: inputs.Backstory.initDeltas,
      clampRange: 0.4   // ±0.4 최대 보정
    },

    // ② Override (§5.4): 트리거→벡터 강제 변동
    overrideConfig: {
      triggers: inputs.Pressure.triggers,
      overrideRules: inputs.Pressure.overrideRules,
      decayRules: inputs.Pressure.decayRules
    },

    // ③ Adapt (§5.5): 유저 태도→실시간 보정
    adaptConfig: {
      baseVector: inputs.V_Final.vector7D,
      adaptabilityRate: inputs.V_Final.alpha,  // 기본 적응률
      driftClamp: 0.3,    // 기본값 대비 ±0.3 최대 이동
      momentumWindow: 3    // 최근 3턴 모멘텀 계산
    },

    // ④ Express (§5.6): 벡터 상태→습관 발현
    expressConfig: {
      quirks: inputs.Pressure.quirksDefinition,
      // 각 quirk: { id, stateKey, threshold, sensitivity, promptInstruction, cooldown }
    }
  } as InteractionRules
```

#### 14.8.5 Output 노드 (4종)

**⑲ consistency — Consistency Check (6-Category 일관성 검증)**

| 항목 | 값 |
|------|-----|
| data | — (설정 없음) |
| inputs | 전체 데이터 (DAG의 모든 결과를 입력으로 받음) |
| 로직 | §11의 6-Category 검증(A~F)을 실행한다. 모든 검증 항목의 통과율을 가중 합산하여 종합 점수를 산출한다. |
| output | `ValidationResult` — 검증 결과 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  errors = [], warnings = [], infos = []

  // ─── A: 구조적 검증 (15%) ───
  // 벡터 범위, 필수 필드, α+β=1.0
  validateStructure(inputs) → errors/warnings

  // ─── B: L1↔L2 역설 검증 (20%) ───
  // 매핑 일관성, Paradox Score 범위
  validateParadox(inputs.Paradox, inputs.Archetype) → warnings

  // ─── C: L2↔L3 정합성 검증 (20%) ───
  // C1: lack↔ParadoxScore, C2: volatility↔neuroticism
  // C3: scope↔openness, C4: moralCompass↔agreeableness
  validateL2L3Coherence(inputs.L2, inputs.L3, inputs.Paradox) → warnings/infos

  // ─── D: 정성적↔정량적 검증 (20%) ───
  // D1: 서사↔Init 벡터, D2: Voice↔L1, D3: Triggers↔L3
  validateQualitative(inputs.Backstory, inputs.Voice, inputs.Pressure, inputs.L1, inputs.L3) → warnings

  // ─── E: 교차축 일관성 검증 (15%) ───
  // E1: 스코어 범위, E2: 관계 유형별 분포, E3: EPS 내부 일관성
  validateCrossAxis(inputs.Paradox.crossAxisProfile) → errors/warnings

  // ─── F: 동적 설정 검증 (10%) ───
  // pressureRange, pressureDecay 유효성
  validateDynamics(inputs.PressureConfig, inputs.ProjectionConfig) → errors

  // ─── 종합 점수 (§11.7) ───
  categoryScores = { A: passRate, B: passRate, C: passRate, D: passRate, E: passRate, F: passRate }
  score = 0.15×A + 0.20×B + 0.20×C + 0.20×D + 0.15×E + 0.10×F

  return {
    score,             // 0.0 ~ 1.0
    passed: score ≥ 0.7 AND errors.length == 0,
    errors,            // Error 수준 항목
    warnings,          // Warning 수준 항목
    infos,             // Info 수준 항목
    categoryScores     // 카테고리별 통과율
  } as ValidationResult
```

**⑳ fingerprint — Fingerprint Generator (P-inger Print 생성)**

| 항목 | 값 |
|------|-----|
| data | `{ mode?: 'compact' \| 'l1-l2' \| 'full' }` |
| inputs | `L1: SocialPersonaVector`, `L2?: CoreTemperamentVector`, `L3?: NarrativeDriveVector`, `Paradox?: ParadoxResult`, `P?: PressureConfig` |
| 로직 | §12의 3종 컴포넌트(레이더/2D 지문/3D Jacks)를 결정론적으로 생성한다. |
| output | `FingerprintProfile` — 3종 시각화 데이터 |
| 평가 전략 | Eager |

```
execute(data, inputs):
  seed = SHA256(persona_id + version + timestamp)
  mode = data.mode ?? 'full'

  // ─── 다층 레이더 차트 (§12.3) ───
  radarData = {
    l1Axes: inputs.L1,                         // 외곽 원: 7축
    l2Axes: (mode != 'compact') ? inputs.L2 : null,  // 중간 원: 5축
    l3Axes: (mode == 'full') ? inputs.L3 : null,      // 내곽 원: 4축
    paradoxConnectors: inputs.Paradox?.paradoxPairs.filter(p => p.score > 0.5)
  }

  // ─── 2D 지문 소용돌이 (§12.4~12.5) ───
  print2d = generate2DFingerprint(
    l1: inputs.L1,                  // 외부 릿지 구조
    l2: inputs.L2,                  // 내부 릿지 질감
    l3: inputs.L3,                  // 중심 패턴
    vFinalColor: inputs.P,          // 색상 온도
    seed,
    rules: SIX_FIXED_RULES          // §12.5 6대 고정 규칙
  )

  // ─── 3D Jacks 오브젝트 (§12.6) ───
  print3d = generate3DJacks(
    l1Arms: 7,                      // L1 7개 팔
    l2Arms: 5,                      // L2 5개 팔
    l3Arms: 4,                      // L3 4개 팔 (맥동 애니메이션)
    values: { l1: inputs.L1, l2: inputs.L2, l3: inputs.L3 },
    seed
  )

  return {
    radarData,
    print2d: { canonical: print2d.svg, display: print2d.displaySvg },
    print3d: print3d.config,
    seed
  } as FingerprintProfile
```

**㉑ test-sim — Test Simulation (테스트 시뮬레이션)**

| 항목 | 값 |
|------|-----|
| data | `{ testScenarios?: TestScenario[], sampleCount?: number }` |
| inputs | `PromptSet: PromptSet`, `Character: CharacterData` |
| 로직 | 완성된 프롬프트로 테스트 시나리오를 실행하고, 페르소나의 응답 품질을 평가한다. |
| output | `TestResult` — 테스트 결과 |
| 평가 전략 | **Manual** (LLM 호출 비용) |

```
execute(data, inputs):
  scenarios = data.testScenarios ?? DEFAULT_TEST_SCENARIOS
  // 기본 시나리오: 일상 대화, 비판적 질문, 트라우마 트리거, 칭찬, 논쟁

  results = []
  for each scenario in scenarios:
    // LLM에 시스템 프롬프트 + 테스트 유저 메시지 전달
    response = LLM_Sonnet.generate(
      systemPrompt: inputs.PromptSet.systemPrompt,
      userMessage: scenario.userMessage
    )

    // 응답 품질 평가 (LLM 자기 평가)
    evaluation = LLM_Sonnet.evaluate(
      response,
      criteria: {
        characterConsistency: "캐릭터 설정과 응답이 일관되는가",
        voiceConsistency: "말투/어휘가 Voice 프로필과 일치하는가",
        paradoxExpression: "역설적 특성이 자연스럽게 드러나는가",
        pressureResponse: "압박 상황에서 적절히 반응하는가"
      }
    )

    results.push({ scenario, response, evaluation })

  return {
    scenarios: results,
    overallScore: average(results.map(r => r.evaluation.overall)),
    sampleOutputs: results.map(r => r.response)
  } as TestResult
```

**㉒ deploy — Deploy (배포)**

| 항목 | 값 |
|------|-----|
| data | `{ targetEnvironment?: 'staging' \| 'production' }` |
| inputs | 전체 데이터 (DAG의 모든 결과) |
| 로직 | 최종 검증 후 페르소나를 DB에 저장하고 활성화한다. Consistency Check가 통과(≥0.7)해야 실행 가능. |
| output | — (사이드 이펙트: DB 저장 + 페르소나 활성화) |
| 평가 전략 | **Manual** (명시적 배포 행위) |

```
execute(data, inputs):
  // ─── 사전 검증 ───
  validation = inputs.Consistency
  if (validation.passed == false):
    throw DeployError("일관성 검증 미통과 (score: {validation.score})")

  if (validation.errors.length > 0):
    throw DeployError("Error 수준 검증 항목 {count}개 존재")

  // ─── 페르소나 객체 조립 ───
  persona = assemblePersona({
    basicInfo: inputs.BasicInfo,
    vectors: { l1: inputs.L1, l2: inputs.L2, l3: inputs.L3 },
    dynamics: inputs.ProjectionConfig,
    paradox: inputs.Paradox,
    character: inputs.Character,
    backstory: inputs.Backstory,
    voice: inputs.Voice,
    activity: inputs.Activity,
    content: inputs.Content,
    pressure: inputs.PressureContext,
    zeitgeist: inputs.Zeitgeist,
    promptSet: inputs.PromptSet,
    interactionRules: inputs.InteractionRules,
    fingerprint: inputs.Fingerprint,
    validation: inputs.Consistency,
    testResult: inputs.TestResult
  })

  // ─── 저장 + 활성화 ───
  env = data.targetEnvironment ?? 'staging'
  await savePersona(persona, env)
  if (env == 'production'):
    await activatePersona(persona.id)

  return { deployed: true, personaId: persona.id, environment: env }
```

#### 14.8.6 노드 실행 요약

| # | 노드 | 타입 | 핵심 로직 | 평가 전략 | LLM |
|---|------|------|-----------|-----------|-----|
| ① | basic-info | Input | 데이터 패스스루 | Eager | — |
| ② | l1-vector | Input | 7D 벡터 검증 | Eager | — |
| ③ | l2-vector | Input | 5D 벡터 검증 | Eager | — |
| ④ | l3-vector | Input | 4D 벡터 검증 | Eager | — |
| ⑤ | archetype-select | Input | 템플릿 로드 + 분산 적용 | Eager | — |
| ⑥ | paradox-calc | Engine | 확장 역설 점수 + 83축 교차 스코어 | Eager | — |
| ⑦ | pressure-ctrl | Engine | 감쇠 상수 + 압박 범위 설정 | Eager | — |
| ⑧ | v-final | Engine | V_Final = (1-P)·L1 + P·(α·L2_proj + β·L3_proj) | Eager | — |
| ⑨ | projection | Engine | α/β + 임계값 + 적응률 설정 | Eager | — |
| ⑩ | character-gen | Generation | 성격 특성 + 행동 패턴 생성 | Manual | Sonnet |
| ⑪ | backstory-gen | Generation | 서사적 기원 + Init delta 추출 | Manual | Sonnet |
| ⑫ | voice-gen | Generation | 말투 + 어휘 + 감탄사 생성 | Manual | Sonnet |
| ⑬ | activity-gen | Generation | 8가지 활동 성향 추론 | Eager | — |
| ⑭ | content-gen | Generation | 콘텐츠 취향 + 리뷰 스타일 | Manual | Sonnet |
| ⑮ | pressure-gen | Generation | 트리거 + 오버라이드 + 습관 규칙 | Manual | Sonnet |
| ⑯ | zeitgeist-gen | Generation | 세대정신 + 가치관 + 문화 원형 | Manual | Sonnet |
| ⑰ | prompt-builder | Assembly | 6개 Generation 출력 → 시스템 프롬프트 | Eager | — |
| ⑱ | interaction-rules | Assembly | Init/Override/Adapt/Express 규칙 조립 | Eager | — |
| ⑲ | consistency | Output | 6-Category 검증 (A~F) | Eager | — |
| ⑳ | fingerprint | Output | 레이더 + 2D 지문 + 3D Jacks | Eager | — |
| ㉑ | test-sim | Output | 테스트 시나리오 실행 + 품질 평가 | Manual | Sonnet |
| ㉒ | deploy | Output | 최종 검증 + DB 저장 + 활성화 | Manual | — |

### 14.9 분기 노드 (Control Flow Nodes)

#### 14.9.1 설계 동기

§14.8의 22개 노드는 DAG에서 **모든 노드가 무조건 실행**되는 구조이다. 그러나 실제 페르소나 설계에서는 **조건에 따라 다른 경로**를 택해야 하는 경우가 빈번하다:

- **역설 강도별 분기**: Paradox Score가 높은 페르소나에게는 깊은 캐릭터 생성, 낮으면 간결한 생성
- **아키타입별 라우팅**: 아키타입 유형에 따라 서로 다른 프롬프트 템플릿 사용
- **레이어 유무 분기**: L3 벡터가 없으면 L1↔L2만으로 계산, 있으면 3-Layer 전체
- **검증 결과별 분기**: 일관성 점수에 따라 자동 배포 / 수동 리뷰 / 재생성 경로 선택

이를 위해 **Control Flow** 카테고리의 3개 노드를 추가한다:

| 노드 | 역할 | 출력 포트 |
|------|------|-----------|
| **Conditional** | if-then-else — 하나의 조건 평가 → 2개 분기 | True, False |
| **Switch** | 다중 분기 — 값 범위/열거형 매칭 → N개 분기 | Case1, Case2, ..., CaseN |
| **Merge** | 분기 합류 — 여러 경로의 결과를 하나로 합침 | 단일 출력 |

#### 14.9.2 DAG 평가 엔진 확장

기존 `evaluateGraph`(§14.5)는 모든 노드를 무조건 실행한다. 분기를 지원하기 위해 **활성 엣지(Active Edge)** 개념을 도입한다.

**핵심 변경:**
1. 입력 노드(진입점)에서 나가는 모든 엣지는 **기본 활성**
2. 일반 노드가 실행되면, 해당 노드에서 나가는 모든 엣지가 **활성**
3. Conditional/Switch 노드가 실행되면, **조건에 맞는 출력 포트의 엣지만 활성**
4. 노드는 **하나 이상의 활성 입력 엣지가 있어야** 실행됨. 없으면 **스킵**
5. 스킵된 노드 하위의 모든 노드도 연쇄적으로 스킵 (활성 엣지 없음)

```
평가 순서 (확장):
  sorted = topologicalSort(graph)
  activeEdges = Set()
  executionPath = []

  for each nodeId in sorted:
    node = graph.nodes[nodeId]
    incomingEdges = node의 입력 엣지들

    // 입력 엣지 중 활성인 것이 없으면 스킵
    if (incomingEdges 존재 AND 활성 입력 엣지 없음):
      executionPath.push({ nodeId, executed: false, reason: "비활성 경로" })
      continue

    // 활성 엣지에서만 입력 수집
    inputs = collectInputsFromActiveEdges(node, activeEdges, results)

    // 노드 실행
    output = executeNode(node.type, node.data, inputs)
    results.set(nodeId, output)

    // 출력 엣지 활성화 결정
    outgoingEdges = node에서 나가는 엣지들
    if (node.type == 'conditional'):
      branch = output.branchTaken  // 'true' | 'false'
      branch 포트의 엣지만 활성화
    elif (node.type == 'switch'):
      activeCases = output.activeCases  // ['case_high'] 등
      해당 case 포트의 엣지만 활성화
    else:
      모든 출력 엣지 활성화

    executionPath.push({ nodeId, executed: true, branch? })
```

**실행 경로 추적**: 모든 노드의 실행/스킵 여부, 분기 결정 이유가 `executionPath`에 기록된다. UI에서 이를 시각적으로 표시한다 (활성 엣지는 굵게, 비활성은 회색 점선).

#### 14.9.3 Conditional Node (조건 분기)

하나의 값을 평가하여 True/False 두 경로로 분기한다.

| 항목 | 값 |
|------|-----|
| 카테고리 | **Control Flow** |
| data | `{ conditionType, operator?, threshold?, rangeMin?, rangeMax?, enumValue?, trueLabel?, falseLabel? }` |
| 입력 포트 | `value: Any` — 평가할 값 (숫자, 문자열, 불리언) |
| 출력 포트 | `true: Any`, `false: Any` — 조건 결과에 따른 분기 |
| 평가 전략 | Eager |

**조건 유형:**

| conditionType | 평가 방식 | 예시 |
|---------------|-----------|------|
| `threshold` | `value [operator] threshold` | `extendedScore > 0.6` |
| `range` | `rangeMin ≤ value ≤ rangeMax` | `0.3 ≤ consistency ≤ 0.7` |
| `enum` | `value == enumValue` | `archetypeId == 'SAGE'` |
| `exists` | `value != null && value != undefined` | L3 벡터 존재 여부 |

```
execute(data, inputs):
  value = inputs.value
  conditionMet = false

  switch (data.conditionType):
    case 'threshold':
      conditionMet = evaluate(value, data.operator, data.threshold)
    case 'range':
      conditionMet = (value >= data.rangeMin AND value <= data.rangeMax)
    case 'enum':
      conditionMet = (value == data.enumValue)
    case 'exists':
      conditionMet = (value != null)

  return {
    branchTaken: conditionMet ? 'true' : 'false',
    reason: describeCondition(data, value),
    value: value   // 원본 값을 분기 경로로 패스스루
  }
```

> **패스스루**: Conditional 노드는 입력 값을 **변환하지 않고** 그대로 선택된 분기로 전달한다. 이렇게 하면 분기 후 노드가 원래 타입의 데이터를 그대로 사용할 수 있다.

#### 14.9.4 Switch Node (다중 분기)

하나의 값을 여러 구간/케이스로 분류하여 N개 경로 중 하나로 분기한다.

| 항목 | 값 |
|------|-----|
| 카테고리 | **Control Flow** |
| data | `{ switchMode, bands?, enumCases?, defaultCaseId? }` |
| 입력 포트 | `selector: Any` — 분류할 값 |
| 출력 포트 | `case_[id]: Any` (동적 — 케이스 수만큼) |
| 평가 전략 | Eager |

**분류 모드:**

| switchMode | 설명 | 출력 포트 예시 |
|------------|------|----------------|
| `threshold-band` | 숫자 구간 분류 | case_low (0~0.33), case_med (0.33~0.66), case_high (0.66~1.0) |
| `enum-match` | 열거형 매칭 | case_sage, case_hero, case_caregiver, ... |

```
execute(data, inputs):
  value = inputs.selector
  activeCases = []

  switch (data.switchMode):
    case 'threshold-band':
      for each band in data.bands:
        if (value >= band.min AND value < band.max):
          activeCases.push(band.id)
          break
      // 경계값 처리: 1.0이 마지막 band에 포함
      if (activeCases.empty AND value == lastBand.max):
        activeCases.push(lastBand.id)

    case 'enum-match':
      if (value in data.enumCases):
        activeCases.push(data.enumCases[value].id)

  // 매칭 없으면 default
  if (activeCases.empty AND data.defaultCaseId):
    activeCases.push(data.defaultCaseId)

  return {
    activeCases,
    selectedValue: value,
    caseLabel: describeCaseMatch(data, activeCases)
  }
```

#### 14.9.5 Merge Node (분기 합류)

여러 분기 경로의 결과를 하나로 합친다. Conditional/Switch로 갈라진 경로가 다시 만나는 지점에서 사용한다.

| 항목 | 값 |
|------|-----|
| 카테고리 | **Control Flow** |
| data | `{ mergeStrategy?: 'first-active' \| 'combine' }` |
| 입력 포트 | `input_1: Any`, `input_2: Any`, ... (다중 입력, `multi: true`) |
| 출력 포트 | `merged: Any` — 합류된 결과 |
| 평가 전략 | Eager |

**합류 전략:**

| mergeStrategy | 설명 |
|---------------|------|
| `first-active` (기본) | 활성 입력 중 첫 번째 값을 출력. Conditional 분기 합류에 적합 (항상 하나만 활성) |
| `combine` | 모든 활성 입력을 배열로 합쳐 출력. 병렬 생성 결과 수집에 적합 |

```
execute(data, inputs):
  activeInputs = inputs에서 null/undefined가 아닌 값들

  if (data.mergeStrategy == 'combine'):
    return { merged: activeInputs }
  else:  // 'first-active'
    return { merged: activeInputs[0] }
```

#### 14.9.6 포트 타입 확장

분기 노드를 위해 포트 타입 시스템(§14.4)에 다음을 추가한다:

| 포트 타입 | 용도 | 호환성 |
|-----------|------|--------|
| `Any` | 범용 — 모든 타입의 데이터 수용 | 모든 기존 타입과 호환 |

> **설계 결정**: `ConditionalBranch`나 `SwitchCase` 같은 별도 타입을 두지 않는다. 대신 `Any` 타입 하나로 통일하여, 분기 노드가 **어떤 데이터든 받아서 그대로 패스스루**하는 구조를 취한다. 이렇게 하면 기존 22개 노드의 포트 시스템을 수정하지 않아도 된다.

**호환성 규칙 추가:**
```
PORT_COMPATIBILITY에 추가:
  Any: [모든 기존 PortType]
  각 기존 PortType에 'Any' 추가
```

#### 14.9.7 그래프 검증 확장

분기 도입에 따라 그래프 검증(§13.8)에 다음 규칙을 추가한다:

| 검증 규칙 | 수준 | 설명 |
|-----------|------|------|
| **분기 합류 필수** | Warning | Conditional/Switch에서 갈라진 경로가 Merge 없이 같은 노드에 연결되면 경고 |
| **데드엔드 없음** | Warning | 분기 경로 중 Deploy까지 도달하지 못하는 경로가 있으면 경고 |
| **도달 가능성** | Error | 어떤 Input에서도 도달할 수 없는 노드가 있으면 오류 |
| **Switch 기본 케이스** | Info | Switch에 default 케이스가 없으면 참고 표시 (매칭 안 되면 전체 스킵) |

#### 14.9.8 유즈케이스

**① 역설 강도별 캐릭터 생성 분기**

```
[L1] → [L2] → [Paradox Calculator] → extendedScore
                                          ↓
                                    [Conditional: > 0.6?]
                                    /                \
                                 True              False
                                 /                    \
                    [Character Gen          [Character Gen
                     (깊은 역설 강조)]        (단순 특성)]
                                 \                    /
                                    [Merge]
                                      ↓
                                [Prompt Builder] → [Deploy]
```

역설 점수가 높으면 복잡한 캐릭터 생성, 낮으면 간결한 생성. 같은 프롬프트 빌더로 합류.

**② 아키타입별 프롬프트 라우팅**

```
[Archetype Select] → archetypeId
                          ↓
                    [Switch: enum-match]
                    /      |       \
                 SAGE    HERO   CAREGIVER
                 /        |        \
          [Sage      [Hero     [Caregiver
           Voice]     Voice]     Voice]
                 \        |        /
                     [Merge]
                       ↓
                 [Prompt Builder]
```

아키타입마다 특화된 Voice 생성 프롬프트를 사용. 같은 Voice 타입으로 합류.

**③ L3 유무에 따른 계산 분기**

```
[L1] → [L2] → [L3?]
                 ↓
           [Conditional: L3 exists?]
           /                \
        True              False
        /                    \
[Paradox 3-Layer]    [Paradox L1↔L2 only]
        \                    /
           [Merge]
             ↓
          [V_Final]
```

L3 벡터가 없으면 L1↔L2만으로 역설 계산. 하위 호환성의 분기적 구현.

**④ 검증 결과별 배포 경로**

```
[Consistency Check] → score
                        ↓
                  [Switch: threshold-band]
                  /        |          \
               POOR     FAIR       GOOD
               (< 0.7)  (0.7~0.9)  (≥ 0.9)
               /          |           \
          [재생성      [수동       [자동
           경고]      리뷰 플래그]   Deploy]
```

일관성 점수에 따라 배포 경로가 달라짐. 낮으면 재생성 권고, 높으면 자동 배포.

#### 14.9.9 노드 카테고리 최종 정리

분기 노드 추가로 전체 노드는 6개 카테고리, **25종**이 된다:

| 카테고리 | 노드 수 | 노드 목록 |
|----------|---------|-----------|
| **Input** | 5 | basic-info, l1-vector, l2-vector, l3-vector, archetype-select |
| **Engine** | 4 | paradox-calc, pressure-ctrl, v-final, projection |
| **Control Flow** | 3 | **conditional**, **switch**, **merge** |
| **Generation** | 7 | character-gen, backstory-gen, voice-gen, activity-gen, content-gen, pressure-gen, zeitgeist-gen |
| **Assembly** | 2 | prompt-builder, interaction-rules |
| **Output** | 4 | consistency, fingerprint, test-sim, deploy |

---

## 15. PersonaWorld RAG — 페르소나의 장기 기억

### 15.1 설계 동기

LLM은 본질적으로 **stateless** — 매 호출마다 이전 대화를 기억하지 못한다. 이로 인해 아무리 정교하게 벡터와 역설을 설계해도 다음과 같은 한계가 발생한다:

- **Voice drift**: 10턴 이상 대화 시 페르소나 고유의 어투가 "일반적 AI 어투"로 회귀
- **기억 단절**: 어제 화가 난 상태였던 걸 오늘 기억하지 못함
- **관계 망각**: 다른 페르소나와 싸웠던 이력을 모름
- **성장 불가**: 3개월에 걸친 growthArc 변화를 실제 출력에 반영할 수 없음

그런데 DeepSight에는 이 문제를 해결할 **데이터가 이미 존재**한다 — PersonaWorld.

### 15.2 PersonaWorld = 페르소나의 자전적 기록

```
PersonaWorld DB에 축적되는 데이터:
├─ PersonaPost          ← 본인이 쓴 포스트 (Voice의 실증 데이터)
├─ PersonaComment       ← 본인이 단 댓글 (대화 패턴의 실증 데이터)
├─ PersonaPostLike      ← 좋아요한 콘텐츠 (관심사의 실증 데이터)
├─ PersonaRepost        ← 리포스트 (공유 성향의 실증 데이터)
├─ PersonaFollow        ← 팔로우/팔로워 (관계망의 실증 데이터)
├─ PersonaActivityLog   ← 모든 활동 로그 (행동 패턴의 시계열 데이터)
└─ ConsumptionLog       ← 비공개 소비 기록 (봤지만 포스트 안 쓴 컨텐츠)
```

이 데이터는 페르소나가 **"실제로 어떻게 말하고 행동해왔는지"** 의 기록이다. 이것을 매 LLM 호출 시 컨텍스트로 주입(RAG: Retrieval-Augmented Generation)하면, LLM이 페르소나의 **장기 기억과 일관성**을 유지할 수 있다.

특히 **ConsumptionLog**는 공개 활동과 기억을 분리하는 핵심 레이어이다. 현실에서 사람은 소비한 컨텐츠의 극히 일부만 리뷰하며, 나머지는 "봤지만 굳이 글은 안 쓴" 상태로 기억에 남는다. ConsumptionLog가 없으면 페르소나가 기억을 유지하기 위해 모든 소비에 대해 포스트를 작성해야 하고, 이는 봇처럼 보이는 부자연스러운 활동 패턴을 만든다.

### 15.3 RAG 컨텍스트 구성

매 LLM 호출 시 다음 5가지를 검색하여 프롬프트에 주입한다:

```
┌─────────────────────────────────────────────────────┐
│  [A] 시스템 프롬프트 (고정)                ~2,000 tok │
│      벡터, 역설, Voice 정의                           │
├─────────────────────────────────────────────────────┤
│  [B] Voice 앵커 (검색)                   ~500 tok   │
│      최근 포스트/댓글 5~10개                          │
│      → "이 페르소나가 실제로 쓴 글"이 few-shot 역할    │
│      → Voice drift 방지의 핵심                       │
├─────────────────────────────────────────────────────┤
│  [C] 관계 기억 (조건부 검색)              ~800 tok   │
│      현재 상호작용 대상과의 최근 대화 이력              │
│      → "어제 이 사람과 논쟁했음"을 기억                │
│      → 관계 톤 연속성 유지                            │
├─────────────────────────────────────────────────────┤
│  [D] 관심사 연속성 (검색)                 ~100 tok   │
│      최근 좋아요/리포스트 주제 요약                    │
│      → "이번 주 독립영화에 관심 많음" 반영             │
├─────────────────────────────────────────────────────┤
│  [E] 소비 기억 (검색)                    ~200 tok   │
│      비공개 소비 기록 중 현재 컨텍스트 관련 항목        │
│      → "기묘한 이야기 시즌 3까지 봤고, 시즌 2가 최고"  │
│      → 포스트 없이도 경험 기반 대화 가능               │
├─────────────────────────────────────────────────────┤
│  [F] 현재 컨텍스트 + 유저 입력 (변동)     ~500 tok   │
└─────────────────────────────────────────────────────┘
총: ~4,100 tok (기존 2,500 대비 +1,600 tok, +64%)
```

### 15.4 해결되는 문제

| 기존 한계 | PersonaWorld RAG 적용 후 | 메커니즘 |
|-----------|------------------------|----------|
| Voice drift (10턴+) | 자기 과거 글이 few-shot 앵커 → 어투 유지 | [B] Voice 앵커 |
| 세션 간 기억 단절 | 최근 활동 로그 검색 → "어제 이런 일 있었지" | [B]+[C] |
| 관계 망각 | 상호작용 이력 검색 → 관계 톤 유지 | [C] 관계 기억 |
| 관심사 단절 | 최근 좋아요 주제 → 연속적 관심사 | [D] 관심사 |
| **소비↔기억 괴리** | **포스트 없이도 소비 기록으로 경험 참조 가능** | **[E] 소비 기억** |
| growthArc 반영 | 초기 글 vs 최근 글 톤 비교 → 성장 방향 유지 | [B] 시계열 비교 |
| 트라우마 은연중 표현 | 과거 트리거 반응 이력 → 패턴 학습 | [C] 이력 검색 |

### 15.5 비용 영향

| 항목 | RAG 없음 | RAG 적용 |
|------|----------|----------|
| 호출당 input 토큰 | ~2,500 | ~4,100 (+64%) |
| 페르소나 1개 월 운영비 | ~82원 | **~134원** |
| DB 검색 오버헤드 | 없음 | ~10-50ms/호출 |

> **월 52원 추가로 Voice drift, 관계 기억, 소비 기억, 연속성 문제 대부분 해결**

---

## 16. 품질 피드백 루프

### 16.1 설계 동기

v3 시스템은 설계만으로 **~70%**, PersonaWorld RAG + 고급 모델 스위칭으로 **~80%** 품질을 달성할 수 있다. 그러나 80% → 85%+ 는 **실데이터 기반 피드백 루프**를 통해서만 가능하다.

"무조건 실데이터구현으로만 테스트"라는 원칙에 따라, 피드백 루프는 v3 아키텍처의 **핵심 구조**로 설계한다.

### 16.2 피드백 루프 전체 구조

```
[콘텐츠 생성]
      ↓
[유저 소비 & 피드백]
  ├─ 명시적: 좋아요/싫어요, "어색했다" 신고
  └─ 암묵적: 이탈율, 재방문, 대화 지속 턴 수
      ↓
[품질 측정 엔진]
  ├─ 역설 표현 스코어 (자동 평가)
  ├─ Voice 일관성 스코어 (과거 글과 비교)
  ├─ Pressure 반응 자연스러움 (구간별 평가)
  └─ 유저 만족도 (명시적+암묵적 종합)
      ↓
[패턴 분석]
  ├─ "어떤 역설 유형에서 실패하는가?"
  ├─ "몇 턴 후 Voice drift가 발생하는가?"
  ├─ "어떤 Pressure 구간에서 부자연스러운가?"
  └─ "어떤 아키타입이 품질이 낮은가?"
      ↓
[개선 적용]
  ├─ Few-shot 라이브러리 갱신
  ├─ Pressure 구간별 프롬프트 튜닝
  ├─ Voice reminder 주입 시점 최적화
  ├─ 아키타입 템플릿 파라미터 조정
  └─ 모델 티어 재조정
      ↓
[재생성/재검증] → [반복]
```

### 16.3 품질 측정 엔진 — 4대 자동 평가 지표

#### 역설 표현 스코어 (Paradox Expression Score)

페르소나의 응답이 설계된 역설을 실제로 표현하고 있는지 자동 평가한다.

```
측정 방법:
  1. 페르소나의 L1↔L2 역설 쌍 중 Paradox가 큰 상위 3개 추출
     예: stance(0.8) ↔ agreeableness(0.9) → paradox=0.7

  2. 생성된 텍스트를 경량 모델(mini)로 분석:
     "이 텍스트에서 '비판적이면서 공감적인' 뉘앙스가 표현되었는가?"
     → 0.0(전혀 없음) ~ 1.0(매우 잘 표현됨)

  3. 역설이 큰 페르소나일수록 높은 표현 스코어가 기대됨
     ParadoxScore=0.7인데 ExpressionScore=0.2 → 경고
```

#### Voice 일관성 스코어 (Voice Consistency Score)

생성된 텍스트가 페르소나의 과거 글과 얼마나 어투가 일관되는지 측정한다.

```
측정 방법:
  1. 페르소나의 최근 글 10개에서 특징 추출:
     - 평균 문장 길이, 감탄사 사용 빈도, 어휘 수준
     - 말버릇(speechPatterns) 출현 횟수

  2. 새로 생성된 글에서 동일 특징 추출

  3. 두 특징 벡터의 코사인 유사도 계산
     유사도 < 0.6 → Voice drift 경고
```

#### Pressure 반응 자연스러움 (Pressure Response Score)

P가 변할 때 응답 톤의 변화가 자연스러운지 측정한다.

```
측정 방법:
  1. 같은 페르소나에게 P=0.1, 0.4, 0.7, 1.0으로 동일 질문
  2. 4개 응답의 톤 변화를 분석:
     - P=0.1→0.4: 미세한 변화 기대
     - P=0.4→0.7: 뚜렷한 변화 기대
     - P=0.7→1.0: 극적 변화 기대
  3. 변화량이 단조 증가하지 않으면 → 부자연스러움 경고
     (예: P=0.7에서 갑자기 차분해지면 이상)
```

#### 유저 만족도 (User Satisfaction Score)

```
명시적 신호:
  - LIKE/DISLIKE 피드백 비율
  - "어색했다" 신고 건수
  - 대화 중 "뭔 소리야" 등 부정적 반응 감지

암묵적 신호:
  - 대화 지속 턴 수 (길수록 만족)
  - 재방문율 (같은 페르소나에게 다시 오는가)
  - 이탈 시점 (몇 턴에서 떠나는가)
```

### 16.4 개선 자동화 수준

| 단계 | 자동화 | 내용 | 목표 시점 |
|------|--------|------|-----------|
| **1단계** | 수동 | 품질 지표 대시보드 확인 → 수동 프롬프트 수정 | v3 출시 |
| **2단계** | 반자동 | 문제 패턴 자동 감지 → 알림 → 수동 수정 | v3.1 |
| **3단계** | 자동화 | Few-shot 자동 수집/교체, 프롬프트 A/B 자동 실행 | v3.2 |

### 16.5 Few-shot 라이브러리 자동 구축

피드백 루프의 가장 구체적인 산출물. "역설을 잘 표현한 응답"을 자동 수집하여 향후 프롬프트의 few-shot 예시로 활용한다.

```
수집 기준:
  - Paradox Expression Score ≥ 0.8
  - Voice Consistency Score ≥ 0.7
  - 유저 LIKE 피드백

저장 구조:
  역설 유형별 분류:
  ├─ stance↔agreeableness (까칠+따뜻) → 예시 10개
  ├─ sociability↔extraversion (사교적+내향) → 예시 10개
  ├─ lens↔neuroticism (논리+불안) → 예시 10개
  └─ ...

활용:
  해당 역설 유형의 페르소나가 콘텐츠 생성 시,
  같은 유형의 few-shot 예시 2~3개를 프롬프트에 주입
```

### 16.6 Auto-Interview 프로토콜 — 행동적 일관성 검증

§11의 일관성 검증은 **구조적 검증**(벡터 범위, 레이어 정합성)에 집중한다. 그러나 "벡터가 유효하다"와 "페르소나가 실제로 그 벡터대로 행동한다"는 다른 문제이다.

Auto-Interview는 페르소나 생성 직후, 시스템이 자동으로 **질문 세트**를 던지고 답변의 벡터 일치도를 검증하는 프로토콜이다.

#### 프로토콜 흐름

```
[페르소나 생성 완료]
      ↓
[질문 생성] — LLM Medium으로 페르소나의 벡터에 맞는 20개 질문 생성
  ├─ L1 검증 질문 (7개): 각 L1 차원을 자극하는 상황 제시
  ├─ L2 검증 질문 (5개): 기질/성격이 드러나는 딜레마 상황
  ├─ L3 검증 질문 (4개): 동기/도덕/변동성이 드러나는 서사적 질문
  └─ 역설 검증 질문 (4개): L1↔L2 역설이 표현되는지 확인하는 갈등 상황
      ↓
[답변 생성] — 페르소나로 20개 질문에 답변 생성
      ↓
[벡터 역추론] — LLM Medium으로 답변에서 벡터값 역추론
  "이 답변의 주인공은 다음 차원에서 어느 정도의 값을 가질 것 같은가?"
  → 역추론된 벡터 (추정값)
      ↓
[일치도 계산] — 설계 벡터 vs 역추론 벡터 비교
  InterviewConsistency = cosineSim(designVector, inferredVector)
      ↓
[판정]
  ≥ 0.85: 통과 (행동적 일관성 우수)
  0.70~0.85: 경고 (일부 차원에서 괴리)
  < 0.70: 실패 → 프롬프트 재조정 필요
```

#### 질문 설계 원칙

각 질문은 **특정 차원을 자극하는 상황**을 제시하고, 답변에서 해당 차원의 값을 추론할 수 있도록 설계한다.

| 타겟 차원 | 질문 예시 | 기대 행동 (값이 높을 때) |
|-----------|-----------|------------------------|
| L1.stance | "친구가 대중적으로 인기 있는 영화를 강력 추천합니다. 어떻게 반응하나요?" | 비판적 분석, 독자적 의견 표현 |
| L1.sociability | "파티에 초대받았지만 아는 사람이 없습니다. 어떻게 하나요?" | 적극적 참여, 새로운 사람과 대화 시도 |
| L2.neuroticism | "중요한 프레젠테이션이 내일인데 준비가 부족합니다." | 불안, 걱정, 과도한 시나리오 상상 |
| L2.agreeableness | "동료가 명백히 잘못된 의견을 고집합니다." | 상대 감정 배려, 우회적 설득 |
| L3.moralCompass | "큰 이득을 볼 수 있지만 약간 비윤리적인 제안을 받았습니다." | 도덕적 원칙 준수, 거절 |
| L3.volatility | "예상치 못한 나쁜 소식을 갑자기 들었습니다." | 감정적 반응, 기복, 즉각적 표현 |
| 역설(stance↔agree) | "존경하는 사람이 당신의 핵심 가치와 반대되는 주장을 합니다." | 비판적이면서도 공감하는 복합 반응 |

#### 비용 및 실행 빈도

```
비용 산출:
  - 질문 생성: Medium 1회 ≈ 15원
  - 답변 생성: 페르소나 20회 ≈ 60원 (Heavy 3원 × 20)
  - 역추론: Medium 1회 ≈ 15원
  - 합계: ~90원/페르소나

실행 빈도:
  - 페르소나 최초 생성 시: 필수
  - 프롬프트 재조정 후: 필수
  - 정기: 아키타입별 월 1회 샘플링
```

### 16.7 Persona Integrity Score — 런타임 무결성 지표

§16.3의 4대 지표가 **개별 응답 품질**을 측정한다면, Persona Integrity Score는 **장기적 캐릭터 유지**를 측정한다. 대화가 길어질수록 "설정 붕괴(Character Collapse)"가 발생하는지 감지하는 지표다.

#### 3대 구성 요소

```
PersonaIntegrityScore = w₁ × ContextRecall + w₂ × SettingConsistency + w₃ × CharacterStability

w₁ = 0.35 (서사 재현율)
w₂ = 0.35 (설정 일관성)
w₃ = 0.30 (캐릭터 안정성)
```

##### ① Context Recall (서사 재현율)

페르소나의 배경 서사(Backstory)에 포함된 핵심 설정 요소가 대화 중에 얼마나 유지되는지 측정한다.

```
측정 방법 (LLM-as-Judge):
  1. backstory에서 핵심 설정 요소 추출 (LLM Medium):
     예: ["가난한 어린 시절", "인정 욕구", "논리적 평론가", "가족 트라우마"]

  2. 최근 N턴 대화에서 설정 요소의 반영 여부 평가 (LLM Light):
     "이 대화에서 '{설정 요소}'가 직접 또는 간접적으로 표현되었는가?"
     → reflected/not_reflected/contradicted

  3. ContextRecall = (reflected 수 + 0.5 × not_reflected 수) / 전체 설정 요소 수
     contradicted가 하나라도 있으면 페널티: ContextRecall × 0.8
```

##### ② Setting Consistency (설정 일관성 — 환각 탐지)

페르소나가 자신의 팩트 설정(나이, 출신, 직업 등)과 모순되는 발언을 하는지 탐지한다.

```
측정 방법 (LLM-as-Judge):
  1. 페르소나의 팩트 설정 목록 구성:
     { name, age, birthplace, occupation, ... }

  2. 매 N턴마다 (기본 10턴) 누적 대화를 검증:
     "이 대화에서 다음 설정과 모순되는 발언이 있는가?"
     → contradictions[] (모순 목록)

  3. SettingConsistency = 1.0 - (contradictions.length × 0.2)
     최소값 0.0 (5개 이상 모순이면 바닥)
```

##### ③ Character Stability (캐릭터 안정성)

대화가 길어질수록 페르소나의 톤/태도가 기본 설정에서 점진적으로 벗어나는 "드리프트(Drift)"를 측정한다.

```
측정 방법:
  1. 대화 초반(1~5턴)의 Voice 특징 벡터 추출 → V_early
  2. 대화 현재(최근 5턴)의 Voice 특징 벡터 추출 → V_current
  3. CharacterStability = cosineSim(V_early, V_current)

     1.0에 가까움: 안정적 캐릭터 유지
     0.8 이하: 드리프트 경고
     0.6 이하: 심각한 캐릭터 붕괴
```

#### Integrity Score 판정 기준

| 점수 범위 | 판정 | 조치 |
|-----------|------|------|
| ≥ 0.85 | 우수 | 유지 |
| 0.70 ~ 0.85 | 주의 | Voice reminder 주입 빈도 증가 |
| 0.55 ~ 0.70 | 경고 | 프롬프트 재조정 + Auto-Interview 재실행 |
| < 0.55 | 위험 | 대화 리셋 또는 페르소나 재생성 권장 |

#### 실행 빈도 및 비용

```
실행 시점:
  - 매 10턴마다 SettingConsistency 체크
  - 매 20턴마다 전체 Integrity Score 계산
  - 대화 종료 시 최종 Integrity Score 기록

비용:
  - ContextRecall: LLM Light 1회 ≈ 1원 (규칙 기반 가능)
  - SettingConsistency: LLM Light 1회 ≈ 1원
  - CharacterStability: 규칙 기반 (비용 0원)
  - 합계: ~2원/20턴 (매우 경량)
```

---

## 17. LLM 모델 전략

### 17.1 기본 원칙

- **최상위 모델**: Claude 3.5 Sonnet (Opus는 비용 대비 효과 불충분)
- **역설 표현이 필요한 상황에서는 반드시 Sonnet급 사용**
- **PersonaWorld RAG를 모든 호출에 적용** (Voice 앵커 + 관계 기억)
- **Prompt Caching을 적극 활용** (고정 프롬프트 부분 캐싱)

### 17.2 3-Tier 모델 구성

| Tier | 모델 | 용도 | 기준 |
|------|------|------|------|
| **Heavy** | Claude 3.5 Sonnet | 페르소나 생성, 리뷰, Paradox 필요 대화 | ParadoxScore > 0.5 or P > 0.4 |
| **Medium** | GPT-4o mini | 일반 포스트, 중간 복잡도 댓글 | ParadoxScore ≤ 0.5 and P ≤ 0.4 |
| **Light** | 규칙 기반 + Voice 템플릿 | 짧은 반응, 좋아요, 리포스트 | 1줄 이하 반응 |

### 17.3 동적 분기 조건

```
IF (persona.paradoxScore > 0.5) → Heavy
ELSE IF (context.pressure > 0.4) → Heavy
ELSE IF (context.triggerDetected) → Heavy
ELSE IF (context.conflictScore > 0.7) → Heavy
ELSE IF (task == 'persona-generation') → Heavy
ELSE IF (task == 'review-generation') → Heavy
ELSE IF (responseLength < 50 chars) → Light (규칙 기반)
ELSE → Medium
```

### 17.4 비용 요약

| 항목 | 페르소나당 |
|------|-----------|
| 생성 비용 (1회) | ~55원 (Sonnet) |
| 월 운영비 (중활동 + RAG) | **~127원** |
| 연간 운영비 | **~1,524원** |
| B2B 고객사 기준 (500개 운영) | **월 6.35만원** |

---

## 18. 기대 효과 (확장)

Section 7의 기대 효과에 추가하여, v3.0 전체 시스템의 확장된 기대 효과를 정리한다.

### 18.1 시각적 정체성

컬러지문(P-inger Print) 시스템을 통해 모든 페르소나가 **지문처럼 고유한 시각적 패턴**을 갖게 된다. 이는 사용자가 페르소나를 "숫자의 집합"이 아니라 "고유한 존재"로 인식하게 만드는 핵심 장치이다. 미래 스캐너 디코딩 가능 설계는 DeepSight의 기술적 차별성을 확보한다.

### 18.2 직관적 생성 경험

ComfyUI 스타일 노드 에디터는 복잡한 106D+ 벡터 + 서사 + 연결 로직의 설정을 **시각적 플로우**로 변환한다. 초보자는 프리셋으로 5분 만에, 고급 사용자는 노드 그래프로 원하는 수준의 커스터마이즈가 가능하다. 포트 타입 시스템이 잘못된 설정을 원천 차단한다.

### 18.3 확장 가능한 색상 체계

83개 교차축, 12+ 아키타입, 엔진 메타 개념까지 포괄하는 색상 체계는 UI의 모든 요소가 **일관된 시각 언어**로 소통하게 만든다. CIELAB+OKLCH 이중 색공간은 기계 정밀도와 인간 시각 경험을 동시에 만족시킨다.

### 18.4 생성 품질 보장

일관성 검증 시스템이 모든 페르소나의 내부 정합성을 자동 검사한다. 아키타입 템플릿 → 생성 파이프라인 → 검증의 자동화된 흐름이 "벡터는 맞는데 서사가 안 맞는" 같은 품질 문제를 사전에 차단한다.

---

## 19. 유저 프로파일링 시스템 v3

### 19.1 설계 동기

#### v2 시스템의 한계

v2 Cold Start는 6D(L1 6차원) 기반으로 설계되었다. LIGHT(12문항)/MEDIUM(30문항) 2단계 구조로, L1과 L2를 **분리된 질문**으로 각각 측정했다. 이로 인해:

1. **질문 수 비효율**: L2(OCEAN) 측정에만 18문항이 별도 필요 → 총 30문항 = 5분+ 소요
2. **이탈률 높음**: 30문항을 끝까지 완료하는 유저 비율이 낮음
3. **정밀도 한계**: L1과 L2를 독립 측정하므로 L1↔L2 상관관계(역설 패턴)를 감지 불가
4. **SNS 연동 미비**: SNS 분석 결과를 벡터에 반영하는 Init 연결이 개념 수준에 머무름
5. **온보딩 후 학습 부재**: 초기 프로필 생성 후 벡터가 갱신되지 않음

#### v3 설계 목표

- **하이브리드 질문**: 1문항으로 L1 + L2 동시 측정 → 질문 효율 2배
- **24문항으로 93%+ 매칭 정밀도** (SNS 없이도 달성)
- **Phase별 저장**: 중간 이탈 시 완료된 Phase까지 저장
- **데일리 마이크로 질문**: 온보딩 후에도 매일 1문항 → 정밀도 지속 상승
- **크레딧 보상**: 질문 답변에 PW 내부 화폐 지급 → 유저 참여 인센티브
- **SNS 연동 강화**: 플랫폼별 비용 최적화, Init 알고리즘 실연결
- **질문 풀 + AI 출제**: 사전 설계된 150문항 풀에서 AI가 적응형 선택, 풀 소진 시 LLM 생성 전환

### 19.2 프로파일링 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    유저 프로파일링 시스템 v3                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐                  │
│  │ Phase 1  │───▶│ Phase 2  │───▶│ Phase 3  │                  │
│  │ 8문항    │    │ 8문항    │    │ 8문항    │                  │
│  │ L1 주력  │    │ L2 주력  │    │ 교차검증 │                  │
│  │ ~65%     │    │ ~80%     │    │ ~93%     │                  │
│  └──────────┘    └──────────┘    └──────────┘                  │
│       │               │               │                        │
│       ▼               ▼               ▼                        │
│  ┌──────────────────────────────────────────┐                  │
│  │         유저 벡터 (L1 7D + L2 5D)         │                  │
│  └──────────────────────────────────────────┘                  │
│       ▲               ▲               ▲                        │
│       │               │               │                        │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐                  │
│  │ SNS 연동 │   │ 데일리   │   │ 활동기반 │                  │
│  │ Init     │   │ 마이크로 │   │ Adapt    │                  │
│  │ (1회성)  │   │ (매일)   │   │ (자동)   │                  │
│  └──────────┘   └──────────┘   └──────────┘                  │
│                                                                 │
│  관리: Engine Studio    │    유저 접점: PersonaWorld             │
└─────────────────────────────────────────────────────────────────┘
```

**데이터 흐름:**

1. **온보딩 (24문항)**: PersonaWorld에서 유저가 질문에 답변 → 벡터 초기화
2. **SNS 연동 (선택)**: 외부 SNS 데이터 → Init 알고리즘(§5.3) → 벡터 보정
3. **데일리 마이크로**: 매 로그인 시 1문항 → qualitativeAdapt(§5.5) → 벡터 미세조정
4. **활동 기반**: PW 활동 데이터(좋아요, 댓글, 체류 시간) → Adapt → 벡터 자동 보정
5. **엔진 스튜디오**: 질문 풀 관리, 매칭률 대시보드, 질문 효과 분석

### 19.3 하이브리드 시나리오 질문 설계

#### 핵심 원리

하이브리드 질문은 **하나의 시나리오에 4개의 선택지**를 제시하고, 각 선택지가 L1 축 1개 + L2 축 1개를 **동시에 측정**하는 구조이다.

```
┌─────────────────────────────────────────────────┐
│ 시나리오: "주말에 친구가 새로 개봉한 실험적인     │
│ 다큐멘터리를 보자고 제안합니다. 당신은?"          │
├─────────────────────────────────────────────────┤
│ A) "재밌겠다! 같이 가자"                         │
│    → L1.taste ↑실험(+0.3) + L2.openness ↑높음(+0.3)  │
│                                                   │
│ B) "일단 리뷰 좀 찾아보고 결정할게"               │
│    → L1.taste ↓보수(-0.1) + L2.conscientiousness ↑높음(+0.3) │
│                                                   │
│ C) "나는 좀 그런 건 별로인데... 넌 가봐"          │
│    → L1.taste ↓보수(-0.3) + L2.openness ↓낮음(-0.3) │
│                                                   │
│ D) "그것보다 우리 같이 볼 만한 다른 거 고르자"     │
│    → L1.sociability ↑높음(+0.2) + L2.agreeableness ↓중간(-0.1) │
└─────────────────────────────────────────────────┘
```

#### 벡터 매핑 규칙

각 선택지는 다음 구조를 가진다:

```typescript
interface ChoiceVectorMapping {
  l1Axis: L1Dimension;      // depth|lens|stance|scope|taste|purpose|sociability
  l1Delta: number;          // -0.4 ~ +0.4 (클램프)
  l2Axis: L2Dimension;      // openness|conscientiousness|extraversion|agreeableness|neuroticism
  l2Delta: number;          // -0.4 ~ +0.4 (클램프)
  confidence: number;       // 0.5 ~ 1.0 (해당 측정의 신뢰도)
}
```

**delta 적용 공식:**

```
새 벡터값 = 기존값 + (delta × confidence × adaptRate)

adaptRate:
  - Phase 1: 0.8 (초기 = 빠른 수렴)
  - Phase 2: 0.6 (중간 = 안정적 조정)
  - Phase 3: 0.4 (후기 = 미세 보정)
  - Daily:   0.2 (유지 = 최소 변동)
```

#### 4지선다의 정보량

- 선택지 4개 = log₂(4) = **2 bit/문항**
- 하이브리드: L1 축 1개 + L2 축 1개 = **2축 동시 측정**
- 1문항의 정보 밀도 = 2bit × 2축 = **4bit/문항** (단일 축 측정 대비 2배)

### 19.4 3-Phase 측정 모델 (8+8+8 = 24문항)

#### Phase 1: L1 주력 (8문항, ~80초)

| 문항 | L1 측정 축 | L2 부가 측정 | 시나리오 주제 |
|------|-----------|-------------|-------------|
| Q1 | depth | openness | 콘텐츠 깊이 선호 |
| Q2 | lens | conscientiousness | 평가 기준 |
| Q3 | stance | agreeableness | 의견 충돌 상황 |
| Q4 | scope | openness | 정보 탐색 범위 |
| Q5 | taste | openness | 새로운 장르 반응 |
| Q6 | purpose | conscientiousness | 콘텐츠 소비 동기 |
| Q7 | sociability | extraversion | 공유/토론 성향 |
| Q8 | depth+lens (교차) | neuroticism | 복합 반응 상황 |

**Phase 1 완료 시:**
- L1 7D: 각 축 최소 1회 측정 → 정밀도 ±0.25
- L2 5D: 부분 측정 (O, C, E 위주) → 정밀도 ±0.35
- **추정 매칭 신뢰도: ~65%**

#### Phase 2: L2 주력 (8문항, ~80초)

| 문항 | L2 측정 축 | L1 교차 검증 | 시나리오 주제 |
|------|-----------|-------------|-------------|
| Q9 | openness | taste | 변화에 대한 태도 |
| Q10 | conscientiousness | scope | 계획성 vs 즉흥성 |
| Q11 | extraversion | sociability | 사회적 에너지 |
| Q12 | agreeableness | stance | 갈등 해결 방식 |
| Q13 | neuroticism | lens | 스트레스 반응 |
| Q14 | openness+extraversion | purpose | 경험 추구 동기 |
| Q15 | conscientiousness+agreeableness | depth | 규범 vs 유연성 |
| Q16 | neuroticism+openness | taste | 불확실성 반응 |

**Phase 2 완료 시 (누적 16문항):**
- L1 7D: 축당 2~3회 교차 측정 → 정밀도 ±0.15
- L2 5D: 각 축 최소 2회 측정 → 정밀도 ±0.20
- **추정 매칭 신뢰도: ~80%**

#### Phase 3: 교차검증 + 역설 감지 (8문항, ~80초)

| 문항 | 측정 목적 | 대상 축 | 시나리오 주제 |
|------|----------|---------|-------------|
| Q17 | L1↔L2 역설 검증 | depth↔openness | 깊이와 개방성 괴리 |
| Q18 | L1↔L2 역설 검증 | stance↔agreeableness | 고집↔순응 괴리 |
| Q19 | L1↔L2 역설 검증 | sociability↔extraversion | 온라인↔오프라인 괴리 |
| Q20 | L1 교차 재검증 | depth+scope | Phase 1 결과 검증 |
| Q21 | L1 교차 재검증 | taste+purpose | Phase 1 결과 검증 |
| Q22 | L2 교차 재검증 | O+N | Phase 2 결과 검증 |
| Q23 | L2 교차 재검증 | C+E | Phase 2 결과 검증 |
| Q24 | 종합 역설 프로파일 | 전체 | 복합 딜레마 시나리오 |

**Phase 3 완료 시 (전체 24문항):**
- L1 7D: 축당 3~4회 측정 → 정밀도 ±0.10
- L2 5D: 축당 3~4회 측정 → 정밀도 ±0.10
- 역설 패턴 감지: L1↔L2 불일치 3쌍 검증
- **추정 매칭 신뢰도: ~93%**

#### 정밀도 수렴 테이블

| 누적 문항 | L1 정밀도 | L2 정밀도 | 역설 감지 | 매칭 신뢰도 |
|----------|----------|----------|----------|-----------|
| 8 (Phase 1) | ±0.25 | ±0.35 | 불가 | ~65% |
| 16 (Phase 2) | ±0.15 | ±0.20 | 부분 | ~80% |
| 24 (Phase 3) | ±0.10 | ±0.10 | 3쌍 검증 | ~93% |
| 30 (Daily +6) | ±0.09 | ±0.09 | 누적 | ~95% |
| 50 (Daily +26) | ±0.07 | ±0.07 | 정밀 | ~97% |
| 100+ | ±0.05 | ±0.05 | 포화 | ~98%+ |

### 19.5 이탈 정책 + 프로필 품질 등급

#### 이탈 정책

**핵심 규칙: Phase 단위 저장.** Phase를 완료하지 못하면 해당 Phase의 응답은 리셋된다.

```
Phase 내 이탈 (예: Q5에서 이탈)
  → Phase 1 미완료 → Phase 1 응답 전체 삭제
  → 다음 로그인 시 Phase 1 처음부터 다시 시작

Phase 간 이탈 (예: Phase 1 완료 후 Q10에서 이탈)
  → Phase 1 데이터 저장 유지
  → Phase 2 응답만 리셋
  → 다음 로그인 시 Phase 2부터 이어서 진행
```

**UX 안내 문구 (Phase 완료 시):**

```
"Phase 1 완료! 지금까지의 정보로 65% 정확도의 매칭이 가능합니다.
더 정확한 매칭을 원하시면 Phase 2를 계속해보세요. (+15% 향상)"
```

#### 프로필 품질 등급

| 등급 | 조건 | 매칭 신뢰도 | 배지 |
|------|------|-----------|------|
| **STARTER** | Phase 1 완료 (8문항) | ~65% | 🌱 |
| **STANDARD** | Phase 2 완료 (16문항) | ~80% | ⭐ |
| **ADVANCED** | Phase 3 완료 (24문항) | ~93% | 💎 |
| **EXPERT** | 24문항 + Daily 30회+ | ~97%+ | 🏆 |

등급은 PersonaWorld 프로필에 표시되며, 높은 등급일수록 매칭 결과의 상위 노출 가중치가 부여된다.

### 19.6 데일리 마이크로 질문 + 크레딧

#### 데일리 마이크로 질문

24문항 온보딩 완료 후, 매 로그인 시 **1문항**을 추가 출제한다.

```
출제 프로세스:
  1. 유저의 현재 벡터에서 uncertainty가 가장 높은 축 선택
  2. 해당 축을 측정하는 질문을 질문 풀에서 검색
  3. 미출제 + 해당 축 관련 → 우선순위 정렬
  4. 최상위 질문 출제
  5. 응답 → qualitativeAdapt(§5.5) → 벡터 미세조정

uncertainty 계산:
  uncertainty(axis) = 1 / (측정 횟수 × 평균 confidence)
  → 측정이 적거나 confidence가 낮은 축이 우선 출제됨
```

**질문 풀 구조:**

```
전체 150문항:
├─ 온보딩 24문항 (Phase 1/2/3 고정 배정)
├─ 추가 풀 126문항 (Daily 출제용)
│   ├─ L1 집중: 42문항 (축당 6문항)
│   ├─ L2 집중: 30문항 (축당 6문항)
│   ├─ L1↔L2 교차: 36문항 (주요 조합 12쌍 × 3문항)
│   └─ 역설 심화: 18문항 (역설 패턴 6종 × 3문항)
└─ 풀 소진 시: LLM 동적 생성으로 전환 (§19.8)
```

#### 크레딧 시스템 (PW 내부 화폐)

```
보상 구조:
  온보딩 Phase 1 완료 (8문항): 100 코인
  온보딩 Phase 2 완료 (8문항): 150 코인
  온보딩 Phase 3 완료 (8문항): 200 코인
  데일리 마이크로 1문항 답변:   10 코인
  7일 연속 답변 보너스:         50 코인
  30일 연속 답변 보너스:        200 코인
```

코인 사용처는 PersonaWorld 서비스 확장에 따라 추가 정의된다 (§ TBD).
- 프로필 꾸미기, 특별 페르소나 DM, 프리미엄 콘텐츠 열람 등 내부 서비스 해금에 활용
- 외부 결제 없이 참여만으로 획득 가능한 순환 경제 구조

### 19.7 SNS 연동 프로파일링

SNS 연동은 질문과 **독립적으로** 유저 벡터를 추정하는 보조 경로이다. 온보딩 질문을 대체하지 않으며, 두 소스의 결과를 **가중 평균**으로 병합한다.

#### 플랫폼별 분석 비용 (Sonnet 기준, 유저 1명)

| 카테고리 | 플랫폼 | 추출 데이터 | 토큰 추정 | 비용 | 매핑 차원 |
|---------|--------|-----------|----------|------|----------|
| 텍스트 기반 | X/Twitter | 트윗, RT, 좋아요 | ~30K tok | ~$0.10 | L1.lens, L1.stance, L2.extraversion |
| 텍스트 기반 | Instagram | 캡션, 해시태그, 스토리 | ~35K tok | ~$0.12 | L1.sociability, L2.extraversion, L2.openness |
| 텍스트 기반 | Threads | 포스트, 답글 | ~25K tok | ~$0.08 | L1.stance, L1.lens, L2.agreeableness |
| 텍스트 기반 | 블로그(네이버) | 본문, 카테고리 | ~50K tok | ~$0.20 | L1.depth, L1.scope, L1.purpose |
| 미디어 기반 | YouTube | 제목+설명+자막 | ~60K tok | ~$0.25 | L1.scope, L1.depth, L2.openness |
| 미디어 기반 | YouTube Music | 플레이리스트 메타 | ~15K tok | ~$0.05 | L1.taste, L2.openness |
| 미디어 기반 | Spotify | 장르+아티스트 메타 | ~15K tok | ~$0.05 | L1.taste, L2.openness |
| 미디어 기반 | Netflix | 시청 기록 메타 | ~20K tok | ~$0.08 | L1.taste, L1.depth, L1.purpose |

#### SNS + 질문 병합 공식

```
최종 벡터(axis) = w_q × 질문벡터(axis) + w_s × SNS벡터(axis)

가중치 결정:
  w_q = questionConfidence(axis)   // 질문 측정 신뢰도 (0~1)
  w_s = snsConfidence(axis)        // SNS 추출 신뢰도 (0~1)
  정규화: w_q + w_s = 1.0

SNS 신뢰도 기준:
  - 데이터 양: 포스트 100개+ → confidence 0.8
  - 데이터 양: 포스트 10~99개 → confidence 0.5
  - 데이터 양: 포스트 <10개 → confidence 0.3
  - 플랫폼 수: 2개+ 연동 시 교차 검증 보너스 +0.1
```

#### 비용 최적화

```
2-Stage 분석:
  Stage 1: Haiku로 1차 분류 (~$0.01/유저)
    → SNS 텍스트에서 키워드/패턴 추출
    → 명확한 차원은 바로 매핑

  Stage 2: Sonnet으로 정밀 분석 (나머지만)
    → 애매한 차원, 역설 패턴만 정밀 분석
    → 비용 40~60% 절감

결과:
  유저당 평균 비용: $0.12 → ~$0.06 (2-Stage 적용 시)
```

### 19.8 적응형 질문 선택 알고리즘

#### Phase 1~3 (온보딩): 고정 배정

온보딩 24문항은 **사전 설계된 고정 질문**을 순서대로 출제한다. 질문 간 축 커버리지가 최적화되어 있으므로 적응형 선택이 불필요하다.

#### Daily (25문항+): 적응형 선택

```typescript
function selectDailyQuestion(
  userProfile: UserVector,
  answeredQuestionIds: string[],
  questionPool: Question[]
): Question {
  // 1. 축별 불확실도 계산
  const uncertainties = calculateAxisUncertainties(userProfile);

  // 2. 가장 불확실한 축 상위 3개 선택
  const targetAxes = uncertainties
    .sort((a, b) => b.uncertainty - a.uncertainty)
    .slice(0, 3);

  // 3. 해당 축을 측정하는 미출제 질문 필터링
  const candidates = questionPool.filter(q =>
    !answeredQuestionIds.includes(q.id) &&
    targetAxes.some(a =>
      a.axis === q.l1Axis || a.axis === q.l2Axis
    )
  );

  // 4. 정보 이득 기대값 순으로 정렬
  const ranked = candidates
    .map(q => ({
      question: q,
      expectedGain: calculateExpectedInfoGain(q, uncertainties)
    }))
    .sort((a, b) => b.expectedGain - a.expectedGain);

  // 5. 최상위 질문 반환 (풀 소진 시 LLM 생성 fallback)
  if (ranked.length > 0) {
    return ranked[0].question;
  }
  return generateQuestionWithLLM(targetAxes[0]);
}
```

#### LLM 동적 생성 (풀 소진 시)

질문 풀 126문항이 소진된 후(약 4개월) LLM이 새 질문을 생성한다:

```typescript
async function generateQuestionWithLLM(
  targetAxis: AxisUncertainty
): Promise<Question> {
  const prompt = `
    다음 조건의 4지선다 시나리오 질문을 생성하세요:
    - 측정 대상: L1.${targetAxis.l1} + L2.${targetAxis.l2}
    - 형식: 일상 시나리오 + 4개 선택지
    - 각 선택지에 l1Delta, l2Delta, confidence 포함
    - 기존 질문과 유사하지 않도록 (기존 질문 목록 제공)
  `;

  const generated = await llm.generate(prompt);

  // 품질 검증: 구조 유효성 + 벡터 매핑 합리성 체크
  if (validateGeneratedQuestion(generated)) {
    await saveToQuestionPool(generated, { source: 'llm', reviewed: false });
    return generated;
  }

  // 검증 실패 시 재생성 (최대 3회)
  return retryGenerate(targetAxis, maxRetries: 3);
}
```

### 19.9 질문 데이터 스키마

질문은 **코드가 아닌 DB 데이터**로 관리한다. Prisma 마이그레이션으로 테이블을 생성하고, 질문 데이터는 별도 SQL 파일로 제공한다.

#### 테이블 설계

```sql
-- 질문 마스터 테이블
CREATE TABLE profiling_questions (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phase           SMALLINT,               -- 1/2/3 = 온보딩, NULL = Daily 풀
  sequence        SMALLINT,               -- Phase 내 순서 (1~8), Daily는 NULL
  scenario_text   TEXT NOT NULL,           -- 시나리오 본문 (한국어)
  choices         JSONB NOT NULL,          -- 4개 선택지 배열
  -- choices 구조:
  -- [
  --   {
  --     "label": "A",
  --     "text": "재밌겠다! 같이 가자",
  --     "l1Axis": "taste",
  --     "l1Delta": 0.3,
  --     "l2Axis": "openness",
  --     "l2Delta": 0.3,
  --     "confidence": 0.8
  --   }, ...
  -- ]
  l1_primary_axis VARCHAR(20) NOT NULL,    -- 주 측정 L1 축
  l2_primary_axis VARCHAR(20) NOT NULL,    -- 주 측정 L2 축
  difficulty      SMALLINT DEFAULT 1,      -- 1=쉬움, 2=보통, 3=어려움
  category        VARCHAR(30),             -- 시나리오 카테고리 (일상/문화/관계/직장 등)
  source          VARCHAR(10) DEFAULT 'manual', -- manual | llm
  is_active       BOOLEAN DEFAULT true,
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  updated_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 유저 응답 테이블
CREATE TABLE user_profiling_answers (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL REFERENCES users(id),
  question_id     UUID NOT NULL REFERENCES profiling_questions(id),
  selected_choice SMALLINT NOT NULL,       -- 0~3 (A/B/C/D)
  phase           SMALLINT,                -- 응답 시점의 Phase (1/2/3/NULL=Daily)
  response_time_ms INTEGER,                -- 응답 소요 시간 (밀리초)
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, question_id)
);

-- 유저 프로필 상태 테이블
CREATE TABLE user_profiling_status (
  user_id         UUID PRIMARY KEY REFERENCES users(id),
  current_phase   SMALLINT DEFAULT 1,      -- 현재 진행 중인 Phase
  phase1_completed BOOLEAN DEFAULT false,
  phase2_completed BOOLEAN DEFAULT false,
  phase3_completed BOOLEAN DEFAULT false,
  quality_grade   VARCHAR(10) DEFAULT 'NONE', -- NONE/STARTER/STANDARD/ADVANCED/EXPERT
  daily_count     INTEGER DEFAULT 0,        -- 데일리 마이크로 누적 답변 수
  last_daily_at   TIMESTAMPTZ,              -- 마지막 데일리 질문 답변 시각
  coin_balance    INTEGER DEFAULT 0,        -- PW 내부 화폐 잔액
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  updated_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 유저 벡터 테이블 (L1+L2)
CREATE TABLE user_vectors (
  user_id         UUID PRIMARY KEY REFERENCES users(id),
  -- L1: 사회적 가면 벡터 (7D)
  l1_depth        REAL DEFAULT 0.5,
  l1_lens         REAL DEFAULT 0.5,
  l1_stance       REAL DEFAULT 0.5,
  l1_scope        REAL DEFAULT 0.5,
  l1_taste        REAL DEFAULT 0.5,
  l1_purpose      REAL DEFAULT 0.5,
  l1_sociability  REAL DEFAULT 0.5,
  -- L2: 본원적 기질 벡터 (5D, OCEAN)
  l2_openness          REAL DEFAULT 0.5,
  l2_conscientiousness REAL DEFAULT 0.5,
  l2_extraversion      REAL DEFAULT 0.5,
  l2_agreeableness     REAL DEFAULT 0.5,
  l2_neuroticism       REAL DEFAULT 0.5,
  -- 메타
  measurement_count JSONB,                -- 축별 측정 횟수 {"l1_depth": 3, ...}
  confidence_map    JSONB,                -- 축별 신뢰도 {"l1_depth": 0.85, ...}
  source_weights    JSONB,                -- 소스별 기여도 {"question": 0.7, "sns": 0.3}
  updated_at        TIMESTAMPTZ DEFAULT NOW()
);

-- SNS 연동 테이블
CREATE TABLE user_sns_connections (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL REFERENCES users(id),
  platform        VARCHAR(30) NOT NULL,    -- twitter/instagram/youtube/spotify/...
  platform_user_id VARCHAR(255),
  access_token_enc TEXT,                   -- 암호화된 토큰
  analysis_status VARCHAR(20) DEFAULT 'pending', -- pending/analyzing/completed/failed
  analyzed_at     TIMESTAMPTZ,
  vector_result   JSONB,                   -- 분석 결과 벡터
  confidence      REAL,                    -- 분석 신뢰도
  data_volume     INTEGER,                 -- 분석한 데이터 수 (포스트 수 등)
  cost_usd        REAL,                    -- 분석 비용
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(user_id, platform)
);

-- 코인 트랜잭션 테이블
CREATE TABLE user_coin_transactions (
  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id         UUID NOT NULL REFERENCES users(id),
  amount          INTEGER NOT NULL,        -- 양수=적립, 음수=사용
  reason          VARCHAR(50) NOT NULL,    -- phase1_complete/phase2_complete/phase3_complete/daily_answer/streak_7d/streak_30d/spend_*
  reference_id    UUID,                    -- 관련 엔티티 ID (질문ID, 서비스ID 등)
  balance_after   INTEGER NOT NULL,        -- 트랜잭션 후 잔액
  created_at      TIMESTAMPTZ DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_profiling_questions_phase ON profiling_questions(phase, sequence);
CREATE INDEX idx_profiling_questions_axes ON profiling_questions(l1_primary_axis, l2_primary_axis);
CREATE INDEX idx_user_answers_user ON user_profiling_answers(user_id, created_at);
CREATE INDEX idx_user_sns_user ON user_sns_connections(user_id, platform);
CREATE INDEX idx_coin_transactions_user ON user_coin_transactions(user_id, created_at);
```

### 19.10 매칭 정밀도 수렴 모델

#### 수학적 모델

유저 벡터의 정밀도는 **측정 횟수의 역제곱근**에 비례하여 수렴한다:

```
precision(n) = σ_base / √n

σ_base = 0.5 (측정 0회 시 기본 불확실도)
n = 해당 축의 누적 측정 횟수

예:
  n=1 → precision = 0.50 (매우 부정확)
  n=2 → precision = 0.35
  n=4 → precision = 0.25
  n=8 → precision = 0.18
  n=16 → precision = 0.13
  n=32 → precision = 0.09
```

#### 매칭 신뢰도 공식

```
매칭 신뢰도 = 1 - (평균 불확실도 / 최대 불확실도)

평균 불확실도 = Σ precision(axis_i) / 12  (12차원)
최대 불확실도 = 0.5 (측정 0회)

24문항 시:
  L1 축당 ~4회 측정, L2 축당 ~4회 측정
  평균 precision = 0.5/√4 = 0.25... 이건 아닌데

  실제 하이브리드 보정:
  24문항 × 2축/문항 = 48 측정 / 12차원 = 축당 평균 4회
  평균 precision = 0.5/√4 = 0.25
  ... 하지만 adaptRate 적용 + 교차검증으로 실질 precision은 ±0.10 수준

  이론치 대비 실질 정밀도 보정 계수 k = 0.4:
  실질 precision = k × σ_base / √n = 0.4 × 0.5 / √4 = 0.10
```

#### SNS 부스트

SNS 연동은 별도의 측정 소스로 **측정 횟수에 가산**된다:

```
SNS 1개 연동 = 해당 매핑 차원에 +3회 측정 효과 (confidence에 따라)
SNS 2개+ 연동 = 교차 검증 보너스 → +1회 추가

예: YouTube 연동 → L1.scope +3, L1.depth +3, L2.openness +3

24문항 + YouTube 연동 시:
  L1.depth: 질문 4회 + SNS 3회 = 7회 → precision ±0.08
  → 전체 매칭 신뢰도 ~95% (24문항 only 대비 +2%p)
```

#### 수렴 시뮬레이션

```
일자별 매칭 신뢰도 (24문항 완료 후 Daily 시작):

Day 0 (24문항):   ~93%
Day 7 (+7문항):   ~94.2%
Day 14 (+14문항): ~95.0%
Day 30 (+30문항): ~96.1%
Day 60 (+60문항): ~97.0%
Day 90 (+90문항): ~97.5%
Day 120 (+120문항): ~97.9% (풀 소진 임박, LLM 생성 전환)
Day 180+:         ~98%+ (수렴 포화)

SNS 2개 연동 시:
Day 0 (24문항+SNS): ~95.5%
Day 30 (+30문항):   ~97.2%
Day 90 (+90문항):   ~98.3%
```
